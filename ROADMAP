# ROADMAP - Phosphoric

## Vision
Phosphoric: create a cycle-accurate ORIC-1/Atmos emulator in C with modern features
including host filesystem sharing and conversion tools for legacy BASIC/machine code programs.

---

## Epic 7 — Assainissement architectural (FAIT — US1/US2/US4/US5 livrées ; US3 partiel : parser générique en backlog)

Suite de l'audit `docs/architecture/AUDIT-ARCHITECTURE.md` (2026-07-17). La dette
est **périphérique** (main.c god-object d'assemblage ; état émulé mêlé aux handles
hôte ; contrat `io_device_t` incomplet), pas centrale. On la traite par
**extraction incrémentale à iso-comportement** — chaque étape vérifiée
byte-identique, dans l'esprit du chantier `io_device_t` (Sprints 121-124). Ordre
= risque croissant ; on peut s'arrêter à n'importe quelle US.

### US 1 — Nettoyage à risque nul (Sprint 125) — DONE (v1.85.0-alpha)
- [x] Retirer les `#include "emulator.h"` inutilisés (`microdisc.c`,
      `loci_core.c` ; 0 usage vérifié).
- [x] `tap_strip_header_padding` était du **code mort** (0 usage) → supprimé
      plutôt qu'extrait. `parse_host_port` → `src/utils/netutil.c`.
      `g_running`+`signal_handler` → `src/utils/appsignal.c`.
- [x] Cible `release` (copie strippée `oric1-emu-release`, sans toucher au
      binaire de travail).
> Fait : -~100 L de main.c, couplage réduit. Iso-comportement golden 4/4, suite
> verte. Incident : un `sed` mal délimité a corrompu 2 fichiers → restaurés via
> `git checkout`, refaits avec Edit (leçon : pas de `sed` sur des lignes `#include`).

### US 2 — Extraire le bus I/O de main.c (Sprint 126) — DONE (v1.86.0-alpha)
- [x] Bloc du bus (6 wrappers `*_dev_*` + table `io_bus[]` + `io_bus_find`/
      `io_bus_find_write`, ~170 L) extrait vers `src/io/io_bus.c` +
      `include/io/io_bus.h`. `main.c` appelle les dispatch externes ;
      registration savestate via `io_bus_devices(&count)`.
> **Écart assumé** vs le plan (« dans chaque module ») : les wrappers ont besoin
> d'`emulator_t` (claims croisés) → les mettre dans `acia6551.c`/`microdisc.c`
> réintroduirait le couplage `emulator.h` retiré en US1. Le bon foyer est un
> **adaptateur unique** (`io_bus.c`, seul io/ à inclure emulator.h). Modules purs
> préservés. Iso-comportement golden 4/4 ; main.c 4690 → 4425 L.

### US 3 — Module argparse déclaratif (Sprint 127) — PARTIEL (v1.87.0-alpha)
- [x] **Données** extraites : enum `OPT_*` + table `long_options[]` +
      `CLI_SHORT_OPTIONS` → `include/cli/cli_options.h`. Iso-comportement par
      construction (relocation de données) ; golden 4/4 + `--help` + suite verte.
- [ ] **NON fait (assumé)** : le parser générique + `cli_args_t`. Mesure :
      63 `case` dont **43 à effets de bord** au parsing + ~89 variables locales →
      **non prouvable iso-comportement** avec le filet actuel. Livrer la part sûre
      plutôt que prétendre une garantie intenable.
> Backlog : élargir la couverture de tests CLI d'abord, puis extraire le parser
> par petits lots vérifiables. main.c 4425 → 4342 L.
- [~] **Prérequis EN COURS — filet de tests CLI** : `tests/integration/test_cli_parsing.sh`
      (cible `make test-cli-parsing`, **22/22**) verrouille le comportement **mesuré** du
      parser avant tout refactor. Couvre : `--help` (usage, rc 0), option inconnue
      **non-fatale** (rc 0), formats `CYCLES:VALUE` malformés → rc 1 message précis
      (`--dump-ram-at`/`--screenshot-at`/`--type-keys`), validation enum/borne **fatale**
      (`--serial`/`--model`/`--dtl2000` bogus, `--scale 99` → rc 1) vs **non-fatale**
      (`--joystick`/`--keyboard` bogus, `--acia-addr` hex invalide → rc 0), et surtout les
      **options à effets de bord** (fopen/load au parsing) avec leurs divergences de
      fatalité non évidentes : succès = artefact produit (`--trace`/`--profile` créent le
      fichier) ; ressource absente = **fatale** (`--disk`, `--symbols` → rc 1) **ou
      non-fatale** (`--tape` warning, `--load-state`, `--breakpoint` bad hex, `--trace`
      vers chemin non inscriptible → rc 0). Ces comportements sont capturés **tels quels** :
      un futur parser devra les reproduire ou les changer consciemment. Binaire inchangé
      (test seul). Le filet couvre maintenant les cas d'erreur + succès des principales
      familles d'options ; l'extraction du parser peut s'appuyer dessus, par petits lots.
- [x] **1re tranche d'extraction livrée (v1.91.1-alpha)** : le motif `CYCLES:FILE` dupliqué
      à l'identique entre `--dump-ram-at` et `--screenshot-at` factorisé dans
      `cli_split_cycles_file()`. **Iso-comportement PROUVÉ octet à octet** (sorties refactoré
      vs binaire d'avant reconstruit depuis git : dump 65536 o + screenshot 161295 o
      identiques) + test-cli-parsing 22/22. `--type-keys` (CYCLES:TEXT) laissé de côté
      (motif différent). Méthode validée pour la suite : petites tranches, chacune prouvée.
- [x] **2e tranche livrée (v1.91.2-alpha)** : motif `fopen` + « Cannot open --NAME file »
      + cleanup + `return 1`, identique entre `--trace-irq`/`--psg-trace`/`--audio-wav`,
      factorisé dans `cli_open_out(file, mode, optname)`. `--trace` (non-fatal, message
      différent) non touché. Iso-comportement PROUVÉ : échec (3 cas test-cli-parsing
      **25/25**, ajoutés AVANT le refactor) + succès (octet à octet `.psg`/`.wav`/`.irq`
      byte-identiques refactoré vs binaire d'avant).
- [x] **3e tranche livrée (v1.91.3-alpha)** : `parse_hex16(s)` remplace les 4 copies
      identiques de `(uint16_t)strtol(arg,NULL,16)` (`--acia-addr`/`--dtl2000-addr`/
      `--mageco-addr`/`--break`). Iso PROUVÉ : test-cli-parsing 25/25 + golden octet à octet
      (`--acia-addr 0500 --break C000`).
- **BORNE HONNÊTE atteinte** : les 3 tranches ci-dessus épuisent la duplication *propre*
      (motifs strictement identiques). Le reste est **couplé aux périphériques** (blocs
      dtl2000/mageco : warnings/erreurs/init spécifiques) → non factorisable sans couplage.
      Aller plus loin = le parser générique `cli_args_t`, qui reste **non prouvable
      iso-comportement** (filet à 25 cas ≠ couverture des 80 options × tous comportements).
      Recommandation : **s'arrêter là** pour US3 (part sûre livrée), ou d'abord étendre le
      filet à ~toutes les options avant d'oser le parser générique.

### US 4 — Savestate DTL2000/Mageco via séparation état/transport (Sprints 128-129) — DONE (v1.88.0-alpha)
- [x] Sections **« DTL »** / **« MAG »** via les hooks `io_device_t`.
      `dtl2000_save`/`load`, `mageco_save`/`load` dans leurs modules.
- [x] Séparation état/transport faite **à la (dé)sérialisation** (pas de découpage
      physique des structs cœur, jugé risqué et non nécessaire) : état émulé sauvé
      en blob, **14 pointeurs hôte de dtl2000_t / 7 de mageco_t préservés** au
      chargement. Garde par taille ; section émise seulement si présent → `.ost`
      normal inchangé. Transport non restauré (même-build, documenté).
- [x] Preuves : test-dtl2000 16/16 (+round-trip pointeurs sentinelles),
      test-midi 19/19, suite 941 → 943, golden 4/4, e2e réel `--dtl2000` +
      `--load-state` OK.
> Gain : dette structurelle #2 (savestate série) résorbée. Reste hors périmètre :
> savestate **LOCI** (handles OS, cf. US5 backlog).

### US 5 — Lifecycle `io_device_t` (Sprints 130-131) — DONE avec écarts assumés (v1.89.0-alpha)
- [x] Tick des périphériques déplacé de `cpu_cycle_tick` (main.c) vers
      `io_bus_tick(emu, cycles)` (io_bus.c), **ordre historique préservé** →
      iso-comportement par construction. VIA/cassette restent dans main.c.
- [x] save/load DTL2000/Mageco : faits en US4.
- [ ] **NON fait (assumé)** : boucle de tick générique sur `io_bus[]` (ordre ≠
      ordre de dispatch ; non prouvable byte-identique pour le timing série) ;
      hooks `reset`/`tick` dans le contrat (reset non uniforme → changerait F5 ;
      champ non câblable = poids mort). Contrat gardé limité à ce qui s'itère
      uniformément (claims/read/write/save/load).
> Gain : main.c ne connaît plus les détails de tick des périphériques.
> Bilan Epic 7 : main.c **4690 → 4334 L**, golden 4/4 à chaque étape, suite verte.

### Hors périmètre (assumé)
- `emulator_run()` en event-loop : risque temporel > bénéfice, **non planifié**.
- Savestate **LOCI** complet : bloqué par les handles OS (montages/descripteurs) ;
  au mieux une restauration *partielle* des montages, jamais du transport live.

---

### Sprint 134: Screenshot texte — `--screenshot-text` + `--screenshot-ansi` (v1.92.0-alpha) - DONE
Réponse à « existe-t-il un screenshot texte ? ». Constat **mesuré** avant tout code :
`video_export_ascii()` existait dans `export.c` mais **n'était branchée à aucune option
CLI** (appelée seulement par les tests) et produit une **image ANSI** (pixel → couleur de
fond), pas un dump texte ; le **contenu texte réel** de l'écran n'existait pas.
- `--screenshot-text FILE` : dumpe le **contenu texte réel** de l'écran (`$BB80`, 40×28)
  en ASCII lisible à la sortie. Nouvelle `video_export_screen_text(memory, fp)` : décode
  `octet & 0x7F` (masque le bit vidéo inverse), codes de contrôle/attributs (< 0x20) →
  espace, 40 col max/ligne, espaces de fin supprimés (rtrim).
- `--screenshot-ansi FILE` : image ANSI true-color du framebuffer à la sortie, via le
  wrapper fichier `video_export_ascii_file(vid, filename, sx, sy)` autour de l'existant.
- **Limite assumée/documentée** : suppose le charset standard ORIC (0x20-0x7F ≈ ASCII),
  un charset redéfini n'est pas résolu ; lit toujours les 28 lignes de `$BB80` quel que
  soit le mode (TEXT/HIRES).
Vérifié **par la mesure** (pas déduit) : boot Atmos 3M cycles → `ORIC EXTENDED BASIC V1.1`
/ `© 1983 TANGERINE` / `37631 BYTES FREE` / `CAPS` (le `©` sort en `` ` ``, code 0x60 du
charset ORIC = la limite ASCII documentée) ; ANSI ~264 Ko avec `ESC[48;2;R;G;Bm`.
Tests : `test-video` 16/16 (+2 : `test_ascii_export_file`, `test_screen_text_export` —
décodage inverse/contrôle + rtrim + paramètres NULL). `make tests` complet vert (exit 0).
**Fausse alerte tranchée par la mesure** : 7 échecs de `test-control-dispatch` observés
dans un `make tests` = **artefact de build incrémental** (objets SDL2/headless mélangés) —
`git stash` → 26/0 sur HEAD, `make clean` → 26/0 avec les changements. Pas une régression.
Builds natif + SDL2 OK.

---

### Sprint 133: AVI sonorisé — `--video` embarque une piste audio PCM (v1.91.0-alpha) - DONE
Suite de Sprint 132. Constat mesuré : `--video` produisait un MJPEG **muet** (1 flux
`vids`, aucun `auds`). Le recorder gagne un **2e flux `auds` PCM 16-bit** entrelacé.
- `avi_recorder_open_av(...audio_rate, channels)` : déclare le 2e `strl`/`auds` +
  `WAVEFORMATEX`, `dwStreams=2`, `hdrl` 192→294 ; `avi_recorder_open` = wrapper
  (rate 0 → sortie **byte-identique**). `avi_recorder_add_audio` écrit les chunks
  `01wb` ; `idx1` fourcc/flags par entrée ; back-patch `dwLength` audio à la fermeture.
- `main.c` : audio dans l'AVI **headless uniquement** (GUI = SDL possède `ay_generate`) ;
  génération audio par trame **consolidée** (un seul `ay_generate`/trame → WAV + AVI,
  jamais deux drains de la file d'événements PSG).
Vérifié (ffprobe/ffmpeg) : AVI PING = 2 flux (mjpeg 6,02 s + pcm_s16le 44100 stéréo),
audio extrait 265482 frames **non silencieux**, vidéo 301 trames OK, `dwLength` correct,
0 erreur conteneur. Tests : `test-avi` 9/9 (+2 : flux auds + dwLength ; non-régression
vidéo-seul), `test-audio-capture` 5/5 (+1 : AVI headless a `auds`+`01wb`).
`make tests` complet vert (exit 0 ; le harnais n'émet pas de total agrégé).
Builds natif + SDL2 OK.

---

### Sprint 132: Capture audio headless — `--audio-wav` + `--psg-trace` (v1.90.0-alpha) - DONE
Équivalent audio de `--screenshot-at` : valider le son PSG dans un run headless
automatique (mesuré, pas déduit). Motivé par le volet son du portage Spellbinder
(`~/spillbender`), encore à zéro : avoir l'outil de mesure **avant** d'implémenter.
- `--audio-wav FILE` : rend le PSG en PCM par trame (882 éch. @ 44,1 kHz) via
  `ay_generate` (le même moteur que la lecture SDL) → WAV 16-bit stéréo, en-tête
  backpatché à la fermeture. Headless uniquement (garde-fou : sans `--headless`,
  le callback SDL est déjà l'unique consommateur du générateur → exit 1).
- `--psg-trace FILE` : journal des écritures registre son AY (reg 0-13) + cycle CPU.
  Ports 14/15 (clavier) exclus ; reg 7 mixer conservé tel quel (mesuré, non filtré).
Preuves : `make test-audio-capture` (`tests/integration/test_audio_capture.sh`, 4/4)
— garde-fou + son ROM PING → WAV non silencieux + trace R0-R13 + déterminisme
(WAV byte-identique sur 2 runs). `make tests` complet vert (exit 0). Builds natif + SDL2 OK.

---

### Sprint 124: Hooks de sérialisation `io_device_t` — l'ULA-NG persiste dans le `.ost` (v1.84.0-alpha) - DONE
Extension du contrat au-delà du dispatch I/O. **Constat honnête** : les devices
bus (DTL2000, Mageco, LOCI, ULA-NG) n'avaient **aucune section savestate** — état
non persisté. Le hook corrige cette lacune, ULA-NG en premier.
- [x] Contrat (`io_device.h`) : `save_tag` + `save(emu,fp)` (retourne `false` →
      aucune section, `.ost` byte-identique si état par défaut) + `load(emu,fp,size)`.
- [x] Plomberie découplée : `savestate.c` ignore `io_bus` ; `main.c` la fournit
      via `savestate_set_io_devices()`. Save = une section par device à hook ;
      load route les tags non gérés en dur vers le `load` du device correspondant
      (tags vraiment inconnus toujours ignorés → compat ascendante).
- [x] ULA-NG (section « UNG ») : `ula_ng_save`/`load` dans le module. POD sans
      pointeur (30696 o) → blob + **garde par taille** au chargement = savestate
      *même-build* (quicksave/load F2/F4) ; section émise seulement si déverrouillée
      → `.ost` d'un boot normal inchangé.
- [x] Preuves : `test-savestate` +2 (round-trip `memcmp==0` ; verrouillée = pas
      de section, cible pré-existante non écrasée) 12/12 ; e2e réel `--ula-ng-poke`
      → `.ost` 96503 o **avec** UNG (30704 o) vs boot normal 65799 o **sans**.
- [x] Doc `docs/architecture/io-bus.md` §6.
> **backlog** : migrer DTL2000 et Mageco sur le même modèle (petits jeux de
> registres). **LOCI = réserve réelle** : ses handles de fichiers OS ne sont pas
> sérialisables tels quels (ne pas prétendre le contraire). Hooks reset/tick :
> non uniformes (reset F5 = CPU+LOCI seulement ; ordre de tick ≠ ordre de table).

---

### Sprint 123: ULA-NG migrée vers le bus — dispatch I/O = boucle + repli VIA (v1.83.0-alpha) - DONE
Dernier périphérique à plage encore câblé en dur. Sa migration a nécessité
d'**étendre le contrat `io_device_t`** (sa sémantique d'écriture ne rentrait pas
dans le contrat précédent, dit honnêtement).
- [x] `write` renvoie désormais `bool` « consommé » (au lieu de `void`) : une
      écriture peut décliner et retomber sur le VIA. Périphériques à plage
      exclusive → toujours `true` ; ULA-NG → verdict de `ula_ng_write`
      (verrouillée, octet neutre non consommé → repli VIA bit-à-bit).
- [x] `claims_write` optionnel (NULL → `claims`) : l'ULA-NG voit les écritures de
      sa fenêtre même verrouillée (guet 'N','G') alors que ses lectures ne
      répondent que déverrouillée. `io_bus_find_write` = `claims_write ?: claims`.
- [x] Wrapper ULA-NG synchronise l'IRQ raster quand l'écriture est consommée.
      Blocs ULA-NG retirés des 2 callbacks → `io_read/write_callback` = boucle du
      bus + repli VIA. **Plus aucun périphérique à plage câblé en dur.**
- [x] Non-régression : `test-ula-ng` 60/60 + garde visible 2/2 (déverrouillage +
      palette/copper + IRQ raster via le dispatch réel), boots byte-identiques au
      pré-migration. `make tests` = 939 checks (outils construits ; 923 sans,
      écart = e2e `bas2tap` skippés qui s'exécutent après `make tools`).
- [x] Doc `docs/architecture/io-bus.md` §3/§5 (contrat étendu, ULA-NG ✅).
> **backlog** : la migration du **dispatch I/O** est terminée. Étendre ensuite
> `io_device_t` aux hooks `init`/`reset`/`savestate`/`tick` (§6 io-bus.md) —
> notamment chaque device sérialisant sa propre section `.ost` (le point qui a
> fait mal avec OCB/OGP).

---

### Sprint 122: LOCI migré vers le bus `io_device_t` (v1.82.0-alpha) - DONE
Suite de la migration du bus I/O. Le **LOCI** — le périphérique précisément
pointé par le constat « que fait le code du LOCI dans le `main` ? » — quitte le
câblage en dur des callbacks pour rejoindre la table `io_bus[]`, en tête.
- [x] Un seul `io_device_t` « loci » modélise les 3 sous-fenêtres disjointes
      (MIA $03A0-$03BF, TAP $0315-$0317 recouvrant le Microdisc, DSK
      $0310-$0314/$0318-$0319 si `!has_microdisc`). `claims` encode la priorité,
      `read`/`write` dispatchent en interne.
- [x] 6 blocs `if (has_loci && ...)` retirés → `io_read/write_callback` = boucle
      de bus + cas ULA-NG + repli VIA. Snoop VIA ORB $0300 (`loci_tap_motor`)
      conservé hors bus (pas un claim).
- [x] Non-régression : suite verte (923), boots LOCI (MIA + image SD/DSK)
      byte-identiques au binaire pré-bus, e2e ACIA-over-LOCI $0380 OK.
- [x] Doc `docs/architecture/io-bus.md` §5 (LOCI ✅, ULA-NG ⏳ justifiée).
> **backlog** : l'ULA-NG reste câblée après la boucle (claim à effet de bord :
> l'écriture doit toujours recevoir l'octet en fenêtre pour guetter 'N','G' et
> renvoie si consommée). La migrer suppose d'étendre le contrat `io_device_t`
> (`write` renvoyant « consommé », `claims` distinct lecture/écriture) → à faire
> avec les hooks génériques init/reset/savestate/tick.

---

### Sprint 121: Bus de périphériques `io_device_t` (v1.81.0-alpha) - DONE
Réponse au constat architectural « que fait le code du LOCI dans le `main` de
l'émulateur ? ». Le routage I/O ($0300-$03FF) était une cascade de `if` codés
en dur dans `io_read_callback`/`io_write_callback`. On introduit une
**abstraction de bus** et on migre les périphériques par la méthode *strangler*,
sans changement de comportement observable.
- [x] `include/io/io_device.h` : contrat `io_device_t` (`claims`/`read`/`write`
      recevant `emulator_t*`). Table `io_bus[]` + `io_bus_find()` dans `main.c`
      (l'ordre de la table = priorité de claim).
- [x] Migrés vers le bus : DTL2000 (sprint précédent), puis **ACIA 6551**,
      **Mageco/ORICON MIDI**, **Microdisc WD1793** → blocs `if` retirés des deux
      callbacks. Wrappers conservant les cas particuliers (picowifi-over-LOCI
      $0380, `fdc_trace` + synchro overlay Microdisc).
- [x] LOCI reste câblé **en tête** (TAP $0315-$0317 / DSK chevauchent le
      Microdisc, priorité +) ; ULA-NG reste câblée **après** le bus (repli VIA).
- [x] Doc `docs/architecture/io-bus.md` (contrat, taxonomie, ordre de migration).
- [x] Fix : `test-savestate` ne compilait plus depuis le retrait d'OCULA
      (symboles supprimés) → 3 tests OCULA obsolètes retirés (.ost rétro-compatible).
      Suite complète verte : 923 tests.
> **backlog** : migrer LOCI et ULA-NG vers le bus (dernier ressort — priorités
> croisées) ; hooks génériques init/reset/savestate/tick sur `io_device_t`.

---

### Sprint 117: VDU intégré ULA-NG v0.1 (v1.77.0-alpha) - DONE
Port de commandes de style VDU (`NG_VDU` $0357) : le 6502 streame des octets,
l'interpréteur vit **dans l'ULA-NG** (stand-in du firmware soft-core FPGA).
Design : `docs/ula-ng/VDU.md` (état de l'art 2026 : Agon VDP, VERA, OCULA-GPU).
- [x] FSM sans allocation dans `ula_ng.c` ; l'exécution appelle `ula_ng_write`
      (zéro duplication). Verrouillé → passthrough.
- [x] Jeu v0.1 : `20` reset, `22 n` MODE, `19 l r g b` palette, `18 a` fond
      couleur par cellule, `31 col row a` colorer une cellule (sans clash).
- [x] Démo `ng_vdu.s` : mosaïque par cellule **pilotée uniquement par le flux**
      ($0357), zéro logique 6502. Menu + README démos.
- [x] test-ula-ng 47→54 + garde visible (stream 18/$21 → fond bleu). 1036 →
      1043 tests. Docs VDU.md (v0.1) + README-ULA-NG §7bis.
> **backlog** : fontes/bitmaps par upload, hook du vecteur OSWRCH (à vérifier),
> PLOT étendu (triangles, coords 16 bits).

---

### Sprint 120: Retrait complet d'OCULA du mainline (v1.80.0-alpha) - DONE
Sur demande : plus aucun code spécifique OCULA dans `main` (OCULA préservé et
recompilable sur la branche `feature/ocula-isolation`).
- [x] Suppression des modules/headers/demos/docs/test OCULA.
- [x] Strip de video.c/h, memory.c/h, main.c, savestate.c, debugger.c, control.c.
- [x] Constantes partagées avec l'ULA-NG **renommées génériques** (VIDEO_MAX_W,
      VIDEO_WIDE_W, VIDEO_BORDER_*, video_border) → ULA-NG (§5.1-5.8 + VDU) intacte.
- [x] Compositing bordure conservé générique (inerte). Sections savestate OCB/OGP
      retirées (rétro-compatible).
- [x] Suite complète verte, test-ocula (101) retiré. 1049 → 948 tests.

---

### Sprint 119: VDU intégré v0.3 — upload sprites par flux (v1.79.0-alpha) - DONE
Protocole « buffered commands » (à la Agon) pour définir un sprite entièrement
par le flux VDU.
- [x] `VDU 23,id` ouvre l'upload du motif (256 octets suivants streamés dans le
      motif du sprite `id`) ; `VDU 24,id,x,y,f` position + enable.
- [x] FSM : compteur `vdu_upload` en tête de `vdu_feed` ; retour auto en mode
      commande. Réutilise `spr_sel`/`spr_wp`.
- [x] Démo `ng_vdu_spr.s` : losange défini par flux de 256 octets. test-ula-ng
      58→60 + garde visible (sprite plein → 256/256 px). 1047 → 1049 tests.

---

### Sprint 118: VDU intégré v0.2 — graphiques + VRAM ULA-NG (v1.78.0-alpha) - DONE
« Le VDU possède ses pixels » : VRAM chunky portée par l'ULA-NG + commandes
graphiques VDU.
- [x] `ula_ng.vram` (160×224 4bpp = 17920 o, hors 64 Ko). Quand `vram_active`
      (posé par `CLG`), le mode chunky lit la VRAM au lieu de la RAM CPU.
- [x] Commandes `16` CLG, `17 c` couleur, `25 x y` PLOT, `26 x0 y0 x1 y1` DRAW
      (Bresenham). `20`/reset désactive la VRAM.
- [x] Câblage `video_t.ng_vram`/`ng_vram_active` ; renderer chunky lit VRAM si
      active, sinon RAM CPU (non-régression).
- [x] Démo `ng_vdu_gfx.s` : sunburst tracé uniquement par commandes VDU (VRAM
      ULA-NG). test-ula-ng 54→58 + garde visible (DRAW ligne). 1043 → 1047 tests.

---

### Sprint 116c: Documentation utilisateur ULA-NG (v1.76.2-alpha) - DONE
- [x] `docs/ula-ng/README-ULA-NG.md` réécrit et mis à jour aux 8 features
      (était « étapes 1-5 ») : mode « ULA normal » (verrouillé = défaut),
      absence de mot-clé BASIC (activation POKE/ML/`--ula-ng-poke`), table
      d'activation par mode (NG_MODE : chunky `$05`, 80col `$09`, attributs
      `$02`, sprites `NG_SPR_CTRL`), retour au normal (reset / `NG_MODE=0`),
      carte complète des registres `$0340-$035F`, recettes des 8 features.
- [x] README principal : section « ULA-NG (next-gen ULA) » + liens.
- [x] Distinction explicite avec `--ula PROFILE` (OCULA, indépendant).

---

### Sprint 116b: Retours d'essai démos ULA-NG (v1.76.1-alpha) - DONE
- [x] **Chunky 4bpp plein écran** (§5.8) : suppression du pied de texte 40 col
      (240 px) qui laissait un trou noir 240-319 en bas d'un écran 320 px. Le
      mode couvre toute la hauteur (0-223) depuis le buffer ; `ng_chunky.s`
      remplit 224 rangées. Non-régression vérifiée.
- [x] **Démo `ng_attributes` refaite en machine code** : mosaïque de dégradé de
      couleur **par cellule** (8 couleurs en diagonale) au lieu d'une encre
      unique cyclée — démontre le « no color clash » (impossible sur l'ULA
      d'origine). Vérifié 8 couleurs.
- [x] Note : test 2 (80 col) n'était pas un plantage — juste le temps de
      remplissage BASIC de l'écran $A000 (~30 s), déjà signalé au menu/README.

---

### Sprint 116: Démos ULA-NG (v1.76.0-alpha) - DONE
Suite de démos des features ULA-NG sur la machine émulée (modèle `demos/ocula/`).
Chaque démo se déverrouille elle-même ('N','G' sur $0340) — `-t <démo>.tap -f`.
- [x] `ng_chunky.s` (machine code, xa) : chunky 4bpp 320×200 16 couleurs
      (buffer $4000, dégradé diagonal), palette animée (rotation).
- [x] `ng_text80.bas` : texte 80 colonnes (480 px, charset RAM $B400).
- [x] `ng_attributes.bas` : attributs parallèles cyclés (sans color clash).
- [x] `ng_copper.bas` : copper/scanline → barres raster arc-en-ciel.
- [x] `ng_sprite.bas` : sprite matériel 16×16 rebondissant.
- [x] `README.md` + `menu.sh` (--render-software défaut). Toutes vérifiées
      headless. Tests inchangés (1036 ; démos hors `make tests`).

---

### Sprint 115: ULA-NG étape 9 — chunky 4bpp / texte 80 col (§5.8) (v1.75.0-alpha) - DONE
Dernière feature ULA-NG : deux modes vidéo étendus sélectionnés par
`NG_MODE.b2-3`, latchés au début de trame (largeur framebuffer stable une
trame, comme les modes OCULA). Données lues depuis `NG_SCRSTART` (défaut $A000).
- [x] **Chunky 4bpp** : 160 px/rangée × 200, 4 bits = index LUT palette NG 16
      entrées (RGB888), 80 o/rangée, chaque px chunky = 2 px fb → **320 px**.
      Rangées 200-223 = texte de statut $BB80.
- [x] **Texte 80 col** : 80 car × 6 px = **480 px**, charset RAM redéfinissable
      ($B400/$B800, mécanisme natif Oric — aucune police inventée), attributs
      série + couleurs LUT NG.
- [x] Caches `chunky_active`/`text80_active` (gate `unlocked && NG_MODE.b0`) ;
      latch `video_t.ng_chunky`/`ng_text80` ; `apply_profile_resolution`
      bascule la largeur. Refactor `render_bottom_text_scanline`. Sprites
      composés dans les 2 modes.
- [x] **Tests visibles** : chunky $05 + palette idx0=magenta → 320×224 magenta
      dominant + 16 couleurs ; 80col $09 → 480×224 caractères rendus. Ajoutés à
      `test-ula-ng-visible`. Non-régression : test-video/ocula/renderer/savestate
      (ula_ng.c ajouté aux liens).
- [x] test-ula-ng 42 → 47 checks. 1031 → 1036 tests.
> **ULA-NG : toutes les features de la spec §5 sont livrées** (§5.1 palette →
> §5.8 chunky/80col). Reste : portage Verilog FPGA (Tang Primer 20K) à partir
> de cette référence logicielle.

---

### Sprint 114: ULA-NG étape 8 — sprites matériels (§5.7) (v1.74.0-alpha) - DONE
Jusqu'à 16 sprites 16×16, motif indexé palette, composés sur le fond avec
priorité par index et détection de collision. Deuxième usage de la mémoire
additionnelle portée par `ula_ng` (miroir DDR3 externe du Tang Primer 20K).
- [x] Table `ula_ng.sprites[16]` : motif 1 o/px (0=transparent, 1-7=index LUT),
      position X/Y, bit visible. Hors des 64 Ko du 6502.
- [x] Registres flux : `NG_SPR_CTRL` ($0350 enable global), `NG_SPR_SEL` ($0351
      + reset ptr motif), `NG_SPR_X`/`Y` ($0352/$0353), `NG_SPR_ATTR` ($0354
      visible), `NG_SPR_DATA` ($0355 flux auto-incr mod 256).
- [x] Composition `ula_ng_composite_scanline()` (module ula_ng possède état
      sprite/palette/collision), appelée par `video_render_scanline` après le
      fond via pointeur `video_t.ng_dev`. Priorité par index (15→0 → sprite 0
      au-dessus).
- [x] Collision sprite-sprite (recouvrement pixels opaques/scanline) →
      `NG_SPR_STATUS` ($0356 b7, clear on read). Gate `spr_active`.
- [x] **Test visible** : sprite 0 (16×16 index 1 = rouge) à (100,100) → bloc
      rouge **256 px pixel-exact** au framebuffer (ajouté à `test-ula-ng-visible`).
      Non-régression (test-video 22/22 ; module ula_ng.c ajouté au link).
- [x] test-ula-ng 36 → 42 checks. 1025 → 1031 tests.
> **Suite** : chunky/80col (§5.8) — dernière feature ULA-NG.

---

### Sprint 113: ULA-NG étape 7 — attributs parallèles (§5.6) (v1.73.0-alpha) - DONE
Plan mémoire de 8 Ko fournissant encre+papier par cellule sans consommer
d'octets pixel dans le flux → **supprime le color clash** sériel de l'Oric.
Premier usage de la mémoire additionnelle portée par `ula_ng` (miroir de la
DDR3 externe du Tang Primer 20K, hors des 64 Ko du 6502).
- [x] Plan `ula_ng.attr[8192]` : octet `(paper<<3)|ink` (3 bits chacun) par
      cellule, indexé `y·40 + col` (0-7999 sur 8192).
- [x] Flux : `NG_ATTR_FILL` ($034D) remplit tout le plan + reset du pointeur
      (une écriture = un fond complet) ; `NG_ATTR_DATA` ($034E) écrit la cellule
      courante puis auto-incrémente (modulo 8192).
- [x] Gate `NG_MODE.b1` (`ULA_NG_MODE_ATTR`), effectif seulement déverrouillé
      (`attr_active`). Actif → composition de la zone principale (0-199) tire
      encre+papier du plan au lieu des attributs sériels ($00-$1F plus interprétés).
      Câblage `video_t` par pointeurs `ng_attr` / `ng_attr_active`.
- [x] **Test visible** : `NG_MODE.b1` + `NG_ATTR_FILL=$21` (papier bleu, encre
      rouge) → zone principale entièrement bleu (45961 px) + rouge (2039 px),
      aucune autre couleur, au framebuffer (ajouté à `test-ula-ng-visible`).
      Non-régression (inactif = inchangé ; test-video 22/22).
- [x] test-ula-ng 31 → 36 checks. 1020 → 1025 tests.
> **Suite** : sprites (§5.7), chunky/80col (§5.8).

---

### Sprint 112: ULA-NG étape 6 — scroll fin X/Y (§5.5) (v1.72.0-alpha) - DONE
Décalage pixel-près (complément du scroll grossier §5.3) → défilement lisse.
- [x] `NG_SCROLLX` ($0344, clampé 0-5 = cellule 6 px), `NG_SCROLLY` ($0345,
      masqué 0-7 = cellule 8 px). Zone principale (0-199) quand actif.
- [x] Y décale la ligne source (`src_y = y + scrolly`) ; X décale l'affichage
      (`px = col·6 - scrollx`, cellule en plus pour le bord droit, `set_pixel`
      clippe hors écran). Câblage `video_t` par pointeurs `ng_scrollx/y`.
- [x] **Test visible** : `NG_SCROLLY=4` / `NG_SCROLLX=3` → décalage **pixel-exact**
      au framebuffer (ajouté à `test-ula-ng-visible`). Non-régression (test-video 22/22).
- [x] test-ula-ng 27 → 31 checks. 1016 → 1020 tests.
> **Suite** : attributs parallèles (§5.6), sprites (§5.7), chunky/80col (§5.8).

---

### Sprint 111: ULA-NG — outil `--ula-ng-poke` + guide + test end-to-end (v1.71.0-alpha) - DONE
Outillage pour piloter/tester l'ULA-NG sans POKE BASIC lents.
- [x] Flag CLI `--ula-ng-poke "AAA=VV,..."` : programme les registres ULA-NG
      ($0340-$035F) au démarrage via `ula_ng_write` (hex), combinable avec
      `--screenshot-at`. `--control write $addr $val` atteint aussi l'ULA-NG.
- [x] `docs/ula-ng/README-ULA-NG.md` : guide utilisateur (livrable spec §8) —
      déverrouillage, carte des registres, exemples `--ula-ng-poke` + asm.
- [x] `tests/integration/test_ula_ng_visible.sh` (`make test-ula-ng-visible`, dans
      `make tests`) : garde de régression **end-to-end vidéo** (palette, copper,
      start-address vérifiés au framebuffer). 1014 → 1016 tests.

---

### Sprint 110: ULA-NG étape 5 — palette par scanline / copper (§5.4) (v1.70.0-alpha) - DONE
Mini-copper : dégradés / splits de couleur verticaux sans CPU par ligne.
- [x] Liste copper **64 entrées** (ligne, index LUT, couleur RGB444). Flux :
      `NG_COP_CTRL` ($034B) reset ; `NG_COP_DATA` ($034C) 3 octets/entrée
      (`ligne`, `(index<<4)|R`, `(G<<4)|B`). Expansion RGB444→888.
- [x] `ula_ng_scanline(line)` applique à `u->pal[index]` les entrées de la ligne
      si `déverrouillé && NG_MODE.b0` ; hook vidéo relit la LUT → effet ligne
      suivante (cohérent §9). Raster IRQ + copper factorisés (gating sur `active`).
- [x] **Test visible** : couleur 7 rouge (ligne 0) puis bleu (ligne 30) → bandes
      verticales (bandeau rouge, « BYTES FREE » bleu). Non-régression (test-video 22/22).
- [x] test-ula-ng 22 → 27 checks. 1009 → 1014 tests.
> **Suite** : scroll fin X/Y (§5.5), attributs // (§5.6), sprites (§5.7),
> chunky/80col (§5.8).

---

### Sprint 109: ULA-NG étape 4 — start-address (§5.3) (v1.69.0-alpha) - DONE
Double buffering + scroll vertical grossier par relocalisation du fetch vidéo.
- [x] `NG_SCRSTART` ($0342 LSB / $0343 MSB, 16 bits) remplace la base du fetch
      ($A000 HIRES / $BB80 TEXT) pour la zone principale (lignes 0-199) quand
      actif (déverrouillé && NG_MODE.b0). **$0000 = base par défaut** (compat, pas
      de blank au reset). Rangées de statut 200-223 fixes à $BB80.
- [x] Câblage `video_t` par pointeur `ng_scrstart` ; flag NG général renommé
      `active` (`ng_active` côté vidéo) — gate palette ET start-address.
- [x] **Test visible** : `NG_SCRSTART = $BB80+40` → écran remonté d'une rangée,
      comparaison **pixel-exacte** du framebuffer. Non-régression (test-video 22/22).
- [x] test-ula-ng 18 → 22 checks. 1005 → 1009 tests.
> **Suite** : palette/scanline (§5.4), scroll fin (§5.5), attributs // (§5.6),
> sprites (§5.7), chunky/80col (§5.8).

---

### Sprint 108: ULA-NG étape 3 — IRQ raster (§5.2) (v1.68.0-alpha) - DONE
Interruption raster programmable (la feature la plus demandée).
- [x] Source `IRQF_ULANG = 0x20` (cpu6502.h), combinée au bitfield 6502
      level-triggered OU-câblé (aucune IRQ existante écrasée).
- [x] Registres `NG_RASTERLINE` ($0346), `NG_STATUS` ($0347 : R b7=pending,
      W=acquit + b0=enable — bit ajouté pour éviter les IRQ parasites au reset).
- [x] `ula_ng_scanline(line)` piloté par la boucle vidéo sur la **trame complète
      0-311** (découplé du visible 0-223) ; IRQ niveau à `line==NG_RASTERLINE` si
      `déverrouillé && NG_MODE.b0 && enable`. Pont `cpu_irq_set/clear(IRQF_ULANG)`
      par ligne + à l'acquittement.
- [x] Validation : test-ula-ng +5 checks + **observable end-to-end** (armer sans
      ISR gèle BASIC → l'IRQ atteint le 6502 ; contrôle non armé tourne). Barre
      raster propre = ISR (vecteur redirigé sous Sedoric/overlay), hors BASIC nu.
- [x] Non-régression (inactif verrouillé/mode0/enable0 ; test-cpu 92/92).
      1000 → 1005 tests (test-ula-ng 13 → 18).
> **Suite** : start-address (§5.3), palette/scanline (§5.4), scroll fin (§5.5),
> attributs // (§5.6), sprites (§5.7), chunky/80col (§5.8).

---

### Sprint 107: ULA-NG étape 2 — palette-indirection (§5.1) (v1.67.0-alpha) - DONE
Deuxième feature ULA-NG : les couleurs logiques passent par une LUT programmable.
- [x] LUT ULA-NG **16 entrées × 12 bits** (RGB444) dans `ula_ng_t`, identité au
      reset (0-7 = couleurs Oric → compat). Registres `NG_MODE` ($0341 b0),
      `NG_PAL_IDX` ($0348, auto-incr), `NG_PAL_DATA` ($0349/$034A). Expansion
      RGB444→888 par réplication de quartet (`c8=c4*0x11`, synthétisable).
- [x] Point unique `get_rgb`/`pal_rgb` alimenté par `palette_latch` (par
      scanline) quand `pal_active` (déverrouillé + NG_MODE.b0). Câblage `video_t`
      par pointeurs (`ng_pal`/`ng_pal_active`), calqué OCULA → `video.c`
      indépendant du module (test-video linke sans lui).
- [x] **Test visible** (§4) : en émulateur, POKE déverrouillage + reprog couleur
      7 blanc→vert → 50201 px verts au framebuffer. Non-régression verrouillé
      (LUT identité + pointeurs NULL = vidéo bit-à-bit).
- [x] test-ula-ng 8 → 13 checks. 995 → 1000 tests.
> **Suite** : IRQ raster (§5.2, `IRQF_ULANG=0x20`, `ula_ng_scanline`), puis
> start-address, palette/scanline, scroll fin, attributs //, sprites, chunky.

---

### Sprint 106: ULA-NG étape 1 — verrou/identité + plomberie page 3 (v1.66.0-alpha) - DONE
Fondation d'une **ULA « next-gen »** (référence logicielle d'un futur portage
Verilog sur **Sipeed Tang Primer 20K / GW2A-18**, cf `docs/ula-ng/AUDIT.md` +
`ULA-NG-SPEC.md`). Module isolé aux frontières miroir-FPGA.
- [x] Audit obligatoire (§0) : `docs/ula-ng/AUDIT.md` — rendu déjà scanline-based,
      fenêtre `$0340-$035F` libre, palette LUT `get_rgb`, IRQ bitfield (bit libre
      `0x20`), source de temps `frame_cycles`.
- [x] 5 décisions post-audit figées dans la spec (détection robuste, lignes 0-311,
      LUT 16×12 b, sémantique latch, mémoire DDR3) + cible FPGA confirmée (photo).
- [x] Module `src/io/ula_ng.{c,h}` : déverrouillage 'N','G' ($4E,$47) sur `$0340`,
      `NG_ID`=$1E, `NG_IDCHK` ($034F)=~NG_ID (handshake anti-faux-positif).
- [x] Plomberie : hooks `io_read/write_callback` (avant fallback VIA), champ
      `ula_ng` dans `emulator_t`, init verrouillé au power-on.
- [x] Non-régression **bit-à-bit** : verrouillé = passthrough VIA (FPGA=tristate) ;
      la fenêtre n'est possédée qu'après déverrouillage.
- [x] `make test-ula-ng` (8 checks) dans `make tests`. 987 → 995 tests.
> **Suite** (`ULA-NG-SPEC.md §4`) : palette-indirection (§5.1), IRQ raster (§5.2),
> start-address, palette/scanline, scroll fin, attributs //, sprites, chunky/80col.

---

### Sprint 105: Descripteurs Sedoric chaînés + doc appel ML (v1.65.0-alpha) - DONE
Extension de l'outillage Sedoric (partagé, sert les projets SCUMM-Oric/scoop-oric)
et documentation du pilotage de Sedoric depuis du code machine. Format chaîné
**confirmé par test émulateur**, pas par déduction.
- [x] FIX `tap2sedoric` + `sedoric_inject.py` mono-descripteur (plantaient
      > ~121 secteurs data) → **chaînage des secteurs descripteur** : 1ᵉʳ = en-tête
      12 o + carte dès `+0x0C` (122 paires) ; suivants = lien `+0,+1` + carte dès
      `+0x02` (127 paires) ; `+0xA,+0xB` (1ᵉʳ) = total data. Garde `total > 255`.
      Validé in-situ : fichier 150 secteurs (2 descripteurs) chargé intégralement
      par `LOAD` (secteurs du 2ᵉ descripteur inclus).
- [x] DOC `docs/SEDORIC.md` §6 (recettes ML validées in-situ) : exécuter une
      commande via le vecteur « ! » `$0467` (interpréteur `$D3AE`, ligne terminée
      `$00` **jamais** CR) ; bascule overlay RAM↔ROM (`$0314` bit1 ROMDIS/bit7
      EPROM, stub `$0477`, shadow `$04FB`) ; lecture d'un secteur à un offset
      (OSGBPB : descripteur BUF1 `$C100`, carte `$C10C`, `XPRSEC $DA73`, interface
      `$C000-$C004`).
- [x] FIX collision post-fichier-chaîné : `scan_used` lisait la carte des
      descripteurs de continuation à `+0x0C` au lieu de `+0x02` → injection après
      un fichier chaîné pouvait écraser ses secteurs data (signalé scoop-oric).
      Corrigé dans les 2 outils, validé (LOAD d'un fichier de 130 secteurs reste
      intact après une injection ultérieure).
- [x] test-sedoric-tools 12 → 15 checks. 984 → 987 tests.

---

### Sprint 104: Outillage Sedoric amendé depuis SCUMM-Oric / scoop-oric (v1.64.0-alpha) - DONE
Reprise des acquis Sedoric des projets SCUMM-Oric et scoop-oric pour corriger et
étendre les outils disque de Phosphoric. Format vérifié **byte-exact** contre le
manuel primaire SEDORIC 3.0 (`sedna3_0.pdf`, dump de la BANQUE n°7) — cf
`docs/SEDORIC.md`.
- [x] FIX `tap2sedoric` : secteur descripteur non conforme — type `+3` jamais
      posé, aucune adresse d'exécution, nb-secteurs en `+9,+10` **big-endian**
      (chevauchant l'exec). Corrigé : `+3` type ($40 bloc data / $41 AUTO),
      `+8,+9` exec (LE), `+0xA,+0xB` nb secteurs (LE).
- [x] FIX collision multi-fichiers : `tap2sedoric` ET `sedoric_inject.py`
      réallouaient depuis piste 21 sec 1 sans exclure les secteurs occupés (2ᵉ
      injection écrasait le descripteur du 1ᵉʳ). Parcours catalogue + cartes de
      secteurs des descripteurs existants avant allocation.
- [x] ADD options `tap2sedoric` `-a` (AUTO), `-e EXEC_HEX`, `-i "INIST"` (boot
      autoexec, piste 20 sec 1 offset 0x1E).
- [x] ADD outil `sedoric-info` (VTOC/nom/INIST/catalogue/descripteurs décodés,
      `--check FREE:FILES`), câblé dans `make tools`.
- [x] ADD chaîne RAW `tools/sedoric_inject.py` + `tools/dsk_raw2mfm.py` (ordre
      blocs MFM corrigé **side-major** = lecteur `src/storage/sedoric.c`).
- [x] ADD **chaînage de secteurs directory** (catalogue > 15 fichiers) dans
      `tap2sedoric` ET `sedoric_inject.py` : parcours de la chaîne `+0,+1`, et si
      pleine, allocation+chaînage d'un secteur catalogue vierge (localisé par
      lien, manuel ANNEXE 7). FIX associé : `scan_used` marque aussi les secteurs
      catalogue chaînés (sinon réalloués/écrasés). Validé in-situ (LOAD d'un
      fichier du 2ᵉ secteur, y compris injecté après création du secteur).
- [x] ADD outil **`tools/sedoric_mkbare.py`** (master « nu » : neutralise/remplace
      l'INIST → boot `Ready`, déterministe vs `make_bootable_sedoric.sh` fragile ;
      MFM CRC refait, RAW géré). Vérifié : `SEDO40u` → `SEDORIC V4.0 / Ready`.
- [x] Validation d'exécution **in-situ** : `.COM` AUTO (type $41) chargé+exécuté
      via **`LOAD"NAME"`** (PAS `LOADM` = ROM cassette → TYPE MISMATCH ; PAS le nom
      nu = BASIC → SYNTAX ERROR). Prouvé sur les deux pipelines (`tap2sedoric` MFM
      et `sedoric_inject.py`+`dsk_raw2mfm` RAW). `sedoric_inject.py` : passer
      l'exec ⇒ AUTO. Recette dans `docs/SEDORIC.md §6`.
- [x] `docs/SEDORIC.md` + `tests/integration/test_sedoric_inject.sh`
      (`make test-sedoric-tools`, dans `make tests`, 12 checks). 972 → 984 tests.
> **Reste en backlog** : édition de la bitmap VTOC (les outils décrémentent le
> compteur `free` mais n'écrivent pas les bits — sûr sur disque neuf, cf
> `analyze_sedoric_vtoc`) ; suppression/renommage de fichiers.

---

### Sprint 103: Injection stuck-bits RAM (Epic 6 / US 6a) (v1.62.0-alpha) - DONE
Volet testable de l'US 6 (parité « RAM errors » de b2). Le volet 6b (overlays
visuels timing) reste en backlog car GUI/SDL, invérifiable en headless.
- [x] Masques `stuck0` (bits→0) / `stuck1` (bits→1) sur les lectures RAM
      $0000-$BFFF (`val=(v&~stuck0)|stuck1`, coût nul quand off) ;
      `memory_set_stuck_bits`.
- [x] REPL `stuck S0 [S1]`, `--control` `stuck-bits <s0> [s1]` (cap
      `stuck-bits`), REST `POST /stuck-bits?zero=&one=` + `GET /stuck-bits`.
      test-memory 19→20, test-control-dispatch 25→26, test-httpapi 43→45.
      Valgrind clean. 964 → 968 tests.
> **Epic 6 : rattrapage b2 substantiellement atteint** — US 1-5 + US 6a livrées.
> Reste seulement US 6b (overlays visuels GUI), à faire sur machine avec écran.

---

### Sprint 102: Groupes de symboles (Epic 6 / US 4) (v1.61.0-alpha) - DONE
Cinquième US livrée de l'Epic 6 — **tout le périmètre non-GUI est fait** (reste
US 6, overlays GUI, optionnel). Table de symboles plate → groupes activables.
- [x] `symbol_entry_t.group` (0-255), `symbol_table_load_group`,
      `symbol_set_group_enabled`/`_group_enabled`/`_group_count` ;
      `symbol_lookup`/`_resolve` filtrent les groupes désactivés. Champ
      `group_disabled[256]` (0=activé) → table zéro-initialisée robuste.
- [x] REPL `sym load FILE [g]`/`sym group N on|off`/`sym groups`, `--control`
      `load-sym FILE [group]`/`sym-group N on|off` (cap `sym-group`), REST
      `POST /sym?path=&group=` + `POST /sym/group`. test-symbols 11→12,
      test-control-dispatch 24→25, test-httpapi 41→43. Valgrind clean.
      960 → 964 tests.
> Epic 6 : 5/6 US livrées (US 1/2/3/4/5). Reste US 6 (overlays visuels GUI,
> optionnel/élevé) — tout le cœur non-GUI du rattrapage b2 est atteint.

---

### Sprint 101: Couverture d'inspection élargie (Epic 6 / US 5) (v1.60.0-alpha) - DONE
Quatrième US livrée de l'Epic 6. b2 a ~12 fenêtres d'inspection ; Phosphoric
couvrait via/psg/disk/acia/tape/loci — ajout de 4 sous-systèmes.
- [x] REPL `video`/`ula`, `kbd`/`keyboard`, `joy`/`joystick`, `printer`/`mcp40`
      (uniquement des champs réels des structs ; champs SDL2-only sous
      `#ifdef HAS_SDL2`).
- [x] `--control peek video|kbd|joy|printer` (key=value) → REST
      `GET /peek/{video|kbd|joy|printer}`. test-debugger 21→22,
      test-control-dispatch 23→24, test-httpapi 37→41. Valgrind clean.
      954 → 960 tests.
> Epic 6 : reste US 4 (symboles scopés par banque), US 6 (optionnel GUI).

---

### Sprint 100: Carte d'accès mémoire r/w/x (Epic 6 / US 3) (v1.59.0-alpha) - DONE
Troisième US de l'Epic 6 (rattrapage b2). Les watchpoints plafonnaient à 8 ;
b2 a des flags r/w/x par octet sur toute la mémoire.
- [x] Carte `g_amap[0x10000]` (file-static) : flags `AMAP_R`/`W`/`X` par
      adresse, régions arbitraires sans limite. `debugger_amap_set/clear/get/
      count/active/parse_flags`.
- [x] Read/write via le callback de trace mémoire (mutualisé avec les
      watchpoints ; installé si `num_watchpoints>0` OU carte a des flags r/w) ;
      exec (`AMAP_X`) testé avant chaque instruction dans `debugger_should_break`.
- [x] Pilotage REPL `wr START END [rwx]` / `wr` / `wr clear`, `--control`
      `watch-region`/`-list`/`-clear` (cap `access-map`), REST
      `POST/GET/DELETE /watch-region`. test-debugger 20→21,
      test-control-dispatch 22→23, test-httpapi 34→37. Valgrind clean.
      949 → 954 tests.
> Epic 6 : reste US 4 (symboles scopés), US 5 (inspection élargie), US 6 (GUI).

---

### Sprint 99: Tracing conditionnel (Epic 6 / US 1) (v1.58.0-alpha) - DONE
Deuxième US de l'Epic 6 (rattrapage b2). `--trace` démarrait au boot et loguait
tout ; b2 déclenche finement et borne la mémoire.
- [x] Déclencheurs `trace_arm` : start `now`/`pc:HEX` ; stop `cycle:N`/`brk`/
      `write:HEX`/`read:HEX`. Buffer circulaire `ring:N` + `trace_save_ring`.
      Symboles inline `sym`. Chemin `--trace FILE` historique préservé.
- [x] Double hook mémoire `memory_set_trace2`/`trace_callback2` : les triggers
      write/read cohabitent avec le callback des watchpoints.
- [x] Pilotage REPL `trace start/stop/save/status/off`, `--control`
      `trace <sub> <spec>` (cap `trace-cond`), REST `POST/GET/DELETE /trace` +
      `/trace/stop` `/trace/save`. test-trace 10→16, test-control-dispatch
      21→22, test-httpapi e2e 29→34. Valgrind clean. 937 → 949 tests.
> US restantes de l'Epic 6 : US 3-5 (backlog), US 6 (optionnel GUI).

---

### Sprint 98: Inspection mémoire banque-aware (Epic 6 / US 2) (v1.57.0-alpha) - DONE
Première US de l'Epic 6 (rattrapage b2). Paramètre de banque pour lire les
couches derrière l'overlay ORIC $C000-$FFFF sans dépendre du paging courant.
- [x] `debugger_peek_bank(emu, addr, bank)` : `cpu` (défaut) / `ram`
      (`upper_ram` = RAM derrière la ROM) / `rom` (BASIC $C000-$FFFF) / `overlay`
      (Microdisc $E000-$FFFF) ; side-effect-free. Helpers `parse`/`name`.
- [x] REPL `m addr [len] [bank]` ; `--control` `read <addr> <len> [bank]`
      (cap `mem-bank`) ; REST `GET /mem?addr=&len=&bank=`. **Absorbe le backlog
      REST B2.**
- [x] Différé : disasm banque-aware (`d`) ; hors périmètre : transfert binaire
      (B1). Tests : test-debugger 19→20, test-control-dispatch 20→21,
      test-httpapi e2e 26→29. Valgrind clean. 932 → 937 tests.
> Détail des US restantes de l'Epic 6 plus bas.

---

### Sprint 97: API REST — parité debug (bridge --control + 7 gaps) (v1.56.0-alpha) - DONE
Les capacités debug (dont les 7 gaps du Sprint 96) n'étaient qu'au REPL. Rendues
pilotables à distance **sans dupliquer de logique** : chaque requête HTTP est
mappée vers une ligne `--control`, seule source de vérité.
- [x] **Bridge** du debug `--control` déjà présent mais non routé dans
      `http_api.c` : `GET /break` `/watch` `/disasm?addr=&n=`, `POST /raster`,
      `DELETE /break/{id}` `/watch/{id}` `/raster/{id}`.
- [x] **Watch modes** : `cmd_watch` prend un mode optionnel →
      `POST /watch ?addr=&mode=w|r|a|c`.
- [x] **Breakpoints conditionnels** : `cmd_break` accepte `if EXPR`
      (`debugger_add_cond_breakpoint`) → `POST /break ?addr=&if=EXPR`
      (URL-encodé, décodé par `get_param`).
- [x] **Cheat-finder** : état `hunt` exposé (`debugger_hunt_*`) →
      `POST /hunt ?op=eq|same|changed|up|down|list|clear[&val=]`.
- [x] **Mémoire⇄fichier** (`debugger_save_region`/`_load_region`) →
      `POST /save ?path=&addr=&len=`, `POST /load ?path=&addr=` (sandbox).
- [x] **Save-state** → `POST /state/save`, `POST /state/load` (`.ost`, sandbox).
- [x] **`set via`** → `POST /set ?via=&val=` ; **symboles** → `POST /sym ?path=`.
- [x] **Littéraux `%`** dans tous les params (`parse_hex` de `control.c`).
- [x] Caps `hello` étendues additivement (`CONTROL_PROTO_VERSION` inchangé).
- [x] Tests : `test-control-dispatch` 13→20, `test-httpapi` e2e 10→26 (un
      endpoint par gap, roundtrips région + save-state, sandbox) ; fiabilisé
      settle post-boot (flake pré-existant révélé en exécutant `HTTPAPI=1`) ;
      valgrind clean. 909 → 932 tests.

> **API REST : parité debug complète.** Tout le debug interactif se pilote via
> HTTP/JSON, en réutilisant le dispatch `--control`.

#### Backlog API REST — issu du comparatif HTTP vs b2 (émulateur BBC Micro)
Comparatif mené le 2026-07-15 (source : `b2/doc/Debug-version.md` +
`b2/src/b2/HTTPMethodsHandler.cpp`). b2 n'expose PAS son debugger sur HTTP (map
close : `reset/paste/poke/peek/mount/run/load-disc/launch`) — son API HTTP vise
l'**automatisation build**. Phosphoric mène sur le debug à distance ; deux
points où b2 est en avance et qui restent à combler :
- [ ] **B1 — Transfert mémoire binaire** : `/mem`, `/save`, `/load` répondent
      en hex dans du JSON (~3× la taille). b2 sert/reçoit de l'`octet-stream`
      brut (`peek`/`poke`). Ajouter une variante binaire (négociée par
      `Accept`/`Content-Type` ou un endpoint dédié) pour les gros transferts.
- [ ] **B2 — Sélecteur de banque dans `/mem` et `/peek`** : Phosphoric lit la
      **vue CPU** courante (overlay ROM/RAM $C000-$FFFF résolu à l'instant t).
      b2 offre des suffixes debugger (`s=`) + un drapeau `MOS's view` (`mos=`)
      pour viser une banque paginée précise. Exposer un paramètre équivalent
      (ex. `?bank=ram|rom`) pour lire sous l'overlay sans dépendre de l'état
      courant du banking. **→ repris par l'Epic 6 / US 2 ci-dessous.**

---

### Epic 6 — Parité inspection & tracing avancés (rattrapage b2) — BACKLOG
Issu du comparatif debugger vs b2 (2026-07-15, ancré sur `b2/doc/Debug-version.md`
+ `b2/src/beeb/include/beeb/BBCMicro.h`). b2 mène sur l'inspection interactive /
matérielle et le tracing ; Phosphoric sur l'inverse-step, les breakpoints
conditionnels, GDB et le pilotage HTTP. Cet Epic comble le retard côté
inspection/tracing, **filtré pour ORIC** (hors périmètre, spécifique BBC : bi-CPU
parasite, `MOS's view`, teletext debug).

- [x] **US 1 — Tracing conditionnel** *(v1.58.0, Sprint 99)* : déclencheurs
      start (`now`/`pc:HEX`) + stop (`cycle:N`/`brk`/`write:HEX`/`read:HEX`),
      **buffer circulaire** (`ring:N`, garde les N dernières instr, save à la
      demande), **symboles inline** (`sym`). Double hook mémoire
      (`memory_set_trace2`/`trace_callback2`) : les triggers write/read
      cohabitent avec les watchpoints. Pilotage REPL `trace start/stop/save/
      status/off`, `--control` `trace <sub> <spec>` (cap `trace-cond`), REST
      `POST/GET/DELETE /trace` + `/trace/stop` `/trace/save`. `--trace FILE`
      historique préservé. test-trace 10→16, test-control-dispatch 21→22,
      test-httpapi 29→34. 937→949 tests.
- [x] **US 2 — Inspection banque-aware** *(v1.57.0, Sprint 98)* : paramètre de
      banque sur `m` (REPL), `read` (`--control`) et `GET /mem?...&bank=` (REST)
      pour lire **sous** l'overlay ORIC $C000-$FFFF sans dépendre du paging —
      `debugger_peek_bank` : `cpu`/`ram` (`upper_ram` derrière la ROM)/`rom`
      (BASIC)/`overlay` (Microdisc). Cap `mem-bank`. **Absorbe le backlog REST
      B2.** Différé : disasm banque-aware (`d`, redirection `cpu_disassemble`) ;
      hors périmètre : transfert binaire (backlog B1). test-debugger 19→20,
      test-control-dispatch 20→21, test-httpapi 26→29. 932→937 tests.
- [x] **US 3 — Carte d'accès mémoire (breakpoints par octet r/w/x)** *(v1.59.0,
      Sprint 100)* : carte `g_amap[0x10000]` (flags `AMAP_R`/`W`/`X` par octet,
      régions arbitraires sans limite). Read/write via le callback de trace
      mémoire (mutualisé watchpoints) ; exec via `debugger_should_break`.
      REPL `wr START END [rwx]` / `wr` / `wr clear` ; `--control`
      `watch-region`/`-list`/`-clear` (cap `access-map`) ; REST
      `POST/GET/DELETE /watch-region`. test-debugger 20→21,
      test-control-dispatch 22→23, test-httpapi 34→37. 949→954 tests.
- [x] **US 4 — Symboles scopés par groupes** *(v1.61.0, Sprint 102)* :
      `symbol_entry_t.group` (0-255) + activation/désactivation par groupe
      (`symbol_set_group_enabled`) ; `symbol_lookup`/`_resolve` filtrent les
      groupes off (champ `group_disabled[256]`, 0=activé → zéro-init robuste).
      REPL `sym load FILE [g]`/`sym group N on|off`/`sym groups`, `--control`
      `load-sym FILE [group]`/`sym-group` (cap `sym-group`), REST
      `POST /sym?…&group=` + `POST /sym/group`. test-symbols 11→12,
      test-control-dispatch 24→25, test-httpapi 41→43. 960→964 tests.
      *(Le scoping par suffixe Exclusive/Inclusive de b2 reste un raffinement
      futur ; les groupes couvrent le besoin ORIC.)*
- [x] **US 5 — Couverture d'inspection élargie** *(v1.60.0, Sprint 101)* :
      commandes snapshot `video`/`ula`, `kbd`, `joy`, `printer`/`mcp40` (champs
      réels des structs uniquement ; SDL2-only sous `#ifdef HAS_SDL2`). REPL +
      `--control peek …` + REST `GET /peek/{video|kbd|joy|printer}`.
      test-debugger 21→22, test-control-dispatch 23→24, test-httpapi 37→41.
      954→960 tests.
- [~] **US 6 — partiellement livrée** :
      - [x] **US 6a — Injection stuck-bits RAM** *(v1.62.0, Sprint 103)* :
            masques `stuck0`/`stuck1` sur les lectures RAM (`memory_set_stuck_bits`,
            coût nul si off) ; REPL `stuck`, `--control` `stuck-bits`, REST
            `POST/GET /stuck-bits`. test-memory 19→20, test-control-dispatch
            25→26, test-httpapi 43→45. 964→968 tests.
      - [ ] **US 6b — Overlays visuels timing** *(GUI/SDL, backlog)* : position
            faisceau TV, lignes raster dans le renderer. **Non fait** :
            invérifiable en environnement headless — à traiter sur machine avec
            affichage, pas livré sans vérification visuelle.

> **Recommandation :** attaquer **US 1 + US 2** (cœur de l'écart, coût
> raisonnable, US 2 recycle le backlog REST) ; US 3-5 en backlog ; US 6 optionnel.

---

### Sprint 96: Parité debugger vs Oricutron — 7 gaps fermés (v1.55.0-alpha) - DONE
Audit comparatif frais du debugger interactif face à Oricutron (inventaire ancré
sur le code + la `ReadMe.txt` réelle d'Oricutron). La note « plus aucun gap »
était périmée : 7 écarts rouverts et fermés.
- [x] **Gap 1 — Watchpoints read/access/change** : `watchpoint_t{addr,mode,
      last_value}`, 4 modes (`WATCH_WRITE`/`READ`/`ACCESS`/`CHANGE`) ; le
      callback de trace mémoire filtre selon le mode (la trace remontait déjà
      `MEM_READ`/`MEM_WRITE`). REPL `w addr [w|r|a|c]`, listing + message de hit
      enrichis. Stub GDB `Z3`(read)/`Z4`(access) + trace (ré)installée sur
      insertion (les watchpoints gdb se déclenchent enfin).
- [x] **Gap 2 — Conditions de breakpoint par expression** : `b addr if EXPR`
      jusqu'à 4 comparaisons jointes `&&`/`||`, évaluées gauche-à-droite
      (`bp_condexpr_t`, `parse_condexpr`/`eval_condexpr`).
- [x] **Gap 3 — Recherche mémoire itérative (cheat-finder)** : commande `hunt`
      (seed sur tout l'espace puis raffine `V`/`=`/`+`/`-`/`!` + `list`/`clear`,
      état file-static, snapshot 64K).
- [x] **Gap 4 — Mémoire⇄fichier** : `save FILE addr len`, `load FILE addr`,
      `disf FILE addr n`.
- [x] **Gap 5 — Save-state depuis le REPL** : `ss FILE`/`sl FILE` (`.ost`),
      dépendance `savestate` découplée par symbole faible (les tests unitaires
      liant `debugger.c` seul ne tirent pas le sérialiseur).
- [x] **Gap 6 — Registres VIA** : `set via <reg 0-15> <val>`.
- [x] **Gap 7 — Littéraux binaires `%`** (`%100000000000`==`$800`).
- [x] Tests : `test-debugger` 13→19, `test-gdbstub` couvre `Z3`/`Z4` ; valgrind
      clean ; toutes suites vertes. 903 → 909 tests.

> **Debugger interactif : parité Oricutron re-confirmée** par audit ancré sur
> sources (plus la partie forensique/GDB/undo/raster où Phosphoric mène déjà).

---

### API REST de commande — backlog (spec `docs/http-api.md`)
Exposer le pilotage de Phosphoric (média, état, exécution) via HTTP/JSON sur un
port dédié, **sans dupliquer** la logique du protocole `--control`. Découpé en
4 sprints ; le risque single-thread (mutations aux frontières de frame) est
isolé dans l'Epic 2.

- **Sprint 92 — Epic 1 : découpler le dispatch de son transport** (v1.51.0-alpha) - DONE
  - [x] `control_sink_t` (stream stdout | buffer mémoire), handlers `cmd_*`
        écrivent dans un sink au lieu de `stdout` — non-régression byte-exacte
        (test-control hot-swap média vert sans modification).
  - [x] Extraire `control_dispatch(emu, sink, line)` → `control_result_t`
        (`CONTINUE`/`RESUME`/`QUIT`) ; `control_repl` devient une boucle mince ;
        `control_poll_pause` réutilise un sink stream.
  - [x] Test unitaire du dispatch via sink buffer : `test-control-dispatch`
        (11 tests, réponses vérifiées à l'octet près). Valgrind propre.
        877 → 888 tests.
- **Sprint 93 — Epic 2 : file de commandes thread-safe** (v1.52.0-alpha) - DONE
  - [x] Module `control_queue` : file mono-consommateur / multi-producteur
        (mutex partagé + condvar par commande). `submit()` enfile et bloque
        jusqu'à exécution ; `drain(emu)` exécute le lot par frame via
        `control_dispatch` + sink buffer et réveille les producteurs.
  - [x] `-lpthread` au build de base ; `create/destroy` réveille les bloqués.
  - [x] Test `test-control-queue` (3 tests) dont concurrent 8×100 write/read
        sur adresse unique/thread (routage réponse→producteur + zéro
        corruption). Valgrind memcheck + helgrind propres. 888 → 891 tests.
  - [x] Câblage `main.c` (drain par frame) — **fait au Sprint 94** avec le
        premier producteur (le serveur HTTP) ; `drain(NULL)` reste un no-op.
- **Sprint 94 — Epic 3 : serveur HTTP** (v1.53.0-alpha) - DONE
  - [x] Module `http_api` (flag `HTTPAPI=1`) : thread socket/`select` calqué sur
        le cast server, parse HTTP/1.1, mappe vers une ligne `--control`,
        exécute via `control_queue_submit`, réponse JSON + CORS.
  - [x] Options `--http-api[=PORT]` (8888), `--http-api-bind` (défaut
        `127.0.0.1`), `--http-api-root` (bac à sable) ; câblage `main.c`
        (queue + drain par frame + arrêt avant destruction).
  - [x] Endpoints GET/POST/DELETE (hello, regs, mem, peek, reset, tape, disk,
        exec) ; sécurité chemins (absolus/`..` rejetés → 403).
  - [x] `test-httpapi` (8 vérifs e2e curl, skip gracieux sans `HTTPAPI=1`) ;
        valgrind runtime clean. 891 → 899 tests.
- **Sprint 95 — Epic 4 : injection clavier + doc/exemples** (v1.54.0-alpha) - DONE
  - [x] Commande `keys <text>` (buffer d'injection dynamique `kbd_inject_*`,
        échappements `\n`→RETURN/`\t`/`\e`/`\\`) ; consommation par frame
        (`feed_kbd_inject`, press/hold/release) sur le thread émulateur.
  - [x] Endpoint `POST /keys` (corps `text=…`) ; aide `GET /` à jour.
  - [x] `test-control-dispatch` +2, `test-httpapi` +2 dont la démo e2e :
        `POST /keys 'POKE 4096,123'`+RETURN → `GET /mem` lit `7B` (BASIC saisi
        via HTTP exécuté). 899 → 903 tests.

> **Initiative API REST : TERMINÉE** (Epics 1-4, sprints 92-95, v1.51→1.54).
> Phosphoric se pilote entièrement à distance via HTTP/JSON.

---

### Sprint 91: Bit d'inversion HIRES — complément XOR 7 (v1.50.1-alpha) - DONE
- [x] Le bit 7 (inversion) d'un octet HIRES/texte **complémente** ink ET paper
      (fg→fg^7, bg→bg^7) au lieu de les **permuter** — conforme à la vraie ULA
      et à Oricutron (`bittab` : inv = `(fg^7, bg^7)`). Ne diffère d'une
      permutation que si ink/paper ne sont pas complémentaires (blanc/noir
      masquait le bug pour le texte courant).
- [x] Déclencheur : rapport **Hialmar** (forum Defence Force
      [p35017](https://forum.defence-force.org/viewtopic.php?p=35017#p35017)) —
      images **AIC** (format du jeu « Tyrann 4 ») rendues différemment vs
      Oricutron. `mapt4.tap` : la mer (octets inverses `$FE`/`$FF`, ink
      bleu/jaune sur noir) sortait **noire** au lieu de **bleue/blanche**.
- [x] Fix `src/video/video.c` : `render_hires_block` (fg=inv?ink^7:ink /
      bg=inv?paper^7:paper) ; les 2 chemins texte (0-199, 200-223) + 80 col
      abandonnent `on = !on` ; `render_attr_block` prend un flag `inverse`
      (cellule attr bit 7 → paper^7, conforme `ula_render_block(inverted,0)`).
- [x] Validé pixel à pixel vs modèle fidèle de l'algorithme ULA d'Oricutron :
      99,9 % sur les 200 lignes HIRES (résidu = curseur BASIC vivant `$BB80`).
      Pixels bleus 720 → 1705. Test `test_hires_inverse_complement`.
      876 → 877 tests.

---

### Sprint 90: Émulation cassette au niveau signal (v1.50.0-alpha) - DONE
- [x] Option `--tape-signal` : génère la forme d'onde de la bande sur VIA CB1
      (Timer 2), lue par la routine CLOAD **réelle** de la ROM — comme Euphoric
      / une vraie machine — au lieu de patcher getsync/readbyte (approche
      Oricutron, incompatible avec les chargeurs cassette maison).
- [x] Générateur `src/io/cassette.c` (ex-stub) : trame 14 bits (start/8 data
      LSB/parité impaire/stops), 1ʳᵉ demi-période constante (208 cyc) + 2ᵉ codant
      le bit (208='1', 416='0'), phase ancrée, leader 64× 0x16. Protocole
      rétro-ingénieré du ROM BASIC 1.0 ($E67D, seuil ~512 cyc).
- [x] Gating de l'émission sur le PC dans `[readbyte_entry..getsync_end]` +
      rewind à la 1ʳᵉ entrée (PB6 inexploitable : scan clavier). Position
      préservée entre blocs → chargeurs multi-blocs protégés OK.
- [x] Débloque *Soccer Manager* (KnightSoft 1984, EOR #$55) : titre PUIS jeu
      lancé (échouait par patch, comme Oricutron).
- [x] Suite `test-cassette` (8 tests) ; valgrind + static-analysis propres.
      868 → 876 tests. Doc `docs/sprint90-cassette-signal.md`.

---

## Phase 1: Foundation (Sprint 1-4) - COMPLETED

### Sprint 1-2: Core CPU (v0.2.0-alpha) - DONE
- [x] Implement 6502 instruction set (151 official opcodes)
- [x] Create CPU state structure (A, X, Y, SP, PC, flags)
- [x] Implement all addressing modes (13 modes)
- [x] Create cycle-accurate timing system
- [x] BCD arithmetic (decimal mode)
- [x] JMP indirect page boundary bug
- [x] NMI and IRQ interrupt handling
- [x] Disassembler
- [x] Write 74 unit tests for CPU

### Sprint 3-4: Memory & I/O (v0.3.0-alpha) - DONE
- [x] Implement 64KB memory management
- [x] Create ROM/RAM banking mechanism
- [x] Memory-mapped I/O at $0300-$03FF
- [x] Implement VIA 6522 emulation (all 16 registers)
- [x] Timer 1 (one-shot + free-running)
- [x] Timer 2 (one-shot)
- [x] Interrupt generation (IFR/IER)
- [x] Keyboard matrix scanning (8x8)
- [x] Cassette interface
- [x] Write 43 tests (19 memory + 24 I/O)

---

## Phase 2: Display & Audio (Sprint 5-7) - COMPLETED

### Sprint 5-6: Video System (v0.4.0-alpha) - DONE
- [x] Text mode (40x28 characters)
- [x] HIRES mode (240x200, 6 colors)
- [x] Character ROM rendering
- [x] Serial color attributes (ink/paper/inverse)
- [x] SDL2 renderer (conditional compilation)
- [x] Headless mode fallback

### Sprint 7: Audio System (v0.5.0-alpha) - DONE
- [x] AY-3-8910 PSG emulation
- [x] 3-channel tone generators
- [x] Noise generator (17-bit LFSR)
- [x] 16 envelope shapes
- [x] SDL2 audio output (conditional compilation)

---

## Phase 3: Storage & Host Integration (Sprint 8-11) - COMPLETED

### Sprint 8-9: Storage (v0.6.0-alpha) - DONE
- [x] .TAP format reader/writer
- [x] Fast load support
- [x] BASIC/binary to TAP conversion
- [x] Sedoric disk filesystem
- [x] FDC WD1793 controller emulation
- [x] Write 12 storage tests

### Sprint 10-11: Host Integration (v0.8.0-alpha) - DONE
- [x] Host filesystem sharing (HostFS)
- [x] VFS abstraction layer
- [x] Conversion tools (bas2tap, bin2tap, tap2sedoric)
- [x] Configuration system (.ini parser)

---

## Phase 4: Integration & Release (Sprint 12-15) - DONE

### Sprint 12-13: Integration (v1.0.0-alpha) - DONE
- [x] Full emulation loop (CPU + VIA per frame)
- [x] I/O callback wiring
- [x] 136 comprehensive tests (100% pass)
- [x] Build system (Makefile with test targets)
- [x] Command-line interface
- [x] Headless testing mode
- [x] Documentation updates

### Sprint 14: Testing & Optimization (v1.0.0-beta) - DONE
- [x] Test with real ORIC ROM images (basic10.rom validated)
- [x] Performance profiling (~90+ MHz, target met)
- [x] Memory leak analysis (Valgrind target ready)
- [x] Static analysis (run with strict flags, no critical issues)
- [x] ROM compatibility tests (12 tests, 100% pass)
- [x] Fix memory map comment (VIA at $0300-$030F)
- [x] Fix strncpy truncation warnings in hostfs.c
- [x] SDL2 display integration testing
- [x] Game compatibility testing — sprint 36b : suite régression 7 titres commerciaux (manic-miner, hopper, breakout, harrier-attack, atlantis, oric-trek, tyrann) via `make test-game-compat`

### Sprint 15: Video Integration & Debug Export (v1.0.0-beta.2) - DONE
- [x] Integrate video_t into emulator main loop
- [x] Connect charset ROM to video renderer
- [x] Render video frame after each CPU frame
- [x] PPM export (P6 binary, zero dependency)
- [x] BMP export (24-bit uncompressed)
- [x] ASCII export (ANSI true-color terminal)
- [x] CLI: --screenshot, --screenshot-at, --frame-dump, --frame-dump-interval
- [x] 11 video tests (text/HIRES render, PPM/BMP/ASCII export, ROM boot)
- [x] make test-video target

### Sprint 15b: PSG Bus Fix & Keyboard (v1.0.0-beta.3) - DONE
- [x] Fix PSG bus decode: CA2=BC1, CB2=BDIR (was swapped)
- [x] Fix ghost keypresses: VIA PB3 keyboard scan feedback
- [x] PSG Port A input callback for keyboard row data
- [x] SDL2 keyboard mapping (64 keys, QWERTY + AZERTY from Oricutron)
- [x] SDL2 event polling: keyboard input, F5/F10/F11/F12
- [x] Clean boot screen validated (no more stars)

### Sprint 16a: CLOAD ROM Patching (v1.0.0-beta.4) - DONE
- [x] CLOAD command support via ROM patching (getsync + readbyte interception)
- [x] Post-CLOAD BASIC rechain (next-line pointer fix for multi-block TAP)
- [x] TAP buffering for sequential reading by ROM routines
- [x] Sync byte detection (0x16) and byte-by-byte tape reading
- [x] -t without -f: buffered for CLOAD; -t -f: direct memory injection
- [x] CPU register injection (A, Z flag, $002F store)

### Sprint 16b: HIRES Fix & Keyboard Automation (v1.0.0-beta.5) - DONE
- [x] Fix HIRES mode detection ($26A bit 1, confirmed by ROM disassembly)
- [x] Fix HIRES attribute detection: (byte & 0x60)==0 (match Oricutron ULA)
- [x] Fix HIRES bit 7 inversion: ink/paper XOR 7
- [x] Fix HIRES charset address: $9800 in HIRES mode
- [x] Keyboard automation: oric_keyboard_press_char() / release_all()
- [x] --type-keys CYCLES:TEXT CLI option (escapes \n \e \u\d\l\r \Cx(CTRL) \Fx(FUNCT) \pN, Sprint 59)
- [x] Poker game compatibility validated (poker-asn.tap)

### Sprint 17: Microdisc & Sedoric Boot (v1.0.0-beta.6) - DONE
- [x] Microdisc controller emulation (I/O $0310-$031F)
- [x] WD1793 FDC connected to Microdisc interface with IRQ routing
- [x] Overlay ROM support (microdis.rom at $E000-$FFFF)
- [x] Memory system extended with upper_ram + overlay logic
- [x] --disk-rom option for Microdisc ROM loading
- [x] -d option functional: loads .DSK via Sedoric + FDC
- [x] Public headers for disk.h and sedoric.h
- [x] Test file updated to use proper headers

### Sprint 17b: Sedoric Boot Fix (v1.0.0-beta.7) - DONE
- [x] Fix RAM initialization: Oricutron-compatible pattern (128x00+128xFF per page)
- [x] Sedoric boot now loads full SYSTEM.DOS (60 sectors instead of 4)
- [x] Boot completes to "SEDORIC V4.0" prompt
- [x] Root cause: Sedoric boot checksum at $B932 needs non-zero upper_ram

### Sprint 17c: Level-Triggered IRQ Fix (v1.0.0-beta.8) - DONE
- [x] Fix CPU IRQ model: edge-triggered boolean → level-triggered bitfield
- [x] New API: cpu_irq_set()/cpu_irq_clear() with IRQF_VIA/IRQF_DISK sources
- [x] VIA IRQ callback handles both assertion and deassertion
- [x] Microdisc IRQ clear now functional (was no-op)
- [x] Keyboard works after Sedoric boot (T1 timer IRQ reasserts correctly)
- [x] Root cause: old model consumed IRQ flag on service, preventing reassertion
  when VIA IER was re-enabled by ROM code after Sedoric init

### Sprint 18a: Audio Output Integration (v1.0.0-beta.9) - DONE
- [x] Connect audio_init()/audio_cleanup() to emulation loop
- [x] Export audio API functions in audio.h header
- [x] SDL2 audio callback generates PSG samples (44100Hz stereo)
- [x] Sound now plays during emulation

### Sprint 18b: PSG Envelope Fix (v1.0.0-beta.10) - DONE
- [x] Fix envelope shapes 0-7: single decay/attack played once then silent (was cycling)
- [x] Fix envelope HOLD shapes (9,11,13,15): hold at final value
- [x] Fix emulator_init: headless/fast_load fields preserved from CLI args
- [x] Boot beep and Sedoric ding sound work correctly
- [x] Root cause: env_step reset to 0 at cycle end instead of holding at 31

### Sprint 18: Release Preparation (v1.0.0-rc) - DONE
- [x] Repository cleanup (debug binaries, screenshots excluded)
- [x] CMakeLists.txt synced with Makefile (microdisc.c, export.c)
- [x] Makefile: install/uninstall targets, test-audio target
- [x] 8 PSG audio tests (init, write/read, envelopes, mixer, tone, silence)
- [x] README.md updated with Quick Start section
- [x] CONTRIBUTING.md: build, style, testing guidelines
- [x] docs/COMPATIBILITY.md: tested programs list
- [x] docs/user-guide/README.md: complete user guide
- [x] Example BASIC programs (hello, sound, graphics) with .tap files
- [x] 155 total tests, 100% pass rate

---

## Phase 5: Advanced Features (Sprint 19+) - IN PROGRESS

### Sprint 66: Cadençage temps réel headless + Write Track WD1793 (v1.25.6-alpha) - DONE

> Deux limites concrètes rencontrées en produisant une vidéo terminal↔BBS,
> consignées ici avec fichier:ligne, impact et piste. **Les deux sont
> désormais corrigées** (--realtime en v1.25.5, Write Track en v1.25.6).

**Limite 1 — Pas de cadençage temps réel en headless (CORRIGÉE v1.25.5)**
- Constat : le frame limiter 50 Hz était sous `#ifdef HAS_SDL2` ET gardé par
  `if (!emu->headless)` (`src/main.c`) → en headless, ~25-45× le temps réel.
  Impact : avec un backend série réseau (`--serial modem:…`), le CPU court
  devant les allers-retours réseau → `--type-keys` désynchronise et XMODEM sur
  réseau réel expire (a empêché la vidéo de prod d'aller jusqu'au téléchargement).
- [x] **`--realtime`** : pacing à 50 Hz PAL même en headless via
  `clock_nanosleep(CLOCK_MONOTONIC, TIMER_ABSTIME)` (deadline absolu sans
  dérive, indépendant de SDL ; resync si on prend > 1 trame de retard sur un
  read réseau bloquant). Champ `emulator_t.realtime`.
- [x] Vérifié empiriquement : 1 M cycles (= 1 s émulée) → **0,04 s sans** vs
  **1,03 s avec** `--realtime` (~3 % d'overhead). Pas de test unitaire
  (comportement temporel → *flaky* en CI). 812 tests inchangés au vert.

**Limite 2 — « Write Track » (formatage WD1793) (CORRIGÉE v1.25.6)**
- Constat : `src/storage/disk.c` — « Write Track (Type III) - not fully
  implemented » ; `currentop = FDC_OP_WRITE_TRACK` posé sans handler de flux de
  données → no-op. Impact : `INIT` Sedoric ne formatait pas → impossible de
  créer une disquette vierge depuis l'émulateur.
- [x] **Parseur du flux IBM/MFM de piste** : machine à états dans le chemin DATA
  (`FDC_OP_WRITE_TRACK`, champs `wt_*`). Reconnaît IDAM (`0xFE`+track/side/
  sector/size) et DAM (`0xFB`/`0xF8`+données), ignore gaps/sync/contrôle CRC,
  place chaque champ de données dans le secteur nommé par le dernier ID
  (position logique image plate, `disk_modified` posé). Terminaison après
  `sectors_per_track` champs.
- [x] +1 test `test_fdc_write_track` (formate 4 secteurs via flux synthétique,
  vérifie les offsets). 813 tests.
- [x] **`--disk-create FILE`** (v1.25.7) : câble `sedoric_create()` au CLI
  (crée + écrit + monte une disquette vierge en lecteur A, arme le write-back).
- [x] **Validation e2e (v1.25.7)** : boot Sedoric V4 (SEDO40u) headless,
  pilotage `--type-keys` → BASIC → `INIT B` → `Y` → écran **« Formating … »**
  puis **« Formating complete »**, write-back `.dsk` déclenché → le Write Track
  est exercé et **correct face au vrai DOS Sedoric**.
- [x] **Raffinement géométrie (v1.25.8)** : `sedoric_create_blank(tracks, sides)`
  + inférence de géométrie raw par la taille (> 42 pistes & paire → double
  face). `--disk-create` crée un blank **double face 42 pistes** (365568 o).
  **Validation e2e bouclée** : `--disk-create` → `INIT B` → nom « TEST » →
  remontée + `DIR B` → « Drive B V4 (Slv) TEST … 2776 free sectors (D/82/17)
  0 Files » → disque double face **reconnu et valide**. +1 test
  `test_sedoric_create_blank_double_sided`. 814 tests.
  → **Chaîne création → INIT → DIR bouclée ; la limite #2 du Sprint 66 est
  entièrement close.**
- [x] **Disquette bootable hands-free (v1.27.1)** : `tools/make_bootable_sedoric.sh`
  pilote tout l'enchaînement (`--disk-create` → `INIT B`/`Y` → nom →
  **Master disc = Y** → copie du DOS) et **vérifie le boot standalone** →
  « SEDORIC V4.0 / © 1985 ORIC INTERNATIONAL / Ready ». **On peut désormais
  fabriquer une disquette Sedoric bootable depuis l'émulateur, sans intervention.**

### Sprint 65: OCULA — rendu overscan visible de la bordure (v1.25.0-alpha) - DONE

> Affichage réel de la bordure modélisée au Sprint 64, sans toucher au
> système de coordonnées de la zone active.

- [x] `video_compose_bordered()` (pur, testable headless) entoure le
      framebuffer actif d'une bande overscan (`OCULA_BORDER_W=32` /
      `OCULA_BORDER_H=24` px/côté) ; couleur `ocula_border[y]` par scanline
      dans les bandes G/D, 1re/dernière ligne pour les bandes haute/basse.
- [x] `renderer.c` compose puis met à jour la texture aux dimensions bordées
      (le renderer suit déjà tout changement de résolution).
- [x] `OCULA_BORDER_W/H`, `OCULA_BORDERED_MAX_W/H`, `video_bordered_w/h()`,
      `renderer_set_border()` / `get_border()`.
- [x] CLI `--no-border` (active par défaut), champ `emulator_t.no_border`.
- [x] 5 tests compositing dans `test_ocula.c` — 84 tests OCULA OK, suite verte.
- À faire :
  - **Migration in-band → registres write-only ROM** — validée par sodiumlb
    (25 juin, p=34926) : palette + bordure pilotables par écritures ROM
    write-only, zéro octet DRAM, lève l'objection `$BFE0-$C000`. Phasage :
    - [x] **Phase A (Sprint 66, v1.26.0-alpha)** : register-file ROM (pages
          `$E0-$E7` palette, `$EA` bordure) **en coexistence** avec l'in-band ;
          décodage `memory.c`, override `video.c` par scanline, savestate `OGP`,
          7 tests. Aucun test/démo cassé.
    - [x] **Phase C — cœur (Sprint 67, v1.27.0-alpha)** : page-3 RAM remappable
          (registre de mapping `$EB`, gated unlock) exposant une fenêtre R/W de
          256 o sur l'état OCULA (signature, caps, banque, palette, bordure) — la
          page-3 RAM apporte la **lecture d'état** absente du write-only et arme
          le register-file en écriture (unifie Phase A/C). Coexiste avec `$03E0`.
          6 tests, 98 OCULA. **Modèle proposé, à valider avec sodiumlb.**
    - [ ] **Phase C — reste** : migrer le GPU `$03E8` vers la fenêtre, déprécier
          `$03E0` + l'in-band, migrer démos/spec.
  - [x] **Exposer la synchro raster** — demandé par Dbug (25 juin, p=34925 :
        « VSync/HSync absolutely necessary… don't flash crazily ») : compteur de
        scanline lisible + wait HSync/VSync, pour timer les écritures registres
        (copper). **Livré Sprint 76 (v1.36.0)** : registres `$03EC` RASTER_LO /
        `$03ED` RASTER_STATUS (bit8/VBLANK/HBLANK). Reste à poster la réponse
        forum (l'interface choisie : page-3 GPU réservée plutôt qu'in-band).
  - [x] Option pour inclure la bordure dans l'export PPM/screenshot/AVI —
        **livré Sprint 77 (v1.37.0)** : `--export-border` (off par défaut),
        `video_export_auto_bordered()` + AVI bordé. Exports par défaut inchangés
        (tests d'export préservés). Vérifié e2e (304×272 vs 240×224).
  - [ ] Registres réservés $BFEB-$BFFF (BORDER_CTL index-palette, SCROLLX/Y
        in-band, SPLIT) — v2.
  - [x] Amendement forum t=2709 **posté** (2026-06-25) : réponse à sodiumlb
        alignée sur son schéma registres (`docs/forum-t2709-border-rgb332.md`).
        En attente de sa réponse (plage de pages pour palette/bordure) ; veille
        cloud surveille le fil.

### Sprint 64: OCULA — registre de bordure $BFEA (amendement RFC #53) (v1.24.0-alpha) - DONE

> Réponse à la demande répétée de Dbug (couleur de bordure + raster bars)
> sur le forum [t=2709](https://forum.defence-force.org/viewtopic.php?t=2709),
> et au choix RGB332 acté. Spec : `docs/ocula_extensions.md` v0.10
> (sections « Pourquoi RGB332 », « Fenêtre de latch », « Bordure et
> registres de position $BFEA-$BFFF »).

- Réalisé (v1.24.0-alpha) — **registre BORDER + modèle par scanline** :
  - [x] `$BFEA` latché par scanline sous le gating opt-in de la palette
        (profil OCULA + déverrouillage + magic `'O','C'`), décodage RGB332
        partagé avec la palette (`rgb332_to_rgb888()`), prédicat
        `ocula_block_armed()`, `border_latch()` dans `video.c`.
  - [x] Défaut sûr : `$00`/désarmé/ULA stock ⇒ bordure noire = Oric d'origine.
  - [x] Raster bars : réécriture de $BFEA entre scanlines → couleur par ligne.
  - [x] API `video_get_border_rgb(vid, y, …)`, champ `ocula_border[]`.
  - [x] 6 tests `test_ocula.c` (gating complet + raster) — 79 tests OCULA OK.
- [x] **Rendu visible** : livré au Sprint 65 (compositing overscan).
- À faire :
  - [ ] Registres réservés $BFEB-$BFFF (BORDER_CTL index-palette, SCROLLX/Y
        in-band, SPLIT) — v2.
  - [ ] Poster l'amendement sur le forum t=2709 + intégrer le retour
        sodiumlb/Dbug.

### Sprint 63 (DONE): Changement de média « hors Oric » à chaud — k7 / .dsk sans quitter l'émulateur

> Noté en fin de session : besoin d'un système **côté hôte** pour changer la
> cassette (.tap/k7) **ou** le disque (.dsk) **sans sortir de l'émulateur**.

- État existant :
  - [x] **Cassette** déjà chaud-échangeable côté hôte : commande `load-tap <path>`
        du protocole de contrôle IPC (`src/control.c`, Sprint 35b, OricForge).
  - [x] **ROM** : `load-rom <path>` (idem).
  - [x] UI web : boutons LOAD / EJECT (`web/shell.html`) — mais le chargement
        **redémarre** la machine (drag-drop relance `-t`/`-d`), pas un vrai insert live.
- Réalisé (v1.23.0-alpha) — **OSD natif (F6)** :
  - [x] **Touche/menu SDL** : overlay en surimpression (`src/video/osd.c`),
        F6 ouvre/ferme, navigation ↑/↓/Entrée/Échap, scan `tapes/`,`disks/`,
        `demos/ocula`,`.` (inspiré du file-requester d'Oricutron, clean-room).
  - [x] **Disque à chaud** : `sedoric_load()` + `microdisc_set_disk()` sur le
        lecteur A depuis l'OSD (la brique « disque à chaud » est livrée ici).
  - [x] **Cassette à chaud** : remplace `tapebuf` depuis l'OSD (puis `CLOAD""`).
  - [x] 5 tests `make test-osd`.
- Réalisé (v1.23.1-alpha) — **OSD : choix du lecteur cible (A–D)** :
  - [x] ←/→ font tourner le lecteur cible (A/B/C/D, wrap) ; en-tête « LECTEUR
        DISQUE CIBLE: X ». `osd_do_load()` charge dans `emu->disks[disk_drive]`
        + `microdisc_set_disk(drive)` (le Microdisc gère 4 lecteurs).
        `emu->disk_path` (UI/savestate) ne suit que le lecteur A.
  - [x] +1 test `test_osd_left_right_cycle_drive` (cyclage + wrap). 799 tests.
- Réalisé (v1.25.3-alpha) — **OSD : éjection disque + write-back avant swap** :
  - [x] Touche `Suppr`/`Retour arrière` → `OSD_EJECT` ; `osd_do_eject()` éjecte
        la disquette du lecteur cible (`microdisc_set_disk(drive, NULL,...)`),
        en-tête « SUPPR=EJECTER ».
  - [x] `osd_writeback_drive()` : sauvegarde `sedoric_save()` avant swap/éjection
        si `--disk-writeback` actif et lecteur *dirty*, puis reset du flag.
  - [x] Suivi par lecteur `emulator_t.disk_paths[4]` + `disk_writeback` (peuplé
        au démarrage, MAJ à chaque swap) ; le write-back de sortie utilise
        désormais `disk_paths[]` → les disques chargés/éjectés via l'OSD sont
        persistés correctement à la sortie.
  - [x] +1 test `test_osd_eject_returns_action`. 811 tests.
- Réalisé (v1.25.4-alpha) — **OSD : éjection de la cassette** :
  - [x] Touche `Suppr` éjecte selon le média surligné : action `OSD_EJECT_TAPE`
        si l'entrée est un `.tap` (sinon `OSD_EJECT` disque, inchangé).
  - [x] `osd_do_eject_tape()` libère `tapebuf` + remet à zéro
        `tapelen`/`tapeoffs`/`tape_loaded`/`tape_path`.
  - [x] +1 test `test_osd_eject_depends_on_selected_media`. 812 tests.
        → **L'OSD couvre désormais tout le hot-swap (charge/éjecte k7 + disque A–D).**
- À faire :
  - [x] **Commandes IPC** `load-disk <drive> <path>` / `eject-disk` / `eject-tape`
        dans `src/control.c` (write-back dirty + `--disk-writeback`), capability
        dans `CONTROL_PROTO_CAPS` — livré Sprint 75 (v1.35.0).
  - [x] **Insert live web** : pont C↔JS d'insertion à chaud (comme `web_key`)
        au lieu de relancer la machine — **livré Sprint 78 (v1.38.0)** :
        `web_insert_tap`/`web_insert_disk` + `loadMedia()` hot-swap (reload en
        repli). (emcc absent ici → validé syntaxe + revue, .wasm non généré.)
  - [x] Tests : `make test-control` (load-disk/eject-disk/eject-tape) — livré
        Sprint 75 (`tests/integration/test_control_media_swap.sh`, 9 assertions).

### Sprint 19: Interactive Debugger (v1.1.0-alpha) - DONE
- [x] Interactive REPL debugger module (src/debugger.c, include/debugger.h)
- [x] emulator_t extracted to shared header (include/emulator.h)
- [x] PC breakpoints (up to 16) with add/remove/list
- [x] Memory write watchpoints (up to 8) via memory_set_trace()
- [x] Single-step (s/step) and step-over (n/next for JSR)
- [x] Continue execution (c/continue)
- [x] Register display (r/regs) and modification (set reg val)
- [x] Disassembly at address (d [addr] [n])
- [x] Memory hex+ASCII dump (m addr [len])
- [x] Stack display (stack)
- [x] VIA 6522 state inspection (via)
- [x] PSG AY-3-8910 state inspection (psg)
- [x] CLI: --debug/-D (break at first instruction)
- [x] CLI: --break ADDR (initial debugger breakpoint)
- [x] F9 key enters debugger during SDL2 emulation
- [x] Legacy breakpoint (-b) promoted to interactive debugger
- [x] 8 debugger unit tests (163 total tests, 100% pass)

### Sprint 19b: VSync CB1 Signal (v1.1.1-alpha) - DONE
- [x] Generate ULA VSync pulse on VIA CB1 at end of each frame (50 Hz)
- [x] via_trigger_cb1() called in emulation loop after CPU frame cycles
- [x] Software can poll IFR bit 4 or enable CB1 interrupt for VBlank sync
- [x] Faithful to real Oric hardware (ULA → CB1 pin → VIA IFR bit 4)
- [x] 163 tests pass (no regression)

### Sprint 19c: VSync CB1 Improvements (v1.1.2-alpha) - DONE
- [x] PAL timing: 312 lines x 64 cycles = 19968 cycles/frame (was 20000)
- [x] VSync mid-frame: CB1 triggers at line 256 (cycle 16384) not end of frame
- [x] Edge-triggered CB1: via_set_cb1() with PCR bit 4 edge selection
- [x] cb1_pin field in via6522_t for pin level tracking
- [x] via_check_irq: transition-only /IRQ callback (irq_line field, no spurious clear)
- [x] VSync fires unconditionally like real hardware (IER guard removed)
- [x] Legacy via_trigger_cb1() refactored to use via_set_cb1() (pulse)
- [x] 5 new CB1 edge detection tests (168 total, 100% pass)

### Sprint 20: MJPEG Cast Server + mDNS Discovery (v1.2.0-alpha) - DONE
- [x] HTTP MJPEG streaming server (src/network/cast_server.c)
- [x] HTML viewer page at / with pixelated rendering
- [x] MJPEG stream endpoint at /stream (720x672 upscaled 3x)
- [x] Vendored stb_image_write.h for JPEG encoding (quality 80)
- [x] Thread-safe multi-client support (up to 8, select() I/O)
- [x] mDNS/DNS-SD Chromecast discovery (_googlecast._tcp.local)
- [x] DNS compression pointer support, PTR/SRV/A/TXT parsing
- [x] CLI: --cast-server[=PORT] (default 8080)
- [x] CLI: --cast-discover (standalone discovery mode)
- [x] Conditional build: CAST=1 (-DHAS_CAST -lpthread)
- [x] No-op stubs when CAST=1 not set
- [x] 13 cast server unit tests (181 total, 100% pass)

### Sprint 21: Native CASTV2 Client (v1.3.0-alpha) - DONE
- [x] Native CASTV2 client (src/network/castv2.c)
- [x] Hand-coded protobuf encoder/decoder (CastMessage, 6 fields)
- [x] TLS connection via OpenSSL (self-signed cert accepted)
- [x] Cast protocol: CONNECT → LAUNCH DashCast → RECEIVER_STATUS → send URL
- [x] Heartbeat thread (PING every 5s to receiver + transport)
- [x] mDNS discovery wrapper (castv2_discover_device with name filter)
- [x] Local IP detection (UDP connect to 8.8.8.8:53 + getsockname)
- [x] Clean disconnect (STOP → SSL_shutdown → close → pthread_join)
- [x] CLI: --cast-to[=DEVICE] (implicitly enables --cast-server)
- [x] Build: CAST=1 now adds -lssl -lcrypto
- [x] cast-play.sh simplified: native --cast-to, zero external deps
- [x] 8 new CASTV2 protobuf unit tests (189 total, 100% pass)

### Sprint 21b: WAV Audio Streaming (v1.3.1-alpha) - DONE
- [x] HTTP endpoint GET /audio: infinite WAV stream (mono PCM 16-bit, 44100 Hz)
- [x] Audio ring buffer (2s, 88200 samples) with mutex + per-client read positions
- [x] Stereo-to-mono downmix (cast_server_push_audio from SDL2 audio callback)
- [x] WAV header builder cast_build_wav_header() (44 bytes, infinite data size)
- [x] audio_set_cast_server() hooks SDL2 audio output to cast server
- [x] HTML `<audio autoplay>` in cast viewer page for Chromecast sound
- [x] 3 new audio streaming tests (192 total, 100% pass)

### Sprint 21c: Type Safety & Naming Consistency (v1.3.2-alpha) - DONE
- [x] Named struct tags for forward declaration support (cast_server_s, memory_s, etc.)
- [x] audio_set_cast_server: void* → cast_server_t* (type-safe parameter)
- [x] cpu6502_t.memory: void* → memory_t* (remove casts in cpu/addressing code)
- [x] pb_encode_enum → pb_encode_varint_field (accurate naming)
- [x] Unified cast_server_ prefix for all cast module helpers
- [x] handle_new_connection → route_http_request (descriptive naming)
- [x] int → size_t/uint32_t for buffer sizes, positions, sample counts
- [x] Removed broken forward declaration + unnecessary void* casts
- [x] 192 tests, 100% pass rate (no functional change)

### Sprint 21d: Full Codebase Type Safety Audit (v1.3.3-alpha) - DONE
- [x] Named struct tags for ALL 15 remaining typedefs (tap, sedoric, fdc, hostfs, video, microdisc, debugger, opcode_info, via6522, ay3891x, keyboard, emulator)
- [x] debugger.h: void* emu → emulator_t* with forward declaration
- [x] debugger.h: int num_breakpoints/num_watchpoints → uint8_t
- [x] microdisc.h: void* cpu_userdata → emulator_t*, typed IRQ callbacks
- [x] Const correctness: sedoric_read_sector, video_render_frame, internal helpers
- [x] Numeric types: fdc cur_sector_len/cur_offset → uint16_t, fdc_ticktock → unsigned int
- [x] video_export_ascii scale → unsigned int, BMP stride → unsigned int
- [x] VIA magic numbers → VIA_IER_MASK/VIA_INT_ANY constants
- [x] Null safety: localtime() check in logging.c
- [x] 192 tests, 100% pass rate (no functional change)

### Sprint 22: Save States (v1.4.0-alpha) - DONE
- [x] .ost (Oric Save sTate) binary file format with 48-byte header
- [x] CRC32 integrity check over serialized data
- [x] 10 sections: CPU, MEM, VIA, PSG, VID, KBD, FDC, MDC, TAP, META
- [x] Forward-compatible section parsing (unknown sections skipped)
- [x] savestate_save() / savestate_load() public API
- [x] CLI: --save-state FILE (save on exit), --load-state FILE (load at startup)
- [x] SDL2 hotkeys: F2 (quick save), F4 (quick load) to oric1_quicksave.ost
- [x] File path metadata tracking in emulator_t (rom, disk, diskrom, tape)
- [x] Internal pointer restoration after load (cpu.memory, video.screen_ram, fdc)
- [x] 8 save state unit tests (176 via make tests, 100% pass)

### Sprint 23: ORIC Atmos Support (v1.5.0-alpha) - DONE
- [x] oric_model_t enum (ORIC_MODEL_ORIC1, ORIC_MODEL_ATMOS) in emulator.h
- [x] ROM auto-detection: detect_rom_version() inspects JMP target at ROM offset 0
- [x] rom_patches_t struct with ROM-version-specific tape patch addresses
- [x] Static tables: rom_patches_basic10 (ORIC-1), rom_patches_basic11 (Atmos)
- [x] CLI: --model / -m (oric1, atmos, 1.0, 1.1) overrides auto-detection
- [x] tape_patches() refactored to use rom_patches table (both ROMs work)
- [x] emulator_t: model field, rom_patches pointer
- [x] 10 new Atmos unit tests (186 total, 100% pass)

### Sprint 24: IJK Joystick Support (v1.6.0-alpha) - DONE
- [x] IJK joystick interface (src/io/joystick.c, include/io/joystick.h)
- [x] ~~PSG Port A active low bit layout: 0=Left, 1=Right, 3=Down, 4=Up, 5=Fire~~
      **modèle faux — corrigé Sprint 37** (cf. ci-dessous)
- [x] Keyboard-as-joystick mode (--joystick keys): arrow keys + RCtrl/RAlt
- [x] SDL2 game controller mode (--joystick gamepad): D-pad, left stick, A/B/X
- [x] ~~Joystick/keyboard blending (AND on PSG Port A read)~~ **retiré Sprint 37**
- [x] SDL_INIT_GAMECONTROLLER + hot-plug device detection
- [x] CLI: -j/--joystick (keys, gamepad)
- [x] 10 new joystick unit tests (196 total, 100% pass)

### Sprint 37: Fix modèle IJK — port imprimante VIA direct (v1.16.83-alpha) - DONE
- [x] Protocole réel (validé Oricutron + testeur externe sur interface
      physique) : enable PB4 sortie+bas, select broches port A 6-7
      (bit6=stick A, bit7=stick B, les deux=aucun), état bits 0-5
      actif bas — layout réel Right=0, Left=1, Fire=2, Down=3, Up=4,
      bit 5 = présence (bas si interface activée)
- [x] oric_joystick_port_a_pins() pure + câblage via.porta_read
- [x] via6522: wired-AND des broches externes avec IRA (ORA/ORA_NH)
- [x] Suppression du blending clavier PSG (non conforme au matériel)
- [x] 7 nouveaux tests dont rejeu de la séquence 6502 d'un jeu réel
- [ ] Validation finale sur interface IJK physique (testeur forum)

### Sprint 51: Digitelec DTL 2000 fidèle — PIA 6821 + ACIA 6850 ($03F8-$03FD) (v1.17.0-alpha) - DONE
- [x] OCR (Tesseract fra) du manuel d'époque « Programmation carte DTL V23 »
      → sémantique bit-à-bit figée (points « à confirmer » résolus)
- [x] Module io/dtl2000 : PIA 6821 (4 reg) + ACIA 6850 (2 reg), distinct du
      backend comportemental digitelec/ACIA 6551
- [x] PIA OR bit2=connexion ligne, bit4=mode asym/sym, DDRA=$F4 ; ACIA reset
      $03, état RDRF/TDRE/DCD/CTS, émission porteuse bit6 ; timing V23 75/1200
- [x] Routage I/O $03F8-$03FD intercepté avant le miroir VIA (conflit page-3)
- [x] CLI --dtl2000 TRANSPORT (loopback/tcp/pty) + --dtl2000-addr
- [x] examples/dtl2000-test.bas (séquences POKE/PEEK transposées de l'OCR)
- [x] 12 tests bit-à-bit (make test-dtl2000), suite complète 535 tests PASS
- [x] Avertissement coexistence Microdisc/DTL (Jasmin) documenté
- [x] Câblage IRQ vers CPU → livré en Sprint 52 (v1.17.1-alpha)
- [ ] Validation contre un logiciel Oric d'origine (cassette DTL introuvable en ligne)

### Sprint 52: DTL 2000 — câblage IRQ ACIA 6850 → CPU (v1.17.1-alpha) - DONE
- [x] Nouvelle source IRQF_DTL2000 (0x08) dans le bitfield level-triggered 6502
- [x] dtl2000_cpu_irq_set/clr reliés à emu->dtl2000.irq_set/clr à l'init
      (re-câblés après le ré-init de base via --dtl2000-addr)
- [x] IRQ réception (RIE + RDRF) et émission (TC=01 TIE + TDRE), désassert
      à la lecture des données / écriture, level-triggered
- [x] 3 nouveaux tests IRQ (désactivée par défaut, RX avec RIE, TX avec TIE)
- [x] Suite complète 538 tests PASS

### Sprint 55: Retour d'intégration OricTel — corrections série & build (v1.19.1-alpha) - DONE

Rapport d'intégration de l'équipe OricTel (émulateur Minitel 1B), qui pilote
l'ACIA 6551 `$031C` via les backends DTL 2000 / PicoWiFi. Trois corrections
côté Phosphoric + un point de confort documenté.

- [x] **#1 — Verrou œuf-poule DCD du backend Digitelec** (`src/io/acia6551.c`) :
      `acia_tick()` ne sondait `poll()` que si `dcd` était déjà vrai, alors que
      le backend Digitelec lève DCD *depuis l'intérieur* de `poll()` sur front
      DTR → en réception pure, aucun CARRIER DETECT avant une frappe clavier.
      `poll()` est désormais inconditionnel ; le garde `dcd` ne conditionne plus
      que la livraison RDRF. Régression `test_dcd_deadlock_autodial`.
- [x] **#2 — Dial PicoWiFi `ATD<hôte>` p/t tronqué** (`src/io/serial_picowifi.c`) :
      `pw_match` insensible à la casse faisait correspondre `Dp` à `DP` (pulse) ;
      `ATDpavi.3617.fr` perdait le `p` initial. Modificateur T/P reconnu via
      `pw_match_cs` sensible à la casse (majuscule seule). Tests
      `test_dial_host_starting_with_p`, `test_dial_uppercase_modifier_keeps_host`.
- [x] **#4 — CMakeLists.txt obsolète** : sources manquantes (dont `symbols.c` →
      `undefined reference symbol_lookup/resolve`) réalignées sur le `Makefile` ;
      `-DHAS_SDL2` ajouté ; version projet 1.19.0. Build CMake vérifié OK.
- [x] **#3 (confort)** — binaire headless par défaut (`SDL2 ?= 0`) documenté dans
      le README (`make SDL2=1` pour l'affichage). Pas de changement de comportement.
- [x] Suite complète 603 tests PASS (+3 : 1 serial, 2 picowifi)

### Sprint 56: Fidélité ULA — double hauteur en parité de rangée absolue (v1.19.2-alpha) - DONE

Le rendu de l'attribut texte double hauteur (`0x0A`) employait un modèle de phase
*dépendant du contenu* qui divergeait du vrai ULA. Confirmation du modèle réel sur
la source Oricutron avant correction.

- [x] **Modèle Oricutron confirmé** (`ula.c`) : `vid_chline = (y>>1)&7` quand
      l'attribut double est actif, sinon `y&7`. C'est de la **parité de rangée
      absolue** (flip-flop implicite reset en haut de trame), pas un suivi par
      contenu.
- [x] **Correction** (`src/video/video.c`) : `effective_chline()` remplace
      `vid->dbl_phase` par `(row & 1)` — équivalent algébrique de `(y>>1)&7` avec
      `y = row*8 + chline`. Rangée paire → moitié haute (glyphe 0-3), impaire →
      moitié basse (glyphe 4-7). Le quirk « aligner sur rangée paire sinon
      brouillé » est désormais reproduit.
- [x] **Suppression code mort/erroné** : tout le suivi de phase
      (`row_had_double`, blocs `chline == 7`, reset par trame) et les champs
      `video_t.dbl_phase` / `dbl_was_active` (non sérialisés en savestate).
- [x] **Test** : `test_double_height_row_parity` (rendu réel) — texte normal
      (chline absolu) + alignement pair (haut/bas) + quirk impair (bas/haut).
- [x] Cas aligné sur rangée paire (le plus courant) : sortie inchangée, non-régression.
- [x] Suite complète 604 tests PASS (+1 video)

### Sprint 60l: Module ACIA 6850 (MC6850) autonome + rebranchement DTL (v1.21.11-alpha) - DONE

- [x] **Module ACIA 6850 complet et réutilisable** (`src/io/acia6850.c` + `.h`) :
      Control/Status + Tx/Rx Data, word-select (trame+masque), transmit-control
      (RTS bas / Tx IRQ), RIE, statut RDRF/TDRE/DCD/CTS/IRQ, chemin RX/TX, IRQ
      (RIE&RDRF | TIE&TDRE), pins DCD/CTS en entrées, callback IRQ.
- [x] **DTL 2000 rebranché** : 7 champs ACIA ad-hoc → `acia6850_t` ; le DTL garde
      le baud/timing V23, le backend et le gating TX, pilote DCD/CTS et reçoit
      l'IRQ par callback. Aucune régression (15 tests DTL + e2e + OTRM identiques).
- [x] **DTL 2000 désormais entièrement composé de PIA 6821 + ACIA 6850 réutilisables.**
- [x] Suite `make test-acia6850` (11 tests). 655 tests PASS (+11).

### Sprint 60m: Commande AT$SCAN (liste des réseaux WiFi) du backend picowifi (v1.21.12-alpha) - DONE

- [x] **`AT$SCAN` dans le backend `picowifi`** (`src/io/serial_picowifi.c`, helper
      `pw_scan()` + branche de dispatch avant `$SSID`) : émule la commande du
      firmware PicoWiFiModemUSB (fork TLS `benedictemarty`) listant les points
      d'accès à portée — un AP par ligne `<index> <ssid>` (1-based), dédupliqué par
      SSID, SSID cachés (vides) ignorés, terminé par `OK`.
- [x] **Scan réel via `nmcli -t -f SSID dev wifi`** (l'émulateur tourne sur desktop),
      avec repli sur une liste simulée (AlterOP New / Livebox-E380 / Freebox-39030C)
      si `nmcli` est absent (CI/headless) → toujours une liste numérotée exploitable.
- [x] **Pas de collision** de préfixe avec `$SSID`/`$SB`/`$SU`/`$SP` (`pw_match` teste
      un préfixe, `$SCAN` diverge au 3ᵉ caractère).
- [x] Permet un menu numéroté des réseaux côté programme Oric de config WiFi (via
      LOCI, ACIA $0380) au lieu d'une saisie manuelle du SSID — comportement
      identique vrai Pico W / émulation. Origine : note équipe firmware
      (`note-equipe-phosphoric-atscan.md`).
- [x] Test `test_scan_lists_networks` (`tests/unit/test_picowifi.c`, 65 tests picowifi).
      656 tests PASS (+1).

### Sprint 60n: Fix CTRL+lettre (CTRL+T caps-lock) en mode clavier symbolique (v1.21.13-alpha) - DONE

- [x] **Bug** : en `-k azerty` (mode symbolique, `src/io/keyboard.c` `handle_symbolic`),
      une lettre n'entrait dans la matrice qu'à l'arrivée de `SDL_TEXTINPUT` ; quand
      CTRL est maintenu, l'OS supprime ce `TEXTINPUT` → la lettre n'atteignait jamais
      la matrice et **CTRL-T (toggle CAPS LOCK, ROM `$F6ED`), CTRL-C (BREAK), etc.
      restaient sans effet**.
- [x] **Fix** : le `KEYDOWN` mappe désormais la lettre directement quand `KMOD_CTRL`
      est actif, **via le keycode `SDLK_a..SDLK_z`** (= caractère réellement produit
      par le clavier) → correct en AZERTY/QWERTZ comme en QWERTY.
- [x] Mode QWERTY positionnel (défaut), qui route déjà CTRL+T via `qwerty_tab`, inchangé.
- [x] Tests `test_azerty_ctrl_t_caps_toggle` + `test_qwerty_ctrl_t_caps_toggle`
      (`make test-keyboard`, 34 tests). 658 tests PASS (+2).

### Sprint 60o: Backend picowifi firmware v0.2.0 — terminaison TLS réelle (v1.21.14-alpha) - DONE

- [x] **Terminaison TLS dans le modem émulé** (`src/io/serial_picowifi.c`),
      fidèle au firmware réel PicoWiFiModemUSB **v0.2.0** (`mbedTLS`/`altcp_tls`
      côté Pico W → **OpenSSL** ici). Le modem déchiffre le lien et présente du
      **clair** à l'Oric (qui ne fait aucune crypto à ~1 MHz) → accès BBS/HTTPS
      chiffrés. Gardé par `HAS_PICOTLS` ; sans TLS compilé, un appel sécurisé
      renvoie `NO CARRIER`.
- [x] **Préfixe dial `#`** combinable (`ATDT#host:992`, `ATDT#=host`) → appel TLS.
- [x] **`ATGET https://host[:port][/path]`** — GET HTTPS (port 443 défaut),
      flux déchiffré streamé à l'Oric.
- [x] **`AT$CV?/0/1`** — vérification du certificat serveur ; `AT$CV1` **refusé**
      (`ERROR`) sans CA chargé.
- [x] **`AT$CA?/=/-`** — magasin CA : taille / upload PEM (terminé par ligne `.`) /
      suppression ; CA + `AT$CV1` → vérif stricte chaîne + hostname (`SSL_VERIFY_PEER`).
- [x] Abstraction **`pw_conn_read/write/close/pending`** (TLS↔socket) sur tout le
      chemin data + `poll`/`SSL_pending`.
- [x] Makefile : flag **`PICOTLS`** (auto-détecte OpenSSL via `pkg-config` ; `0`/`1`).
- [x] **Validé live** (réseau réel) : `ATGET https://example.com/` → handshake
      **TLSv1.3**, l'Oric reçoit `HTTP/1.1 200 OK` + HTML en clair. Vérif certificat
      prouvée 2 sens : root Comodo AAA (1517 o) → `TLS session up (verified)` +
      CONNECT ; CA auto-signé non concordant → `TLS verify failed` → pas de session
      (miroir validation badssl.com du firmware).
- [x] 6 tests hermétiques (`make test-picowifi` 65 → **71**). 664 tests PASS (+6).

### Integration: Fusion de la branche OCULA dans main — sprints 39-46 (v1.22.0-alpha) - DONE

Intégration de toute la chaîne OCULA (banc d'essai logiciel du remplacement
ULA par RP2350), développée en parallèle sur branche, réconciliée avec la
ligne 1.21.x de main (MIDI, web/WASM, write-back disque, savestate). Détail
par sprint conservé dans l'historique git de `feature/sprint*-ocula-*`.

- [x] Profil ULA enfichable (`--ula ula|ocula`, `video_set_profile`, survit
      au reset) — sprint 39
- [x] Mode texte 80 colonnes + miroir BASIC (`--ocula-80col-basic`) — sprints
      40 / 44
- [x] HIRES étendu 320×200 + palette redéfinissable $BFE0-$BFE7 relue par
      scanline (split/rasters/plasmas) — sprints 41 / 46
- [x] Banking mémoire $A000-$BFFF (8 banques) + registre d'identification —
      sprint 42
- [x] OCULA-GPU $03E8-$03EF (FILL/COPY/SCROLL/WAIT_VBL, étirement PHI0) —
      sprint 43
- [x] Déverrouillage opt-in par écriture aveugle ROM (knock 'O'/'C' page
      $FB00) — sprint 45
- [x] Sections savestate OCULA (profil/`OCB`/`OGP`) rétrocompatibles
- [x] Conflits résolus : double-hauteur de main conservée (`effective_chline`
      row-based, Sprint 34r) ; `get_rgb(vid, …)` par instance d'OCULA adopté
      partout (palette). 793 tests PASS (test-ocula 73/73).
- [x] Unifier les deux mécaniques de double-hauteur : code mort `dbl_phase`
      supprimé (v1.22.1-alpha), seule la voie `effective_chline` row-based
      subsiste
- [ ] RGB444 (4096 teintes Multicoloric) — à coordonner avec sodiumlb

### Fix: Write-back MFM_DISK in-place — corrige la corruption des .dsk réels (v1.21.30-alpha) - DONE

Le write-back `.dsk` du Sprint 62 corrompait les disques MFM_DISK (le format
de tous les vrais disques Oric) : `sedoric_save()` dumpait le buffer plat des
secteurs par-dessus le conteneur MFM, de taille différente.

- [x] `sedoric_save()` aiguille raw vs MFM via `is_mfm`
- [x] Conteneur MFM d'origine conservé au load (`mfm_raw`/`mfm_raw_size`)
- [x] `sedoric_save_mfm()` + `mfm_inject_track()` : réinjection en place de
      chaque secteur 256 o (inverse de `mfm_extract_track`) avec recalcul du
      CRC-16/CCITT (`mfm_crc16`) → image MFM valide relisible
- [x] `sedoric_destroy()` libère `mfm_raw`
- [x] +1 test `test_sedoric_mfm_writeback_roundtrip` (réinjection + CRC +
      taille MFM préservée) ; E2E réel SAVE→.dsk MFM intact→recharge→LIST ;
      Valgrind clean. 728 tests PASS.
- [x] Corriger le log `main.c` (affichait `disk->size` plat au lieu des octets
      MFM réellement écrits) — **corrigé v1.36.1** : `is_mfm ? mfm_raw_size :
      size`, cohérent avec `sedoric_save()`.

### Sprint 62: Persistance des sauvegardes in-game — write-back .dsk + capture savestate (v1.21.29-alpha) - DONE

- [x] **Diagnostic confirmé** : l'écriture de secteur FDC (`disk.c` `$A0`) mute
      l'image en mémoire (relecture en session OK) mais rien ne persistait
      (`sedoric_save()` jamais appelé ; `.ost` ne capturait pas `disk->data`).
- [x] **Section `DSK` du savestate** (`src/savestate.c`) : capture les 4 images
      lecteurs (index, géométrie, taille, octets), restaurées au chargement
      (réalloc si taille différente, `microdisc_set_disk` re-pointe FDC+Microdisc).
      Format rétro/avant-compatible (section inconnue ignorée + `fseek` borné →
      pas de bump de version `.ost`).
- [x] **Write-back `.dsk` opt-in** (`--disk-writeback`) : sur sortie, les lecteurs
      réellement écrits sont réécrits via `sedoric_save()`. Opt-in pour ne jamais
      écraser un `.dsk` par accident ; seuls les lecteurs *dirty* sont sauvés.
- [x] **Suivi dirty** : `fdc_t.disk_modified` → `microdisc.disk_dirty[drive]`.
- [x] +3 tests (`test_fdc_write_sets_modified`, `test_sedoric_save_roundtrip`,
      `test_save_load_disk_image`) ; smoke e2e (.dsk intact si rien écrit).
      727 tests PASS.
- [ ] Pistes futures : write-back non destructif vers `*.save.dsk` ; e2e Sedoric
      SAVE complet (write guest réel) ; flag dirty exposé dans l'UI/contrôle IPC.

### Sprint 61: Interface MIDI Mageco/ORICON — MC6850 $03FE/$031C + MIDI temps réel (ALSA/CoreMIDI/WinMM) + lecteur .mid SMF (v1.21.25 → v1.21.28-alpha) - DONE

- [x] **Carte MIDI Mageco** (`src/io/mageco.c` + `include/io/mageco.h`) émulant
      l'interface MIDI Mageco pour Oric (forum Defence-Force t=2525, relancée par
      Dbug en shield MIDI « Oric-Con ») : un MC6850 ACIA mappé à **$03FE-$03FF**
      (`$03FE` Control/Status, `$03FF` Tx/Rx Data) pilotant les 2 prises DIN-5 MIDI
      (IN optoisolé TIL 111, OUT direct). Réutilise le module `acia6850.c` autonome
      (Sprint 60l) — seule la couche carte est neuve (~210 LOC).
- [x] **Cadence MIDI 31250 baud, 8-N-1** côté hôte (le 6850 n'a pas de baud
      interne) : 10 bits/trame → 320 cycles/octet à 1 MHz (TX + RX).
- [x] **Aucun handshake modem** : DCD/CTS épinglés « présent/clair » (boucle de
      courant unidirectionnelle), l'émetteur met toujours l'octet écrit sur le fil.
- [x] **Option CLI `--mageco TRANSPORT`** (+ `--mageco-addr ADDR`, défaut `03FE`)
      sur les backends transparents : `file:IN[:OUT]` capture/rejoue le flux MIDI
      brut (`.mid`/`.syx`), `loopback`/`tcp`/`pty`. IRQ via `IRQF_MAGECO`.
- [x] **Routage I/O** `$03FE-$03FF` intercepté devant le miroir VIA $0300-$03FF
      (averti si Microdisc — conflit d'adresses signalé par le forum).
- [x] **Équivalence émulateur ↔ Oric réel + carte prouvée** : flux d'octets
      identique à un vrai Oric+Mageco vu via une interface USB-MIDI (seul le fil est
      virtuel). Étude `docs/specs/mageco-midi-feasibility.md`.
- [x] Suite `make test-midi` (**13 tests**) + validation e2e binaire (boot BASIC →
      `POKE1022,21`+`POKE1023` → capture `90 3C 7F`). 713 tests PASS (+13).
- [x] **Niveau B — backend MIDI temps réel ALSA** (v1.21.26-alpha) : transport
      `midi[:TARGET]` (`SERIAL_BACKEND_MIDI`, `src/io/serial_backend.c`) ouvrant un
      port séquenceur ALSA nommé/souscriptible « Phosphoric MIDI:Mageco MIDI »
      (visible `aconnect -l`). Émission octets→évènements (`snd_midi_event`),
      réception évènements→octets. Build optionnel `MIDI=1` (lie `-lasound`), stub
      NULL sinon. Auto-connexion `--mageco midi:128:0`. L'Oric émulé **pilote
      FluidSynth/DAW**, un clavier MIDI **joue dans l'Oric**. Validé temps réel
      (ALSA 1.2.14, `aseqdump` reçoit le Note On du BASIC). +1 test. 714 tests PASS.
- [x] **Lecteur de fichiers `.mid` (SMF)** (v1.21.27-alpha) : parseur Standard
      MIDI File (`src/io/smf.c`, format 0/1, division PPQN/SMPTE, carte de tempo
      intégrée, running status, SysEx) → transport `smf:FILE[:loop]`
      (`SERIAL_BACKEND_SMF`) rejouant un `.mid` **dans** l'Oric en MIDI IN cadencé
      à l'horloge temps réel `CLOCK_MONOTONIC` (tempo respecté). +6 tests
      (`make test-smf`). Validé temps réel (`90 3C 7F` → `80 3C 00` à +300 ms).
- [x] **Portages CoreMIDI (macOS) / WinMM (Windows)** (v1.21.27-alpha) : backend
      temps réel `midi[:TARGET]` porté sur ces plateformes (même vtable/factory que
      ALSA, `Makefile` détecte `uname`). ⚠️ écrits selon les API documentées mais
      **non compilés/vérifiés** sur l'hôte Linux ; branche par plateforme, build
      Linux inchangé — à valider sur macOS/Windows réels.
- [x] **Mode ORICON ($031C-$031F)** (v1.21.28-alpha) — après lecture **complète**
      du fil (4 pages, la 1ʳᵉ version ne couvrait que la page 1) : le fil décrit
      2 conceptions, la carte Mageco d'origine (#3FE/#3FF, Dbug) et le reboot
      **ORICON** (iss, p.3, vérifié verbatim). Ajout de `mageco_init_oricon`
      (MC6850 à $031C/$031D + générateur d'horloge $031E/$031F en latches, fenêtre
      4 registres, décodage placé devant le Microdisc), option `--oricon TRANSPORT`.
      Mode Mageco conservé. +4 tests, validé e2e (`POKE796/797` → `90 3C 7F`).
      Étude corrigée (distinction Mageco/ORICON, SAM2695, midi2oric).
- [ ] Pistes futures : validation CoreMIDI/WinMM sur matériel réel ; encodage exact
      du générateur d'horloge ORICON $031E/$031F (non publié dans le fil) ; SAM2695
      GM intégré (shield MIDI-2) ; SMF SMPTE étendu ; tempo-warp à la volée.

### Sprint 60y: Séquençage multi `--type-keys` — automatisation multi-écrans (v1.21.24-alpha) - DONE

- [x] **`--type-keys` répétable** (`src/main.c`, `include/emulator.h`) : jusqu'à
      `TYPE_KEYS_SEQ_MAX = 16` entrées empilées dans `type_keys_seq[]`, triées par
      cycle d'armement croissant.
- [x] **Activation séquentielle** par la boucle d'émulation : l'entrée suivante
      est chargée dès que la précédente est terminée ET que son cycle est atteint.
- [x] **Vrai relâchement clavier entre entrées** : `oric_keyboard_release_all()`
      (+ `loci_kbd_clear()` si LOCI), reset `last_char`/`debounce` → deux entrées à
      touches identiques (1 au cycle X, 1 au cycle Y) sont vues distinctement par le
      `wait_release` des TUI/terminaux LOCI. Remplace « un seul `--type-keys` retenu ».
- [x] **Diagnostic corrigé** : `\pN` est en cycles émulés (`ORIC_CLOCK_HZ * N`),
      déterministe en `--headless` — un parcours figé venait d'un budget `-c N` trop
      court, pas d'une pause en temps réel.
- [x] Aide `--help` mise à jour. Test `test_type_keys_seq_fields`. 700 tests PASS (+1).
- [ ] Pistes futures : pause `\pN` à granularité fine (ms/cycles explicites),
      synchronisation sur événement (PC/mémoire) plutôt que cycle fixe.

### Sprint 60x: Polissage UI web — filtre CRT, LEDs I/O, save/load état (v1.21.23-alpha) - DONE

- [x] **Filtre CRT** (`web/shell.html`) : overlay scanlines + vignette + boost
      phosphore, bouton **CRT** du rail, persistant `localStorage`, 100% CSS.
- [x] **LEDs d'activité TAPE/DISK** sous l'écran via export
      `web_io_activity()` (`tape_readbyte_active` / `fdc.status & FDC_ST_BUSY`).
- [x] **Sauvegarde/restauration `.ost` depuis l'UI** : boutons **SAVE**
      (download instantané) / **REST** (upload + restauration **à chaud**),
      exports `web_save_state()` / `web_load_state()` (`savestate_save/load`).
- [x] Exports `__EMSCRIPTEN__`-only → natif inchangé. Validé Chromium headless
      (CRT, round-trip état SAVE=1/65799 o/LOAD=1). 699 tests inchangés.
- [ ] Pistes futures : courbure d'écran CRT (WebGL), persistance auto de l'état
      (resume on reload), slots de sauvegarde multiples.

### Sprint 60w: Intégration des contributions LOCI de xahmol (v1.21.22-alpha) - DONE

- [x] **PR #10** intégrée : `op_unlink` (`src/io/loci_fs.c`) supprime les dossiers
      vides (`stat`+`rmdir`), `ENOTEMPTY`→`LOCI_EACCES` — conforme `FatFs f_unlink()`.
- [x] **PR #19** intégrée : `op_write_xstack` déduit le count de
      `XSTACK_SIZE - xstack_ptr` (conforme `std_api_write_xstack`,
      `sodiumlb/loci-firmware`) au lieu d'un `uint16` explicite.
- [x] **PR #20 déclinée** : `\fN` déjà couvert par `\Fx` (HID LOCI, Sprint 59,
      validé LFMV2) + collision sentinelle DEL `0x84` (clavier web 60v).
- [x] 2 tests LOCI ajoutés, `test_write_read_roundtrip` corrigé
      (`make test-loci` 150→**152**). 699 tests PASS.
- [x] PR #10/#19 fermées (réf. commit d'intégration), #20 fermée avec explication.

### Sprint 60v: UI web — sélecteur ROM, glisser-déposer, clavier virtuel, Reset (v1.21.21-alpha) - DONE

- [x] **Page web façon JOric** (`web/shell.html`) : rail d'icônes vertical à
      gauche (MODEL ORIC-1/Atmos relance à froid `sessionStorage`, LOAD +
      glisser-déposer `.tap`/`.dsk` via IndexedDB→FS virtuel `preRun`, EJECT,
      RESET, KEYS, FULL).
- [x] **Clavier virtuel ORIC fidèle en overlay semi-transparent masquable**
      (bouton KEYS) : disposition réelle (ESC/CTRL/FUNCT/2×SHIFT/RETURN/DEL/
      SPACE/flèches), modificateurs collants CTRL/FUNCT/SHIFT, **symboles
      shiftés dérivés de la matrice réelle** (`char_map` : 2→@, 6→^…),
      **FUNCT absent en ORIC-1** (touche Atmos-only).
- [x] **Plein écran** : canvas centré et mis à la hauteur d'écran (classe `.fs`
      posée en JS + `!important` battant le style inline SDL, ratio conservé).
- [x] **Pont clavier C↔JS** (`src/main.c`, `__EMSCRIPTEN__`) : `web_key()` +
      `web_key_release_all()` écrivent la matrice ORIC → **la touche CTRL à
      l'écran contourne le vol de CTRL+T par le navigateur**. Sentinelle DEL
      `0x84` (`src/io/keyboard.c`).
- [x] Test natif `test_press_char_del_sentinel` (34→35). **Validé live**
      (Chromium headless) : `PRINT 6*7`+RET via web_key → `42` ; ORIC-1 sans
      FUNCT ; overlay + plein écran centré vérifiés. 697 PASS.
- [ ] Pistes futures : filtre CRT, indicateur disque/cassette, PWA installable,
      sauvegarde/chargement d'état (.ost) depuis l'UI.

### Sprint 60u: Build WebAssembly — l'émulateur dans le navigateur (`make wasm`) (v1.21.20-alpha) - DONE

- [x] **Cible `make wasm`** (Emscripten) : émulateur complet compilé en WASM+JS,
      rendu `<canvas>`, audio Web Audio, clavier DOM (réutilise le port SDL2) ;
      sortie `web/phosphoric.{html,js,wasm,data}`, ROMs préchargées, shell
      `web/shell.html`, démarre l'Atmos.
- [x] **Boucle principale compatible navigateur** : sous `__EMSCRIPTEN__`, le
      limiteur de frame (`src/main.c`) cède via **Asyncify** (`emscripten_sleep`,
      ~50 Hz) ; `-sSTACK_SIZE=8MB` (gros `emulator_t` sur la pile).
- [x] Réseau natif (série TCP/PTY/COM, GDB, cast, TLS) lié en no-op navigateur ;
      cœur machine + vidéo + audio + clavier + cassette + disque OK.
- [x] **Fidélité prouvée** : sortie WASM byte-identique au natif (boot Atmos
      headless sous Node.js → même capture PPM à 2M et 5M cycles). Build propre
      (0 warning). Doc `docs/wasm.md`. Artefacts générés ignorés par git.
- [ ] Pistes futures : UI web (boutons reset/load TAP-DSK, sélecteur ORIC-1/Atmos,
      glisser-déposer de fichiers), relais WebSocket pour rétablir le modem/TLS
      dans le navigateur, plein écran + filtres CRT, PWA installable.

### Sprint 60t: Record/replay déterministe des entrées (movie TAS) (`--record`/`--replay`) (v1.21.19-alpha) - DONE

- [x] **Module movie** (`src/utils/movie.c` + `include/utils/movie.h`) :
      capture la matrice clavier 8 octets (seule entrée non déterministe) par
      frame, la rejoue bit-à-bit. Format texte diffable, changements seulement,
      le rejeu conserve l'état entre changements.
- [x] **Options CLI** `--record FILE` / `--replay FILE` (ignore le clavier live) ;
      hook au sommet de la boucle frame (matrice avant l'exécution CPU) ;
      auto-sortie headless en fin de movie (régression CI sans surveillance).
- [x] **Rejeu bit-déterministe prouvé** (2 rejeus → captures identiques) →
      tool-assisted runs, repro de bugs, régression CI. Inédit chez les
      émulateurs Oric.
- [x] 5 tests unitaires (`make test-movie`) + 3 vérifs intégration
      (`make test-movie-replay`). 696 tests PASS (+5).
- [ ] Pistes futures : enregistrer un savestate d'amorce dans le movie (replay
      depuis un point arbitraire) ; granularité cycle pour capturer aussi
      `--type-keys` à l'identique ; timeline scrubbable + frame-advance (TUI/GUI).

### Sprint 60s: Stub GDB remote — déboguer le 6502 depuis GDB/IDE (`--gdb`) (v1.21.18-alpha) - DONE

- [x] **Serveur GDB Remote Serial Protocol** (`src/network/gdbstub.c` +
      `include/network/gdbstub.h`) : `gdb`/lldb/IDE s'attache via
      `target remote :PORT` ; breakpoints, single-step, lecture/écriture
      registres+mémoire du 6502. **Inédit chez les émulateurs Oric.**
- [x] Option CLI **`--gdb[=PORT]`** (défaut 1234) ; réutilise `debugger_t`
      (breakpoints GDB ↔ REPL natif = même modèle) ; démarre arrêté au reset.
- [x] Commandes RSP : `? g G p P m M c s Z0/z0 Z1/z1 Z2/z2 H D k`,
      `qSupported/qAttached/qC/qfThreadInfo/qsThreadInfo/qOffsets/qSymbol`,
      `qXfer:features:read:target.xml`, `QStartNoAckMode`, `vCont?/vCont`,
      interruption Ctrl-C (poll par frame), gestion déconnexion.
- [x] Logique protocole (checksum/dispatch) séparée du transport socket →
      testable hors réseau. Câblé dans `main.c` (branche `gdb_mode`).
- [x] 14 tests (`make test-gdbstub`) + validé live (client RSP : handshake,
      registres=vecteur reset $F88F, mem R/W, breakpoint, set PC, step→S05,
      detach). 691 tests PASS (+14).
- [ ] Pistes futures : `vFlashErase/Write` (programmation), hardware watchpoints
      R (`Z3`), expose la table de symboles via `qSymbol`, intégration `.gdbinit`
      + extension VS Code (cc65/ld65 debug info).

### Sprint 60r: Debugger — assembleur inline (`a`) + recherche mémoire (`find`) (v1.21.17-alpha) - DONE

- [x] **Assembleur inline** (`src/debugger.c`, `assemble_one()`) : commande
      `a addr MNEMONIC [operand]` assemblant une instruction 6502 en place et
      écrivant les octets en mémoire. Tous les modes : implicite, accumulateur,
      immédiat, ZP/absolu (auto ZP↔ABS selon largeur du littéral), `,X`/`,Y`,
      indirect `($nnnn)`, `($nn,X)`, `($nn),Y`, branches relatives (offset
      calculé + contrôle ±128). Reverse-lookup sur `opcode_table[]`.
- [x] **Recherche mémoire** : commande `find B1 [B2…]` (octets hex) ou
      `find "texte"` (ASCII), balayage $0000-$FFFF **sans effet de bord** (RAM
      array pour $0000-$BFFF, vue CPU pour $C000-$FFFF), résolution symbolique.
- [x] `debugger_repl_run_line()` exposé dans `include/debugger.h` (au lieu d'un
      `extern` dans `tui.c`).
- [x] Ferme les 2 derniers gaps debugger vs Oricutron ; les autres
      (bp conditionnels, raster, disasm paginé, symboles, édition mémoire,
      undo/rewind) étaient déjà présents.
- [x] 5 tests (`make test-debugger` 8 → **13**) : 12 formes d'adressage, branches
      avant/arrière, rejets, `find` octets + ASCII. 677 tests PASS (+5).
- [ ] Piste future : labels/macros dans l'assembleur ; `find` dans une plage
      d'adresses ; assemblage multi-lignes.

### Sprint 60q: Export vidéo Motion-JPEG AVI (`--video`) (v1.21.16-alpha) - DONE

- [x] **Module recorder AVI MJPG autonome** (`src/video/avi_recorder.c` +
      `include/video/avi_recorder.h`) : écrit un fichier AVI complet
      (RIFF/`hdrl`/`avih`/`strl`/`strh`/`strf` BITMAPINFOHEADER/`movi`/chunks
      `00dc`/index `idx1`) en **Motion-JPEG**, **sans dépendance externe** —
      parité avec l'export AVI d'Oricutron. Tailles RIFF/movi + `dwTotalFrames`/
      `dwLength` + suggested-buffer back-patchées à `avi_recorder_close()`.
- [x] **TU unique `src/video/stb_image_write_impl.c`** hébergeant
      l'implémentation `stb_image_write` (partagée avec le cast server MJPEG) →
      plus de symbole dupliqué ; `cast_server.c` n'inclut que les déclarations.
- [x] **Options CLI** `--video FILE` / `--video-fps N` (déf. 50) /
      `--video-quality N` (déf. 85), branchées dans la boucle de rendu de
      `main.c` (1 trame/frame, finalisation à la sortie).
- [x] Validé bout-en-bout : AVI relu par `ffprobe` (mjpeg, 240×224, 50 fps,
      201 trames) et trame décodée par ffmpeg (écran de boot Atmos correct) ;
      builds headless + `CAST=1` linkent OK.
- [x] Suite `make test-avi` (`tests/unit/test_avi.c`, **7 tests**). 672 tests PASS (+7).
- [ ] Pistes futures : export GIF animé ; multiplexage audio (AVI PCM stream) ;
      export AVI depuis le menu GUI/cast.

### Sprint 60p: picowifi — persistance du CA et de tlsVerify (firmware v0.2.0) (v1.21.15-alpha) - DONE

- [x] **CA persistant** (`src/io/serial_picowifi.c`, helpers `pw_ca_path/persist/load`)
      comme le `ca.pem` LittleFS du firmware (fichier indépendant) : écrit dans un
      **sidecar `<nvram>.ca`** dès `AT$CA=` (pas conditionné par `AT&W`), supprimé
      par `AT$CA-`, **rechargé au boot** ; survit aux reboots **et** au factory
      reset `AT&F`.
- [x] **`tls_verify` persisté** dans le NVRAM (clé `tls_verify`, écrite par `AT&W`,
      comme `SETTINGS_T.tlsVerify`) et restauré au boot.
- [x] Test `test_tls_ca_persists_across_reboot` (upload + `CV1` + `AT&W` → reboot →
      CA et `CV?` rétablis ; `AT$CA-` + reboot → `CA: 0 bytes`). `make test-picowifi`
      71 → **72**. 665 tests PASS (+1).

### Sprint 60k: Module PIA 6821 (MC6821) autonome + rebranchement DTL (v1.21.10-alpha) - DONE

- [x] **Module PIA 6821 complet et réutilisable** (`src/io/pia6821.c` + `.h`) :
      2 ports A/B (DDR/OR/CR), CA1/CB1 IRQ1 (enable/front/flag/clear-on-read),
      CA2/CB2 entrée (IRQ2) et sortie (set-reset/pulse/handshake), résolution
      `OR & DDR`, lecture mixte entrée/sortie, callbacks port/CA2/CB2/IRQ.
- [x] **DTL 2000 rebranché** : 6 champs PIA ad-hoc → `pia6821_t pia` ; offsets
      $03F8-$03FB routés vers `pia6821_read/write` ; décodage ligne/mode inchangé.
- [x] Aucune régression (15 tests DTL + e2e + boot OTRM identiques).
- [x] Suite `make test-pia6821` (14 tests). 644 tests PASS (+14).
- [x] Suite logique : factoriser aussi l'ACIA 6850 hors de `dtl2000.c` — fait
      (émulation dans `src/io/acia6850.c`) ; **v1.36.3** : dernières constantes de
      statut `DTL_ACIA_SR_*` converties en alias des `ACIA6850_SR_*` partagés
      (source unique de vérité), plus aucune duplication.

### Sprint 60j: Timing baud réel sur horloge externe — reco #6 (v1.21.9-alpha) - DONE

- [x] **`--serial-baud N`** : quand un programme sélectionne l'horloge externe de
      l'ACIA 6551 (index baud 0), remplace l'« instant transfer » (1 cycle/octet)
      par une cadence réelle de N bauds — pour tester les logiciels sensibles au
      débit. Champ `acia6551_t.ext_clock_baud` + `acia_set_ext_clock_baud()`.
- [x] Warning « instant transfer » ne spamme plus (garde `ext_clock_warned`).
- [x] Test unitaire `test_ext_clock_baud`. Débits internes 50-19200 déjà cycle-exacts.
- [x] 630 tests PASS (+1). **Diagnostic comms : 5/5 recommandations livrées.**

### Sprint 60i: Dépréciation du backend `digitelec:` — reco #3 (v1.21.8-alpha) - DONE

- [x] **`--serial digitelec:` DÉPRÉCIÉ** au profit de `--dtl2000` (carte fidèle).
      Le comportemental modélise le DTL 2000 comme un modem externe sur l'ACIA 6551
      ($031C), pas le vrai matériel (carte PIA 6821 + ACIA 6850 $03F8, validée OTRM).
- [x] Avertissement de dépréciation au boot + commande de migration
      (`--serial digitelec:H:P` → `--dtl2000 tcp:H:P`), backend gardé fonctionnel un cycle.
- [x] `--help`, README (matrice : digitelec ⚠️ deprecated + note de migration) à jour.
- [x] 2 assertions e2e (warning émis + backend toujours fonctionnel). 629 tests PASS (+2).

### Sprint 60h: Fix SDL écran noir + doc matrice puce × transport — reco #5 (v1.21.7-alpha) - DONE

- [x] **Fix SDL fenêtre noire** : `renderer_init()` forçait `SDL_RENDERER_ACCELERATED`
      (créé OK mais cadre tout noir sur certaines configs GPU : llvmpipe/X logiciel,
      NVIDIA/Xwayland). Nouveau flag `--render-software` (+ champ `render_software`,
      signature `renderer_init(scale, prefer_software)`), respect de
      `SDL_RENDER_DRIVER=software`, fallback auto en logiciel si l'accéléré échoue.
      Validé empiriquement (OTRM visible sur la carte DTL).
- [x] **README — section « Serial communication: chips × transports »** : tableau
      des puces (`--serial` ACIA 6551 $031C/$0380, `--dtl2000` PIA 6821 + ACIA 6850
      $03F8, `--loci` MIA) + matrice de disponibilité des transports par puce.
- [x] Distinction **transparent** (loopback/tcp/pty/com/file, dispo partout) vs
      **à protocole** (modem/digitelec/picowifi, réservés à `--serial`), + portée
      réelle des options `--serial-*` (ACIA 6551 only) + exemple `file:`.
- [x] Référence CLI README corrigée (com:/file:, --serial-buffer/--serial-irq-on-rdrf).
- [x] Doc uniquement, 627 tests inchangés.

### Sprint 60g: Backend série `file:` replay/capture — reco #4 (v1.21.6-alpha) - DONE

- [x] **Backend `file:IN[:OUT]`** (`SERIAL_BACKEND_FILE`, `serial_backend_file_create()`) :
      transport transparent déterministe — RX rejoué depuis `IN`, TX capturé dans
      `OUT`. Formes : `file:in` (replay), `file:in:out` (les deux), `file::out`
      (capture). Lookahead 1 octet (poll exact), flush eager.
- [x] Câblé dans `serial_transport_create()` → disponible `--serial` ET `--dtl2000`.
- [x] `examples/dtl2000-echo.bas` (echo RX→TX) + test e2e `make test-serial-file`
      (capture « ABCDEFGHIJ », round-trip `in==out` « ORIC! », plomberie `--serial`).
- [x] Tests de protocole reproductibles sans réseau/pair ; 627 tests PASS (+3).

### Sprint 60f: Unification des transports série DTL 2000 — reco #1 (v1.21.5-alpha) - DONE

- [x] **Helper partagé `serial_transport_create()`** (+ `parse_host_port()`) :
      source unique de vérité pour les transports *transparents* (tuyaux d'octets
      bruts : loopback, tcp:H:P, pty, com:), utilisé par `--serial` et `--dtl2000`.
- [x] **`--dtl2000` accepte `com:`** (vrai port série) en plus de loopback/tcp/pty.
- [x] **Transparent vs protocole** : `modem` (Hayes AT), `digitelec:`/`picowifi`
      injectent leur couche commande/UART → réservés à `--serial`. Le DTL 2000 est
      composé par son PIA 6821 (bit ligne, pas d'AT) et transporte du V23 brut →
      `--dtl2000 modem` rejeté (une couche Hayes derrière serait infidèle).
- [x] `--help` + message d'erreur DTL mis à jour ; 3 assertions e2e (pty accepté,
      modem rejeté, inconnu rejeté).
- [x] Refactor sans régression sur `--serial` (`--serial modem` OK) ; 624 tests PASS (+3).

### Sprint 60e: Trace série TX/RX du Digitelec DTL 2000 + validation OTRM (v1.21.4-alpha) - DONE

- [x] **Trace TX/RX ACIA 6850** : `--serial-trace` câblé sur la carte `--dtl2000`
      (champ `dtl2000_t.trace` + `dtl2000_set_trace()`), chaque octet émis/reçu
      journalisé (dir, hex, ASCII, statut, compteurs, note sent/recv/dropped).
- [x] **Test e2e `make test-dtl2000-txrx`** (`tests/integration/test_dtl2000_txrx.sh`) :
      boote `examples/dtl2000-test.bas` en loopback, vérifie 10 octets `A`..`J`
      renvoyés identiques en RX, 0 dropped (6 assertions, skip gracieux).
- [x] **OTRM 2.00** (`otrm-mc6850@3fc.tap` d'iss/raxiss, trouvé via fil forum
      defence-force p33463) booté sur l'émulation DTL : auto-exec $C7, statut
      « ACIA 6850 @ 0x3fc » → validation par vrai logiciel d'époque (manquante
      depuis le sprint 51). POKEs d'init 1200/1200 d'iss confirment `DDRA=$F4`.
- [x] 621 tests PASS (+6).

### Sprint 60d: Terminal NNTP/Usenet (CRLF) Eternal-September (v1.21.3-alpha) - DONE

- [x] **`tools/modem/modem_nntp.bas`** (+ tap) : terminal CRLF (NNTP exige CR+LF,
      vs CR seul de telehack) pour lire ET poster des news Usenet.
- [x] **Doc README** : connexion `ATDT-news.eternal-september.org:119`
      (transparent), AUTHINFO/GROUP/HEAD/BODY/ARTICLE/POST, compte gratuit.
- [x] Connexion validée empiriquement (greeting INN 2.8.0 + HELP via le vrai
      PicoWiFiModemUSB). Doc/tools uniquement, 615 tests inchangés.

### Sprint 60c: Doc test BBS telehack.com:23 + taps prébuilts (v1.21.2-alpha) - DONE

- [x] **`tools/modem/README.md`** : section « TELEHACK » — connexion BBS telnet
      réelle validée end-to-end (`ATDT telehack.com:23` → `CONNECT 9600` →
      bannière TELEHACK 1206 octets sur l'écran du Oric, via le vrai modem +
      WiFi réel) ; liste de BBS joignables + note diagnostic NO CARRIER=DNS.
- [x] **Taps prébuilts committés** (`modem_term.tap`, `modem_probe.tap`) pour
      lancement direct sans étape `bas2tap` (corrige un blocage utilisateur).
- [x] Doc/tools uniquement, 615 tests inchangés.

### Sprint 60b: Terminal WiFi LOCI — PicoWiFiModemUSB réel (v1.21.1-alpha) - DONE

Utilisation d'un VRAI PicoWiFiModemUSB (branché en USB sur l'hôte) depuis le
Oric émulé, en config LOCI authentique.

- [x] **Outils `tools/modem/`** : `modem_term.bas` (terminal full-duplex
      clavier↔modem), `modem_probe.bas` (sonde ATI), `README.md`.
- [x] **Chaîne** : Oric BASIC → ACIA 6551 `$0380` (`--acia-addr 0380`, `--loci`)
      → backend `--serial com:9600,8,N,1,/dev/ttyACM0` → vrai Pico → WiFi réel.
- [x] **Validé end-to-end** (`--serial-trace`) : `TX ATI\r` → `RX "Pico WiFi
      modem ... WiFi status: CONNECTED ... OK"` du vrai modem affiché à l'écran
      du Oric (RSSI live). Outils/doc uniquement, 615 tests inchangés.

### Sprint 60: Injection SHIFT G/D + validation clavier ORIC-1/Atmos (v1.21.0-alpha) - DONE

Complète l'injection des modificateurs (CTRL/FUNCT) avec les deux SHIFT, et
livre un harnais de validation clavier couvrant les deux modèles.

- [x] **Escapes `\Lx` (LEFT shift+x) et `\Rx` (RIGHT shift+x)** ; helpers
      `oric_keyboard_press_lshift` (col 4/row 4) + `press_rshift` (col 7/row 4)
      ; chemin LOCI HID = bits USB 0x02 (LSHIFT) / 0x20 (RSHIFT). +3 tests.
- [x] **Sentinelle de combo unifiée (0x90)** : sépare tout combo consécutif.
- [x] **Outils `tools/keytest/`** : `keydetect.bas` (interactif), `keytest.bas`
      (capture buffer #2000), `validate_keys.sh` (harnais), `README.md`.
- [x] **Validation 86 touches/combos × 2 ROMs = 172/172 OK** (ORIC-1 BASIC 1.0
      + Atmos BASIC 1.1). Comportements ROM documentés : CTRL+C=BREAK, FUNCT
      ignoré par le ROM (→ touche de base), SHIFT L/R distinct matrice mais
      équivalent ROM, shift+2='@', flèches 0B/0A/08/09.
- [x] Suite complète 615 tests PASS (+3).

### Sprint 59b: Fix position FUNCT (col 5) + validation empirique LFMV2 (v1.20.1-alpha) - DONE

Test du Sprint 59 sur le LOCI File Manager réel (locifilemanager-v2), qui a
révélé une mauvaise position matricielle pour FUNCT.

- [x] **Bug** : `oric_keyboard_press_funct()` pressait col 3/row 4 (non connectée)
      au lieu de **col 5/row 4** → `\Fx` ne déclenchait jamais `MOD_FUNCT`.
- [x] **Source de vérité** : scanner clavier ROM LOCI (`VKEY_FUNCT = 5*8+4`,
      LFMV2 `include/keyboard.c/.h`) + `qwerty_tab` Phosphoric (col5/row4=LALT).
- [x] Table SDL `special_keys` corrigée aussi (TAB (3,4)→(5,4)).
- [x] **Validation empirique** : LFMV2 booté via `--loci`, `--type-keys` + dump
      RAM symboles. `\Cc`→keyb_char($84E7)=0x03 (KEY_CTRL_C) ; `\F1`→0xB1 (KEY_F1,
      échouait à 0x31 avant fix). Tests unitaires mis à jour. 612 tests PASS.

### Sprint 59: Injection des modificateurs CTRL et FUNCT (--type-keys) (v1.20.0-alpha) - DONE

Extension de l'auto-frappe `--type-keys` aux deux modificateurs Oric (CTRL,
FUNCT) qui n'étaient câblés que dans le mapping SDL temps réel.

- [x] **Escapes `\Cx` (CTRL+x) et `\Fx` (FUNCT+x)** : le modificateur reste
      pressé pendant l'appui de la touche compagne `x` (3 caractères consommés).
- [x] **Chemin clavier standard** (`src/io/keyboard.c`) : `oric_keyboard_press_ctrl()`
      (LCTRL col 2/row 4) + `oric_keyboard_press_funct()` (FUNCT col 5/row 4,
      cf. fix 59b), abaissent le seul bit du modificateur ; sentinelle par
      modificateur forçant une trame de relâche entre deux combos consécutifs.
- [x] **Chemin LOCI HID** (`src/main.c`) : CTRL = bit modificateur USB HID 0x01 ;
      FUNCT = accord `Tab` (0x2B) faute d'équivalent HID standard (cohérent SDL).
- [x] Aide `--help` enrichie (liste complète des escapes).
- [x] 5 tests `tests/unit/test_keyboard.c` (positions, combos maintenus, relâche).
- [x] Suite complète 612 tests PASS (+5) ; build `SDL2=1` validé.

### Sprint 58b: Garde-fou de non-régression sur l'invariant tap_cap (v1.19.6-alpha) - DONE

Durcissement de suivi du fix Sprint 58 : empêcher toute ré-introduction future
du débordement de tas +8/+9.

- [x] **Garde runtime toujours actif** (`src/main.c`) : après la boucle
      `tap[t++]`, vérification `t == tap_cap`. Divergence → `log_error` + refus
      d'émettre le TAP (free + invalidation `csave_snap_valid`).
- [x] **Choix non purgeable** : garde runtime préféré à `assert()` car la release
      est compilée `-DNDEBUG` (qui supprimerait l'assert) ; sous AddressSanitizer
      le débordement est de toute façon capturé à l'écriture fautive.
- [x] Aucun effet sur le chemin nominal (`t == tap_cap` toujours vrai post-fix).
- [x] Suite complète 607 tests PASS (inchangé) ; build `SDL2=1` validé.

Note : v1.19.5-alpha (commit `6a80e33`, outil `tap2sedoric`) s'est intercalée et
n'avait mis à jour que le CHANGELOG ; suivi (EMU_VERSION/VERSION_TRACKING/
CIRRUS_OS/ROADMAP) rétabli à 1.19.6 ici.

### Sprint 58: Heap overflow d'1 octet à chaque CSAVE (v1.19.4-alpha) - DONE

Correction d'un débordement de tas différé signalé par CR upstream
(`malloc(): invalid next size` après plusieurs CSAVE, typiquement via terminal/BBS).

- [x] **Diagnostic** : dans la construction du TAP canonique (`src/main.c`),
      `tap_cap` réservait `8` octets d'en-tête alors que la boucle `tap[t++]` en
      écrit **9** (2 padding + type + autorun + 2 end + 2 start + 1 reserved).
      Chaque CSAVE débordait `malloc(tap_cap)` d'un octet → corruption cumulative
      des métadonnées de l'allocateur, crash différé. Confirmé octet par octet.
- [x] **Correctif** (`src/main.c`) : `+ 8 /*header*/` → `+ 9` ; l'allocation
      couvre désormais exactement les octets écrits (`t == tap_cap`).
- [x] **Validation** : détectable de façon déterministe dès le 1er CSAVE sous
      AddressSanitizer ; correctif d'1 ligne sans changement de comportement.
- [x] CR de référence (upstream) : `Phosphoric-upstream/docs/CR/2026-06-15_Bug_CSAVE_Heap_Overflow.md`.
- [x] Suite complète 607 tests PASS (inchangé).

### Sprint 57: Garde-fou ROM système absent (faux bug Microdisc Nova2026) (v1.19.3-alpha) - DONE

Investigation d'un « bug overlay Microdisc » rapporté (démo Cybernova/Nova2026,
boucle BRK `$0000` ~cycle 349k). Reproduit puis **réfuté** : ce n'est pas un bug
d'émulation mais un `-r` manquant dans la commande.

- [x] **Diagnostic** : `--disk-rom` sans `-r` → zone ROM BASIC `$C000-$FFFF` à zéro.
      La démo fait `$0314=$06` (romdis off → ROM BASIC mappé) puis `JMP $F8B8` ;
      sans ROM chargé, `$F8B8` lit `$00` = BRK → boucle `$0000`. Avec
      `-r roms/basic11b.rom` : la démo tourne et **s'affiche** (18→34 % de l'écran
      à 8–20 M cycles). Le mapping Microdisc est donc correct.
- [x] **Réfutation du diagnostic externe** : le rapport `BUG_phosphoric_rmw_mem_M16`
      cité visait un **65C816** (`cpu65c816_opcodes.c`, scheduler `--wm-taskmode`)
      d'un autre projet — Phosphoric est un **6502**. Sans rapport.
- [x] **Garde-fou** (`src/main.c`) : `--disk-rom` sans `-r` = erreur fatale
      (config impossible : ROM BASIC soudé, EPROM Microdisc additionnelle) ;
      aucun ROM = avertissement non fatal. `--load-state` reste en avertissement.
- [x] **Test** : `tests/integration/test_rom_guard.sh` (3 cas : erreur+exit 1,
      avertissement, silence avec `-r`) + cible `test-rom-guard` dans `make tests`.
- [x] Suite complète 607 tests PASS (+3)

### Sprint 38: Trace FDC + fix dump RAM 64 Ko (v1.16.84-alpha) - DONE
- [x] FDC_TRACE=1 : trace stderr écritures registres FDC (PC 6502 +
      cycle), SEEK (état interne) et READ (piste/secteur) — a permis
      de rétracter le faux bug WD1793 « track+1 » du projet SCUMM-Oric
- [x] --dump-ram-at : retour aux 64 Ko documentés ($C000-$FFFF = vue
      CPU bankée, sans effet de bord), régression 48 Ko corrigée
- [x] Suite de tests complète 0 échec (profil sans SDL2)
- [x] VERSION_TRACKING : 1.16.84-alpha tracé
- [x] Élucider test_renderer_init_headless en profil SDL2=1 (pré-existant) —
      **corrigé v1.36.2** : prototype obsolète à 1 arg (vs `int,bool`) + exigence
      du comportement stub en vrai SDL2. Test conditionné `#ifdef HAS_SDL2` ;
      vérifié 10/10 dans les deux profils.

### Sprint 45: PicoWiFiModemUSB — modem WiFi LOCI (v1.17.0-alpha) - DONE

Émulation du PicoWiFiModemUSB de sodiumlb (l'auteur de LOCI) : une
Pico W pont USB CDC ↔ WiFi que le firmware LOCI expose à l'Oric comme
un ACIA série à $0380. Branche feature/picowifi (depuis main/sprint 38).

- [x] Backend dédié src/io/serial_picowifi.c (SERIAL_BACKEND_PICOWIFI),
      état opaque heap-alloc dans l'union serial_backend_t
- [x] CLI --serial picowifi[:SSID[:PASS]] ; combiné à --loci → ACIA $0380
      automatique = montage réel Pico W + LOCI reproduit
- [x] Jeu de commandes AT v0.1.0 complet et fidèle au firmware réel :
      WiFi (AT$SSID/$PASS/$MDNS, ATCn), dial (ATDT/ATDS/ATD TCP),
      telnet (ATNET, AT$TTY/$TTS/$TTL), série (AT$SB/$SU), registres S
      (S0/S2), codes résultat (ATE/Q/V/X), flux (AT&K/&D), numéros
      rapides (AT&Z), serveur (AT$SP/&R), profil (AT&V/&W/&F), infos
      (ATI), online (ATO), échappement +++ gardé, répétition A/
- [x] WiFi associé simulé ; connexions de données = vrais sockets TCP
      (réutilise l'infra getaddrinfo/connect non-bloquant du backend modem)
- [x] 28 tests unitaires make test-picowifi (config, codes résultat,
      registres, numéros rapides, telnet, reset usine, erreurs), 551 total
- [x] CHANGELOG / VERSION_TRACKING / CIRRUS_OS / README / CLAUDE.md à jour
- [x] E2E BASIC : PRINT vers ACIA $0380 + lecture (round-trip) — livré
      Sprint 74 (v1.34.0), `test-loci-acia-e2e` (loopback déterministe ;
      picowifi headless non déterministe, à traiter)
- [ ] Numéros rapides AT&Z persistés (AT&W actuellement no-op)

---

## Backlog PicoWiFiModemUSB — fidélité au firmware réel

Audit du code source réel (clone `sodiumlb/PicoWiFiModemUSB`, fichiers
`at_basic.h`/`at_extended.h`/`at_proprietary.h`/`support.h`/`tcp_support.h`/
`wifi_modem.cpp`). Le sprint 45 a livré la **surface AT** ; ce backlog vise
la **fidélité comportementale**. Périmètre = reproduire le firmware réel ;
toute extension au-delà (ex. proxy TLS) est explicitement hors-périmètre
(voir Future Enhancements).

### Sprint 46: PicoWiFi — défauts + codes résultat + parsing composé (v1.17.1-alpha) - DONE

Corrige les divergences vs le code source (l'impl sprint 45 suivait le
README, partiellement obsolète/hérité de l'ancêtre ESP8266).

- [x] **Défauts d'usine corrects** (`factoryDefaults`) : mDNS `picomodem`
      (PAS `espmodem`), baud `9600` (PAS 1200), location `Computer Room`,
      terminal `ansi`, NET1, X1, V1, S2=43, 8N1
- [x] **3 numéros rapides préchargés** : slot 0 `particles`, slot 1
      `altair`, slot 2 `heatwave` (préfixe telnet `+` reporté au sprint 47)
- [x] **Enum codes résultat fidèle** : R_OK=0, R_CONNECT=1, R_RING=2,
      R_NO_CARRIER=3, R_ERROR=4, R_NO_ANSWER=5, R_RING_IP=6 (retiré
      NO_DIALTONE/BUSY inventés)
- [x] **Parsing de commandes composées** (`ATS0=1 X1 Q0`) : boucle de
      tokens, seul le DERNIER émet OK/valeur, token inconnu → ERROR
- [x] **`ATC?` numérique** (`0`/`1`) au lieu de texte
- [x] **`ATX` effectif** : `CONNECT <baud>` et `NO CARRIER (HH:MM:SS)` en
      X1, formes simples en X0
- [x] **`AT$PASS?`** affiche la vraie valeur (fidèle au firmware)
- [x] **`ATZ`** = no-op `OK` (le firmware ne reset pas : casserait l'USB)
- [x] Tests : 5 nouveaux (défauts, numéros rapides, ligne composée, token
      inconnu), 33 picowifi / 556 total

### Sprint 47: PicoWiFi — protocole Telnet (cœur de la fidélité) (v1.17.2-alpha) - DONE

Sans ça, une vraie session BBS telnet recevait un flux d'octets erroné.

- [x] **Modes NET0/NET1/NET2** appliqués au flux bidirectionnel
      (`session_telnet`, distinct du défaut `ATNET`)
- [x] **Sortant** : doublage `IAC` (REAL+FAKE), `CR`→`CR+NUL` (REAL)
- [x] **Entrant** : machine à états IAC, filtrage `CR+NUL`→`CR` (REAL),
      dé-doublage `IAC IAC`→`IAC`, contrôle telnet retiré du flux Oric
- [x] **Négociation IAC** : DO → WILL pour BINARY/ECHO/SUP_GA/TTYPE/TSPEED,
      WONT sinon ; WILL → DONT pour LINEMODE/NAWS/LFLOW/NEW_ENVIRON/XDISPLOC,
      DO sinon
- [x] **Sous-négociation SB** : TTYPE→`terminal`, TSPEED→`baud,baud`,
      NAWS→`width/height`, LOC→`location`
- [x] **Préfixes `ATDT`** `-`/`=`/`+` → NO/REAL/FAKE telnet (session),
      réinitialisé au défaut sur NO CARRIER
- [x] **`AT$AYT`** (émet `IAC AYT`) + réponse `[Yes]` à un AYT entrant
- [x] Tests : 10 tests d'intégration sur sockets localhost réels (CR/NUL/
      IAC sortant REAL/FAKE/NET0, négociation DO TTYPE / WILL NAWS,
      filtrage entrant, IAC IAC = donnée, AYT, cas Minitel NET0), 566 total
- [~] **`+++` guard-time** : reste approximé (compteur de polls) — un timing
      cycle-exact (1 s) demanderait une horloge dans la vtable série ;
      reporté, sans impact fonctionnel sur la détection d'échappement

### Sprint 48: PicoWiFi — commandes réseau réelles (v1.17.3-alpha) - DONE

- [x] **`ATGET`** : GET HTTP/1.1 (`Host:` + `Connection: close`), mode
      données NET0, flux brut (headers inclus) vers la série, HTTP seulement
- [x] **`ATRD`/`ATRT`** : NIST DAYTIME `time.nist.gov:13` (RFC 867), sortie
      `YY-MM-DD HH:MM:SS`, ERROR si déjà en appel ou échec réseau
- [x] **Dial par alias** (`ATDTmybbs`) + **numéro 7 chiffres identiques**
      (`ATDT7777777` → slot 7) quand aucun port explicite
- [x] **`AT$BM`** message occupé (get/set, défaut firmware)
- [x] Tests : 9 nouveaux dont ATGET end-to-end sur socket localhost (requête
      émise + flux réponse), parse DAYTIME (+ rejet ligne invalide), dial
      alias/7-chiffres, escape +++/ATO, ATRD refusé en appel ; 52 picowifi,
      575 total. Connexion réelle time.nist.gov / HTTP distant non testée
      auto (réseau externe), comme ATDT — parsing et erreurs couverts.

### Sprint 49: PicoWiFi — persistance NVRAM + boot (v1.17.4-alpha) - DONE

Dernier sprint du chantier PicoWiFi — **fidélité complète** avec le firmware.

- [x] **Config persistée** : fichier hôte = flash littlefs (chemin via
      PHOSPHORIC_PICOWIFI_NVRAM sinon $HOME/.phosphoric_picowifi.cfg),
      format clé=valeur, magie `PWNV1` (absente/invalide → défauts d'usine)
- [x] **`AT&W`** sauvegarde, **`AT&V0`** courant vs **`AT&V1`** stocké,
      **`AT&F`** reset + persiste les défauts
- [x] **Boot** (open) : défauts → superposition NVRAM → identifiants CLI
      prioritaires ; WiFi auto-associate simulé si SSID ; exécution `AT$AE`
- [x] **`AT&D`/`AT&K`/`AT$W`** stockés ; effet matériel inerte documenté
      (pas de vraies broches DTR/RTS/CTS ni d'attente sur la variante USB-CDC)
- [x] Tests : 7 hermétiques (NVRAM temp via env var) — round-trip, non-
      persistance sans &W, &V0≠&V1, priorité CLI, AT$AE au boot, &F persisté,
      auto-associate ; 59 picowifi, 582 total

**Bilan PicoWiFi (sprints 45-49)** : backend fidèle au firmware réel
sodiumlb/PicoWiFiModemUSB v0.1.0 — surface AT complète, défauts/codes
exacts, commandes composées, protocole Telnet (IAC/CR-NUL/négociation),
réseau réel (ATGET/ATRD), persistance NVRAM. Reste optionnel : garde-temps
+++ cycle-exact (vtable série), per-slot telnet prefix des numéros rapides.

### Sprint 50: PicoWiFi — pont réel vers le réseau de l'hôte (v1.18.0-alpha) - DONE

Reflète l'état réseau réel de l'hôte (lecture seule), au-delà de la
simulation. Le chemin de données était déjà réel (TCP via l'interface
active) depuis le sprint 45 ; ce sprint rend aussi le *reporting* réel.

- [x] Option `PHOSPHORIC_PICOWIFI_REALNET=1` (opt-in, off par défaut)
- [x] `ATI` : IP locale réelle (`getifaddrs`) + état de connexion réel
- [x] `ATC?`/`ATC1` : connectivité internet réelle (test de route UDP
      8.8.8.8:53, aucun paquet émis ; échec si hors-ligne)
- [x] Adoption du SSID WiFi réel de l'hôte au boot (best-effort `iwgetid`)
      si aucun SSID configuré
- [x] **Lecture seule par conception** : jamais de contrôle/scan/association
      de la carte (OS-spécifique, privilégié, perturberait la vraie connexion)
- [x] 3 nouveaux tests (62 picowifi, 585 total) ; mode simulé inchangé par
      défaut. Démo : ATI → `IP: 192.168.1.19 (host)` `WiFi: (host) UP`
- [ ] Hors périmètre : piloter la carte WiFi (associer/scanner) — voir
      Future Enhancements, déconseillé

### Sprint 89: Windows 11 — Pages + port natif .exe (v1.49.0-alpha) - DONE

- [x] WASM publié : https://benedictemarty.github.io/Phosphoric/
- [x] oscompat.h + serial_backend_win.c (loopback/file) + gdbstub_win.c
      + async-pause neutralisé ; Makefile WIN=1 (MinGW -posix + SDL2)
- [x] CI windows-build : artifact oric1-emu.exe (PE32+) + SDL2.dll +
      libwinpthread + roms + LISEZMOI ; verte au 2ᵉ run
- [ ] v2 (backlog) : Winsock pour tcp/modem/picowifi/gdb, async-pause
      via thread, test smoke wine en CI, release automatique sur tag

### Sprint 88b: README resynchronisé (v1.48.3-alpha) - DONE

- [x] Bannière 1.22.0/793 → 1.48.3/868 ; savestate 15 sections ;
      IPC 30 commandes ; Testing 868 (cpu 92, loci 163) ; footer à jour

### Sprint 88: Documentation — README + docs/loci.md (v1.48.2-alpha) - DONE

- [x] README : VIA CA2/CB2, FDC réel + bad sectors, LOCI complet,
      options --fdc-timing/--bad-sector/--loci-usb, F6/F8
- [x] docs/loci.md : doc de référence (mémoire, ABI firmware, stockage,
      bouton Action, timing MIA, divergences hors périmètre)

### Sprint 87b: LOCI — copie du menu vérifiée e2e (v1.48.1-alpha) - DONE

- [x] Audit copie/suppression : copie = client-side (menu, touche 'i'),
      suppression = API UNLINK/RENAME uniquement (pas dans le menu v0.3.0)
- [x] +1 test (868) : file_copy rejoué — 12 000 octets clé → interne,
      2 tours 8 Ko via xram, byte-identique

### Sprint 87: LOCI — vraies clés USB du host (v1.48.0-alpha) - DONE

- [x] resolve_path : volumes « 1: »-« 4: » → racine host enregistrée ;
      « 0: »/chemins nus = flash root ; garde anti-échappement conservée
- [x] loci_add_usb_storage() (device list + racine volume)
- [x] --loci-usb DIR (répétable, 4 max) + auto-détection /media/$USER,
      /run/media/$USER au démarrage ; --loci-usb none
- [x] +1 test (867) ; e2e « USB storage 2: (fakeusb) »

### Sprint 86: LOCI — device list + diag ROM (v1.47.0-alpha) - DONE

Le sélecteur du menu doit lister les périphériques (« 0: Internal
storage », clé USB, picowifi) ; l'appui long doit booter la diag ROM.

- [x] opendir(vide) = itérateur périphériques fd 0 (dir.c FD_OFFS_DEV) ;
      readdir : internal storage + lignes usb_set_status + nom vide
- [x] loci_add_usb_device() ; SDIMG (« MSC x.x MB ») et picowifi
      (« CDC modem mounted ») déclarés par main.c
- [x] Diag ROM Mike Brown v1.08k (roms/loci/test108k.rom + 60 Hz) ;
      appui long F8 >= 2 s (EXT_BTN_LONGPRESS_MS) ou loci-button long
- [x] +1 test (866) ; e2e : « Oric Diag ROM V1.08k / RAM Test Passed »

### Sprint 85b: LOCI — garde de réentrance du bouton Action (v1.46.1-alpha) - DONE

- [x] F8 dans le menu : ignoré (plus de re-snapshot du menu dans
      loci_resume.ost qui cassait le resume) ; drapeau loci_menu_active
      posé au warm boot, levé par tout swap ROM $C000

### Sprint 85: LOCI — bouton Action → menu, ROM patchée, ADJ_SCAN vivant (v1.46.0-alpha) - DONE

Le F8 n'avait aucun effet visible ; le menu affichait FW 242.241.240
(placeholders non patchés) et aucun timing.

- [x] F8 = EXT_CAPTURE_IRQ → EXT_BOOT_LOCI : snapshot session au press,
      boot ROM menu au relâchement (LOCIROM/locirom flash root, repli
      <dossier -r>/loci/locirom)
- [x] Resume : MIA_BOOT + LOCI_BOOT_RESUME → re-swap ROM + snapshot
- [x] loci_patch_rom_info : VERSIONS $FFF7-9 → FW 0.3.1 ; TIMINGS
      $FFEF-F3 → tmap/tior/tiow/tiod/tadr (gardes placeholders)
- [x] ADJ_SCAN vivant : loci_adj_tick écrit 0x80|tior dans l'octet ROM
      $FFF0 pollé par la page tune (100 ms lead-in + 1 pas/5 ms)
- [x] MIA_BOOT : repli basic11b/basic10/microdis vers le dossier de -r
- [x] Commande IPC loci-button ; +1 test (865) ; e2e « FW 0.3.1 » vérifié

### Sprint 84: LOCI — MAP_TUNE_*/ADJ_SCAN conformes (v1.45.0-alpha) - DONE

Suite de l'audit fidélité LOCI : sémantique exacte des réglages de
timing MIA (map.c/cfg.c/adj.c du firmware).

- [x] MAP_TUNE_* renvoie la valeur courante dans AX (avant : 0)
- [x] A > 31 = requête pure, valeur intacte (avant : masqué et écrasé)
- [x] tiod stocké non masqué (& 0x07 appliqué seulement au PIO réel)
- [x] ADJ_SCAN : xram[$FFF0] = tior configuré, bit 7 clair (ADJ_CLEANUP)
- [x] +1 test, 1 réécrit (864) ; suite complète verte

### Sprint 83: LOCI — errno FatFS + dir fd 64+ (ABI firmware) (v1.44.0-alpha) - DONE

Volet « LOCI » de l'initiative fidélité v2 : dernières divergences vs
sodiumlb/loci-firmware, vérifiées sur la source re-clonée.

- [x] Erreurs filesystem = API_EFATFS (32+FRESULT) sur host-FS et SDIMG
      (rôle carte SD/FAT) ; codes 1-18 réservés aux erreurs API
- [x] LOCI_DIR_OFFSET 32 → 64 (dir.c FD_OFFS_FAT — plage FAT)
- [x] Note d'audit invalidée : fd FICHIERS firmware = 3-18 (STD_FIL_OFFS=3),
      déjà conformes ; 0/32/64 ne concernent que les RÉPERTOIRES
- [x] +1 test ABI (863) + 6 assertions alignées ; suite complète verte

### Sprint 82: VIA 6522 — modes CA2/CB2 complets (v1.43.0-alpha) - DONE

Volet « ORIC physique » de l'initiative fidélité v2 : dernier écart connu
du 6522 (le S70 avait livré SR/T2-pulse/pins, pas les modes PCR complets).

- [x] Handshake output (100) : CA2 sur accès ORA, CB2 sur écriture ORB
      SEULEMENT (write handshake) ; restauré par le front actif CA1/CB1
- [x] Pulse output (101) : un cycle φ2 exact, restauré par via_update()
- [x] Modes entrée (000-011) : via_set_ca1(), via_set_ca2_input(),
      via_set_cb2_input() avec détection de fronts (PCR b0/b2/b6)
- [x] Latching IRA/IRB (ACR bits 0-1) au front actif CA1/CB1
- [x] Clear IFR « independent » (001/011) vérifié correct + couvert
- [x] +6 tests test_io (862) ; e2e boot + PRINT 6*7 via clavier OK

### Sprint 81: FDC — timings mécaniques réels du WD1793 (v1.42.0-alpha) - DONE

Volet « lecteur de disquette » de l'initiative fidélité matérielle v2 :
la mécanique d'un vrai lecteur 3" remplace les délais courts hérités.

- [x] `timing_mode` FAST/REAL + `rot_pos` (300 tr/min, 200 000 cycles/tour)
- [x] Type I : 6/12/20/30 ms par piste (r1r0), +30 ms si V (verify)
- [x] Type II/III : premier DRQ à la latence rotationnelle réelle (+30 ms
      si E) ; multi-secteur au rythme de la rotation ; Write Track à l'index
- [x] RNF après 5 impulsions d'index (~1 s BUSY) — retentatives sur
      --bad-sector au rythme du vrai matériel
- [x] Status Type I vivant : index pulse ~4 ms/tour + TRK0 recalculés
- [x] Microdisc = REAL par défaut ; LOCI dsk_fdc = FAST (SD servie par le
      RP2040, sans mécanique — fidélité là aussi) ; `--fdc-timing real|fast`
- [x] Savestate FDC +5 octets (garde de taille, anciens .ost acceptés)
- [x] +2 tests (856) ; e2e : SEDORIC V4.0 et Manic Miner bootent en REAL

### Sprint 80: Bad sector map — le dommage suit le média (v1.41.0-alpha) - DONE

Lève les trois limitations du sprint 79 : la map était portée par le FDC,
donc par le lecteur ; sur le vrai matériel le dommage appartient au média.

- [x] `fdc_bad_map_t` ; `fdc_t.bad` = map du média sous la tête ;
      `fdc_set_bad_map()` appliquée au select de lecteur
- [x] Microdisc : `bad_map[4]` per-drive, effacée à l'insertion d'une
      nouvelle disquette (hot-swap OSD/IPC/WASM), conservée si même image
- [x] LOCI : `dsk_fdc` couvert — `dsk_bad_map[4]` + `dsk_bad_cli[4]`
      (injections CLI semées à chaque `op_mount`)
- [x] Savestate : section `BAD` per-drive rétro-compatible
- [x] CLI : `--bad-sector [D:]S:T:N` (lecteur A-D), routé Microdisc et/ou
      LOCI, appliqué après le chargement des disques
- [x] +3 tests (854) : storage (media follows), savestate (roundtrip BAD),
      loci (semis au mount)

### Sprint 79: Bad sector map — injection de secteurs illisibles (v1.40.0-alpha) - DONE

Demandé par les tests de robustesse SCUMM-Oric (sputm S517) : le modèle
d'image plate « soignait » silencieusement un ID field MFM corrompu.

- [x] `fdc_t.bad_sectors[16]` + `fdc_add_bad_sector()` ; `fdc_find_sector()`
      renvoie NULL pour un secteur marqué → `FDC_ST_NOT_FOUND` + INTRQ sans
      DRQ (chemin RNF existant, comme un vrai WD1793 sur secteur abîmé)
- [x] Option CLI `--bad-sector S:T:N` (répétable, 16 max), appliquée à
      l'init du Microdisc ; format invalide = erreur fatale
- [x] +1 test `test_storage` (RNF sur le secteur marqué, voisin lisible,
      même id sur une autre piste intact, map pleine). 851 tests.

### Sprint 78b: WASM — fast-load du hot-insert + média par URL (v1.39.0-alpha) - DONE

Finitions du Sprint 78 après un test réel (jeu poker dans le navigateur).

- [x] `web_insert_tap()` fait un **fast-load** (parse TAP + 1er bloc, injection RAM
      différée au frame suivant) au lieu d'un `CLOAD""` à vitesse cassette.
- [x] `web/shell.html` : `?media=<fichier>` (fetch serveur + hot-insert au
      démarrage, avec retry le temps que `g_web_emu` soit prêt) et
      `?rom=oric1|atmos` (force la machine, ex. jeu ORIC-1-only).
- [x] **emcc installé** (emsdk 6.0.1) → `.wasm` réellement généré et exports
      vérifiés (`web_insert_tap`/`web_insert_disk` présents dans le bundle).
- [x] Diag poker : jeu **ORIC-1** (plante en `ILLEGAL QUANTITY` sur Atmos) ;
      rendu rouge correct. → `?rom=oric1&media=poker-asn.tap`. 850 tests.

### Sprint 78: Insertion de média à chaud dans le build WASM (v1.38.0-alpha) - DONE

Comble la dernière lacune du hot-swap (Sprint 63) : dans le navigateur, déposer
un média rechargeait toute la page. Pont C↔JS sur le modèle de `web_key`.

- [x] `src/main.c` (`#ifdef __EMSCRIPTEN__`) : `web_insert_tap(path)` (remplace le
      buffer cassette vivant + arme l'auto-`CLOAD""` via `tape_auto_cload_pending`)
      et `web_insert_disk(drive, path)` (`sedoric_load` + `microdisc_set_disk` ;
      renvoie 0 sans Microdisc → repli reload côté JS).
- [x] `Makefile` : `_web_insert_tap`/`_web_insert_disk` (+ `_malloc`/`_free`) dans
      `EXPORTED_FUNCTIONS` de la cible `wasm`.
- [x] `web/shell.html` : `loadMedia()` tente le hot-swap (`FS.writeFile` + `ccall`)
      puis reload en repli ; média persisté IndexedDB/`sessionStorage`.
- [x] **Limite** : `emcc` absent de l'environnement → `.wasm` non généré ; bloc
      WASM validé par `gcc -D__EMSCRIPTEN__ -fsyntax-only` (0 erreur) + revue.
      Build natif et suite complète verts. 850 tests.

### Sprint 77: OCULA — bordure dans les exports image et AVI (v1.37.0-alpha) - DONE

Comble la lacune Sprint 65 : les captures/AVI ignoraient la bordure overscan
OCULA (pourtant composée à l'affichage). Option `--export-border` (off par
défaut) pour l'inclure — montrer les barres copper/la couleur de bordure des
démos dans un screenshot ou une vidéo.

- [x] `src/video/export.c` : encodeurs PPM/BMP refactorisés en writers bas
      niveau `(rgb, w, h)` partagés ; `video_export_auto_bordered()` compose via
      `video_compose_bordered()` puis écrit (format auto .bmp/.ppm).
- [x] `src/main.c` : flag `--export-border` (`emulator_t.export_border`) ; helper
      `emu_export_image()` pour screenshot de sortie, `--screenshot-at`, F12,
      frame-dump ; AVI ouvert aux dimensions bordées + composition par frame.
- [x] Exports par défaut **inchangés** (zone active) → tests d'export préservés.
- [x] +1 test `test_video` (`test_export_bordered_dimensions`, 12 → 13).
      Vérifié e2e : `--ula ocula --screenshot-at` → 240×224 sans flag,
      **304×272** avec `--export-border`. 850 tests ; builds natif + SDL2 OK.

### Sprint 76: OCULA — synchronisation raster $03EC/$03ED (v1.36.0-alpha) - DONE

Répond à la demande de Dbug (forum t=2709, p=34925 : « VSync/HSync absolutely
necessary… don't flash crazily »). Expose la position du faisceau pour caler les
écritures palette/bordure sur le balayage (barres copper) sans scintillement.

- [x] `$03EC` **RASTER_LO** (R) : octet bas de la scanline (PAL 0..311).
- [x] `$03ED` **RASTER_STATUS** (R) : bit0 = bit8 de la scanline (≥256),
      bit1 = VBLANK (≥256), bit2 = HBLANK (fin de ligne). Scanline complète =
      `LO | ((STATUS&1)<<8)`.
- [x] `src/io/ocula_gpu.c/.h` : fonctions pures `ocula_raster_lo()` /
      `ocula_raster_status()` depuis `frame_cycles` (géométrie PAL locale,
      découplée, testable headless) ; flags `OCULA_RASTER_LINE8/VBLANK/HBLANK`.
- [x] `src/main.c` : `io_read_callback` route `$03EC`/`$03ED` sous `--ula ocula`
      avant la fenêtre GPU ; inertes hors OCULA.
- [x] +3 tests `test_ocula` (98 → 101). `docs/ocula_extensions.md` MAJ + motifs
      BASIC. Vérifié e2e : raster bp ligne 100 → `read $03EC` = `$64`.
- [x] 849 tests unitaires + 3 e2e/intégration ; builds natif + SDL2 OK.

### Sprint 75: Commandes IPC de hot-swap média (--control) (v1.35.0-alpha) - DONE

Comble la lacune Sprint 63 : `--control` ne chargeait qu'une cassette
(`load-tap`). Le pilotage par agent/IDE peut désormais changer disque et
cassette à chaud, comme l'OSD (F6/Suppr).

- [x] `src/control.c` : `load-disk <drive A-D> <path>` (write-back du disque
      sortant si modifié + `--disk-writeback`, `sedoric_load` +
      `microdisc_set_disk` ; `OK drive=X size=… tracks=… sectors=…`),
      `eject-disk <drive>` (`OK drive=X ejected writeback=0|1`), `eject-tape`.
- [x] Helpers `control_drive_index()` (A-D/a-d/0-3) et `control_writeback_drive()`
      (miroir de `osd_writeback_drive`). `CONTROL_PROTO_CAPS` annonce les 3 caps.
- [x] `src/main.c` : `--disk-rom` seul (sans disque) active le contrôleur
      Microdisc → un `.dsk` peut être inséré à chaud ensuite (fidèle : le
      Microdisc est présent même sans disquette).
- [x] `tests/integration/test_control_media_swap.sh` (cible `make test-control`,
      dans `tests`) : 9 assertions, déterministe, skip propre si assets absents.
- [x] 846 tests unitaires + 3 e2e/intégration ; builds natif + SDL2 OK.

### Sprint 74: Test e2e — BASIC pilote l'ACIA 6551 du LOCI à $0380 (v1.34.0-alpha) - DONE

Comble la lacune backlog « E2E BASIC : PRINT vers ACIA $0380 + lecture réponse ».
Un programme BASIC dans l'émulateur exerce le chemin complet d'un terminal réel
sur l'ACIA 6551 à `$0380` (adresse de l'ACIA sous `--loci`, fenêtre du modem).

- [x] `examples/loci_acia_e2e.bas` : init 6551 (reset status, control 9600 8N1,
      command DTR/RTS) ; pour chaque octet de `ATDT5551234`<CR> : attend TDRE
      (bit4), `POKE $0380`, attend RDRF (bit3), relit l'octet, stocke à `#4000` ;
      sentinel `$FF` à `#3FFC`.
- [x] `tests/integration/test_loci_acia_e2e.sh` : bas2tap `--auto-run`, headless
      `--loci --serial loopback`, `--dump-ram-at`, vérifie sentinel + round-trip
      (octets relus == chaîne émise) → TX (`POKE $0380`) et RX (`PEEK $0380`)
      prouvés. Loopback = déterministe et hermétique (ni réseau ni NVRAM).
      Skip propre si ROM/outils absents.
- [x] `Makefile` : cible `test-loci-acia-e2e` (`.PHONY` + `tests`).
- [x] Outillage/exemples seuls, binaire inchangé. 846 tests unitaires + 1 e2e.

**Note** : backend **loopback** et non `picowifi` — le boot **headless** du
backend PicoWiFi n'est pas encore déterministe (le TX se bloque par moments) ;
le couple `--loci --serial picowifi` reste démontré interactivement
(`examples/picowifi_test.bas`). À fiabiliser dans un sprint dédié.

*(Correction v1.34.1 : un « bug bas2tap tokeniserait les REM » initialement noté
ici était un faux positif — vérifié, le reste de ligne après le token `REM`
(`$9D`) est copié verbatim ; l'instabilité observée venait du non-déterminisme
du backend picowifi headless.)*

### Sprint 73: Modèle de timing du bus MIA (LOCI) — blocage picowifi (v1.33.0-alpha) - DONE

Branche dédiée `feature/loci-mia-timing` (off-main, indépendante des sprints
fidélité CPU 68-72). Reproduit un symptôme **matériel réel** : sur un vrai LOCI,
un mauvais réglage de timing du bus MIA bloque l'accès au modem picowifi.

- [x] Compréhension : la MIA échantillonne le bus 6502 via PIO (RP2040) à des
      décalages sous-cycle (0-31) autour de PHI2, réglés par `MAP_TUNE_*`
      (valeur dans le registre A) : `tmap`(=RV1), `tior`/`tiow`, `tiod`, `tadr`.
      Le picowifi = fenêtre **ACIA 6551** `$0380-$0383` lue par ce même
      échantillonnage → `tior` mal calé corrompt les accès → modem injoignable.
- [x] `loci_t` stocke les 6 paramètres de timing (défauts firmware tmap=10/
      tula=10/reste 0) + fenêtre fiable `[mia_tior_lo, mia_tior_hi]`
      (défaut 0-31 = tout marche → rétro-compatible)
- [x] `MAP_TUNE_*` lisent le registre A et stockent (au lieu d'un no-op) ;
      `loci_mia_io_reliable()` ; `loci_set_mia_window()`
- [x] `main.c` : accès ACIA `$0380` sous `--loci` avec `tior` hors fenêtre
      corrompu (lecture `$FF`, écriture ignorée) → reproduit le blocage ;
      option `--loci-mia-window LO-HI` (banc d'essai du réglage)
- [x] +4 tests `test_loci` (152 → 156) ; LOCI = 6551 confirmé (firmware
      `oric/acia.c`) → `acia6551.c` fidèle au modèle de registres
- [x] 842 → 846 tests ; builds natif + SDL2 OK

**Note de fidélité** : l'émulateur n'a pas de bus analogique, donc le « bon »
réglage dépend de la carte (le firmware `ADJ_SCAN` le balaye par essai). Le
modèle est *comportemental* (fenêtre fiable configurable), pas ns-exact — il sert
à reproduire/diagnostiquer le blocage et tester la logique de tuning du logiciel.

### Sprint 72: Cœur cycle-par-cycle — cadencement périphériques au cycle (v1.32.0-alpha) - DONE

Cinquième jalon « fidélité matérielle », sur **branche dédiée**
`feature/cpu-percycle-tick` (isolée à la demande, vu le rayon d'impact).

- [x] CPU : horloge per-cycle — champ `on_cycle` + `cpu_set_cycle_callback()` +
      `cpu_tick()` ; chaque accès bus consomme 1 cycle, les cycles internes sont
      réconciliés en fin d'instruction (padding jusqu'au compte officiel)
- [x] Handlers NMI/IRQ alignés (le `+= 7` direct devient padding ; lectures de
      trace IRQ via `memory_read` pour ne pas perturber l'horloge)
- [x] `src/main.c` : `cpu_cycle_tick()` enregistré ; le bloc batch
      post-instruction (VIA/FDC×2/ACIA/DTL/Mageco) est retiré → cadencement en
      phase avec les accès bus
- [x] Rétro-compat : sans callback (tests unitaires isolés), le CPU n'accumule
      que les cycles → comportement et compte **identiques**
- [x] +2 tests `test_cpu` (90 → 92) ; validation intégration : suite 856/0,
      boots ORIC-1 + Atmos rendus, `--type-keys` (clavier IRQ) traité, bench
      95–113 MHz éq. (overhead négligeable), Valgrind clean
- [x] 840 → 842 tests ; builds natif + SDL2 + headless OK

**Note** : bénéfice observable volontairement limité sur l'ORIC (le 6502
n'échantillonne l'IRQ qu'aux frontières d'instruction). L'apport est la justesse
du modèle (accès bus visibles au bon cycle) et la fondation pour un éventuel
rendu raster sub-ligne. Branche séparée pour merge décidé après revue.

### Sprint 71: Fidélité CPU — write-back RMW (début cycle-accurate) (v1.31.0-alpha) - DONE

Quatrième jalon « fidélité matérielle », première brique du chantier
cycle-accurate. Sur le 6502 NMOS, les Read-Modify-Write font **trois** accès
mémoire : lecture, **réécriture de la valeur d'origine** (write-back), puis
écriture modifiée. Le write-back manquant est invisible sur RAM mais observable
sur les registres I/O sensibles à l'écriture.

- [x] `src/cpu/opcodes.c` : RMW mémoire factorisés en helpers (`op_asl_m`,
      `op_lsr_m`, `op_rol_m`, `op_ror_m`, `op_inc_m`, `op_dec_m`) + write-back ;
      idem RMW illégaux (`SLO/RLA/SRE/RRA/DCP/ISC`)
- [x] Comportement fonctionnel (valeur finale, drapeaux, cycles) inchangé ; seul
      l'accès bus intermédiaire est ajouté
- [x] +2 tests `test_cpu` prouvant le double write sur I/O instrumentée ($0310)
      (88 → 90) ; suite 854/0, `test-rom` 12/12, boot Atmos écran rendu
- [x] 838 → 840 tests ; builds natif + SDL2 + headless OK, 0 régression

**Décision de scope** : la suite de la cycle-accuracy stricte (relocalisation du
cadencement de TOUS les périphériques — VIA/FDC×2/ACIA/DTL/Mageco/raster — au
cycle près, et timing sub-instruction des 256 opcodes) est un chantier à très
fort rayon d'impact pour un bénéfice **observable faible sur l'ORIC** : le 6502
n'échantillonne l'IRQ qu'aux frontières d'instruction, donc le cadencement
per-cycle vs per-instruction produit un résultat identique à cette granularité ;
seuls de rares cas (lecture d'un timer VIA en plein milieu d'un RMW) diffèrent.
Reporté comme effort dédié, à n'engager que sur un cas de test défaillant concret.

### Sprint 70: Fidélité VIA 6522 — registre à décalage, T2 pulse-counting, pins CA2/CB2 (v1.30.0-alpha) - DONE

Troisième jalon « fidélité matérielle » (audit Phosphoric vs Oric réel).
Comblement des fonctions du 6522 jusqu'ici absentes (champs présents mais jamais
cadencés). Non exercées par la ROM ORIC standard (qui n'utilise CA2/CB2 que comme
sorties manuelles pour le PSG) → fidélité périphériques / Telestrat. **Ajouts
strictement additifs : les chemins clavier/PSG/timers existants sont inchangés.**

- [x] **Registre à décalage** (ACR bits 4-2) : 8 modes (désactivé, shift-in/out
      sous T2, sous φ2, sous horloge externe CB1) ; 8 bits → drapeau SR + arrêt,
      sauf shift-out libre (100) continu sans drapeau
- [x] Cadencement dans `via_update()` (φ2 : 1 décalage / 2 cycles ; T2 :
      1 / (latch+2)) ; horloge externe via `via_shift_clock()` ; amorçage par
      écriture (sortie) / lecture (entrée) de SR
- [x] **Timer 2 pulse-counting** (ACR bit 5) : `via_pb6_pulse()` décrémente T2
      sur front descendant PB6, underflow → drapeau T2
- [x] **Pins CA2/CB2** : `via_get_ca2()` / `via_get_cb2()` (sortie manuelle PCR
      + données shift-out) + entrée série `via_set_cb2_input()`
- [x] +6 tests `test_io` (31 → 37) ; savestate `.ost` inchangé
- [x] 832 → 838 tests ; builds natif + SDL2 + headless OK, boot ORIC vérifié

**Reste pour la « réplique parfaite »** : cœur cycle-par-cycle (dummy-write des
RMW observable sur l'I/O, timing sub-instruction, raster sub-ligne) ; modes
CA2/CB2 pulse/handshake complets ; effets analogiques PAL (hors-scope logiciel).

### Sprint 69: Fidélité audio — écritures PSG horodatées au cycle (digidrums) (v1.29.0-alpha) - DONE

Deuxième jalon « fidélité matérielle » (audit Phosphoric vs Oric réel).
`ay_generate()` rendait un buffer entier (~46 ms) depuis l'état courant des
registres, lu sur le thread audio SDL pendant que le CPU les modifiait → toute
écriture intra-buffer s'appliquait à résolution d'un buffer. Les **digidrums**
(échantillons 4 bits joués en martelant le registre de volume à cadence audio)
s'effondraient à une valeur par buffer.

- [x] `include/audio/audio.h` + `src/audio/ay3891x.c` : file d'écritures
      horodatées au cycle CPU (`ay_event_t`, anneau SPSC lock-free `_Atomic` de
      4096 entrées) ; `ay_write_data_timed(ay, data, cpu_cycle)`
- [x] `ay_generate()` rejoue les écritures sur un état de lecture `ay_play_t`
      dédié, à la position d'échantillon intra-buffer exacte (proportionnelle à
      la position en cycles de l'écriture dans la fenêtre du buffer)
- [x] Découplage strict : `registers[]` autoritaires mis à jour immédiatement
      (lectures/clavier/headless/savestate) ; chemin non horodaté (`ay_write_data`,
      tests/headless) identique octet pour octet à l'historique
- [x] `src/main.c` psg_decode : horodatage avec `emu->cpu.cycles`
- [x] `src/savestate.c` : `ay_sound_resync()` après load (`.ost` inchangé,
      sérialisation champ par champ)
- [x] +4 tests `test_audio` (8 → 12) dont la preuve du timing sous-buffer du
      digidrum ; mixage linéaire + courbe DAC log inchangés (déjà conformes)
- [x] 828 → 832 tests ; builds natif + SDL2 + headless OK, 0 régression

**Reste pour la « réplique parfaite »** : cœur cycle-par-cycle (dummy-write des
RMW, timing I/O sub-instruction, raster sub-ligne) ; VIA 6522 (shift register,
modes CA2/CB2 complets, T2 pulse-counting) ; effets analogiques PAL (hors-scope).

### Sprint 68: Fidélité CPU — opcodes illégaux NMOS 6502 (v1.28.0-alpha) - DONE

Premier jalon du chantier « fidélité matérielle » (issu de l'audit comparatif
Phosphoric vs silicium réel / Oricutron). Le 6502 NMOS de l'ORIC-1/Atmos décode
les 256 opcodes ; les 105 codes non officiels étaient jusqu'ici traités comme
NOP, cassant les démos/intros de la scène qui s'en servent.

- [x] `src/cpu/opcodes.c` : table d'opcodes corrigée (noms réels, tailles et
      cycles NMOS exacts au lieu de `???`/taille 1)
- [x] RMW combinés `SLO`/`RLA`/`SRE`/`RRA`/`DCP`/`ISC` (7 modes chacun) via les
      helpers ALU officiels (`op_adc`/`op_sbc`/`op_cmp`)
- [x] `LAX` (LDA+LDX), `SAX` (store A&X)
- [x] Famille AND-immédiat `ANC`/`ALR`/`ARR`/`SBX`
- [x] Groupe instable `ANE/XAA`/`LXA`/`LAS`/`SHA`/`SHX`/`SHY`/`TAS` — modèles
      documentés (constante magique `0xEE`, `reg & (high(addr)+1)`), commentés
      comme instables sur silicium réel
- [x] NOP multi-octets : lecture-bus « morte » de l'opérande (fidélité I/O) +
      pénalité de franchissement de page en abs,X
- [x] `JAM`/`KIL` (12 codes) : figent le CPU (`cpu->halted`) comme le vrai NMOS
- [x] +14 tests `test_cpu` (74 → 88) ; `test_rominfo` : 2ᵉ JSR de la ROM de test
      replacé `$C020` → `$C012` (sweep linéaire aligné, padding `$FF` = `ISC`)
- [x] Audit collatéral : flag Z décimal calculé sur le résultat binaire =
      comportement NMOS réel (pas un bug) → inchangé
- [x] 814 → 828 tests ; builds natif + SDL2 OK, 0 régression

**Reste pour la « réplique parfaite » (prochains jalons fidélité)** : horodatage
fin du PSG (digidrums, écritures registre intra-buffer) ; cœur cycle-par-cycle
(dummy-write des RMW, timing I/O sub-instruction, raster sub-ligne) ; shift
register + modes CA2/CB2 complets + T2 pulse-counting de la VIA 6522.

### Sprint 67: PicoWiFi — AT$SCAN drapeau de sécurité (fidélité doScan) (v1.27.6-alpha) - DONE

Bug remonté par l'équipe OricTel : `AT$SCAN` omettait le drapeau de sécurité
par réseau, divergeant du firmware réel `PicoWiFiModemUSB`. Un client suivant
le format firmware voyait TOUS les réseaux « ouverts ».

- [x] Réf firmware `at_proprietary.h` doScan() : `printf("%d %s\t%c\r\n", ...,
      scanOpen[i] ? 'O' : 'S')` — `O`=ouvert, `S`=sécurisé
- [x] `src/io/serial_picowifi.c` pw_scan() : sortie `"%d %.32s\t%c\r\n"`,
      `seen[]` → `pw_ap_t{ssid,sec}` (miroir scanList[]/scanOpen[])
- [x] Source nmcli élargie à `nmcli -t -f SSID,SECURITY` ; drapeau dérivé
      (`sec = SECURITY vide || "--" ? 'O' : 'S'`)
- [x] `pw_split_ssid_sec()` : découpe sur le dernier `:` NON échappé (terse
      échappe `:`→`\:`, `\`→`\\`) + dé-échappe le SSID → SSID à `:` non tronqué
- [x] Liste de repli (headless/CI) avec drapeaux mixtes O/S
- [x] `test_scan_lists_networks` renforcé (vérifie `\tO`/`\tS`) ; 72/72
      picowifi, 814 total inchangé

### Sprint 25: Centronics Printer Emulation (v1.7.0-alpha) - DONE
- [x] Centronics printer module (src/io/printer.c, include/io/printer.h)
- [x] VIA Port A data capture via CA2 STROBE detection (PCR forced low→high)
- [x] CLI: -p/--printer FILE (capture LPRINT/LLIST output to file)
- [x] Automatic flush on newline/carriage return for real-time output
- [x] Strobe sequence validation (CA2 forced low must precede forced high)
- [x] Open/close/reopen semantics with byte count tracking
- [x] Integration: io_write_callback intercepts PCR writes for strobe detection
- [x] 10 new printer unit tests (206 total, 100% pass)

### Sprint 26: MCP-40 Plotter Emulation (v1.8.0-alpha) - DONE
- [x] MCP-40 4-color pen plotter module (src/io/mcp40.c, include/io/mcp40.h)
- [x] Command parser: H (Home), D (Draw), M (Move), J (Color), P (Print), I (Init), L (LineType), Q (CharSize)
- [x] 480x400 RGB framebuffer with Bresenham line drawing
- [x] 4 pen colors: black (0), blue (1), green (2), red (3)
- [x] Built-in 5x7 bitmap font (95 ASCII glyphs) with scalable char size
- [x] 5 line types: solid + 4 dashed patterns
- [x] BMP export (24-bit uncompressed, bottom-up BGR)
- [x] Command chaining (multiple commands per line) and multi-coordinate polylines
- [x] Printer type routing: PRINTER_NONE/PRINTER_TEXT/PRINTER_MCP40
- [x] CLI: --printer-type (text, mcp40) to select printer mode
- [x] Integration with Centronics STROBE capture (Sprint 25)
- [x] 10 new MCP-40 unit tests (216 total, 100% pass)

### Sprint 27: Display Scaling (v1.9.0-alpha) - DONE
- [x] CLI: --scale N (1, 2, 3, 4) with default x3 (720x672)
- [x] Window scaling: x1=240x224, x2=480x448, x3=720x672, x4=960x896
- [x] Nearest-neighbor upscale (SDL_HINT_RENDER_SCALE_QUALITY=0, pixel-perfect)
- [x] SDL2 renderer scaling via SDL_RenderSetLogicalSize (GPU-accelerated)
- [x] F3 hotkey: cycle scale x1→x2→x3→x4 at runtime
- [x] renderer_set_scale/get_scale/cycle_scale API
- [x] Fullscreen mode preserved (window resize skipped in fullscreen)
- [x] 10 new display scaling tests (226 total, 100% pass)

### Sprint 28: CPU Trace Logging (v1.10.0-alpha) - DONE
- [x] `--trace FILE`: log every CPU instruction with disassembly + register state
- [x] `--trace-max N`: limit trace to N instructions (0 = unlimited)
- [x] `cpu_trace_t` structure: fp, active, count, max_count, owns_fp
- [x] `trace_init/open/attach/log_instruction/close/set_max` API
- [x] Output format: `CYCLES  PC  BYTES  DISASM  A=XX X=XX Y=XX SP=XX P=XX`
- [x] Integration in emulation loop (before cpu_step, captures pre-execution state)
- [x] 10 new trace tests (236 total, 100% pass)

### Sprint 29: CPU Performance Profiler (v1.11.0-alpha) - DONE
- [x] `--profile FILE`: write CPU performance profile report on exit
- [x] Per-address execution counts (64K address space)
- [x] Per-address cycle usage tracking
- [x] Opcode frequency histogram (256 opcodes)
- [x] Top 20 hotspots by execution count and by cycle usage
- [x] Summary: total instructions, total cycles, avg cycles/instruction
- [x] `profiler_init/start/stop/record_instruction/record_cycles/reset/report` API
- [x] 10 new profiler tests (246 total, 100% pass)

### Sprint 30: ROM Analysis Tools (v1.12.0-alpha) - DONE
- [x] `--rom-info [FILE]`: analyze ROM vectors, targets, strings, usage stats
- [x] Vector extraction: RESET ($FFFC), NMI ($FFFA), IRQ ($FFFE)
- [x] JSR/JMP target scanning with reference counts (up to 512 targets)
- [x] ASCII string detection (min 4 chars, up to 256 strings)
- [x] Code vs data vs fill usage classification
- [x] Byte pattern search (rominfo_find_pattern)
- [x] `rominfo_init/analyze/report/report_to_file/find_pattern` API
- [x] 10 new ROM analysis tests (256 total, 100% pass)

### Sprint 31: Comprehensive Documentation (v1.13.0-alpha) - DONE
- [x] API reference (docs/api/README.md): 24 headers, ~200 functions, 22 module sections
- [x] User guide rewrite (docs/user-guide/README.md): 17 sections, all features
- [x] Compatibility list update (docs/COMPATIBILITY.md): 256 tests, Atmos ROM
- [x] Fixed stale data in README, CIRRUS_OS quality gates, ROADMAP
- [x] Version bump to 1.13.0-alpha

### Sprint 32: Code Coverage Measurement (v1.14.0-alpha) - DONE
- [x] `make coverage` target: gcov instrumented build + per-file report
- [x] `COVERAGE=1` Makefile option for instrumented builds
- [x] 24 new coverage improvement tests (keyboard, microdisc, FDC, sedoric, debugger, CPU)
- [x] Coverage results: 72.2% global, 78.9% excluding interactive REPL
- [x] 15 modules >= 80%, 5 modules 60-80%, 4 modules < 60%
- [x] 280 total tests (256 + 24 coverage, 100% pass)

### Sprint 32 bugfix: Deferred Fast-Load Fix (v1.14.1-alpha) - DONE
- [x] Fix fast-load écrasé par test RAM ROM BASIC 1.0 (Popeye #1)
- [x] Injection différée : buffer TAP data, inject after 3M cycles
- [x] `emulator_t` : `fastload_buf`, `fastload_addr`, `fastload_size`, `fastload_pending`
- [x] Cleanup du buffer dans `emulator_cleanup()`
- [x] 3 nouveaux tests deferred fast-load (283 total, 100% pass)

### Sprint 32 bugfix: Timer Exact-Zero & Type-Keys Debounce (v1.14.2-alpha) - DONE
- [x] Fix Timer1/T2 IFR jamais levé quand compteur atteint exactement 0 (Popeye #4)
- [x] Fix type-keys perd les caractères répétés consécutifs (Popeye #3)
- [x] `emulator_t` : `type_keys_last_char`, `type_keys_debounce`

### Sprint 32 bugfix: Post-CLOAD BASIC Rechain (v1.14.3-alpha) - DONE
- [x] Fix BASIC next-line pointers stale après CLOAD (TYRANN.TAP, 6 blocs)
- [x] `basic_rechain()` : routine C reproduisant la logique ROM $C56F
- [x] Détection fin CLOAD via `cload_data_rts` ($E502 BASIC 1.0, $E50A Atmos)
- [x] Rechain après fast-load ET après chaque CLOAD via ROM patching
- [x] Protection type BASIC : ne rechain que si ZP $66 == $00
- [x] 286 tests (100% pass)
- [x] 3 nouveaux tests (286 total, 100% pass)

### Sprint 33: ACIA 6551 Serial Interface (v1.15.0-alpha) - DONE
- [x] MOS 6551 ACIA at $031C-$031F — 4 registers (Data, Status, Command, Control)
- [x] Compatible Digitelec DTL 2000, MCP RS232-C, Kenema 300/300, Telestrat
- [x] Backend loopback (circular buffer 256 bytes, TX → RX, for unit tests)
- [x] Backend TCP (non-blocking socket, BBS/Telnet/Minitel connections)
- [x] Backend PTY (POSIX pseudo-terminal, minicom/screen interface)
- [x] V23 Minitel/Prestel mode (1200/75 bauds asymétriques, --serial-v23)
- [x] IRQ support: IRQF_SERIAL (0x04), RX (RDRF) and TX (TDRE) interrupts
- [x] Baud rate table: 50-19200 from Control Register, cycle-accurate timing
- [x] Signal lines: DCD, DSR, CTS with active-low encoding
- [x] CLI: --serial (loopback|tcp:host:port|pty), --serial-v23
- [x] 11 new tests (297 total, 100% pass)

### Sprint 33b: ACIA Oricutron-level corrections (v1.15.1-alpha) - DONE
- [x] Fix: ACIA clock uses 1.8432 MHz crystal / 16 prescaler (not 1 MHz CPU clock)
- [x] Fix: Frame format variable — calcul dynamique (1 start + 5-8 data + 0-1 parity + 1-2 stop)
- [x] Fix: Data bitmask from word length (0xFF/0x7F/0x3F/0x1F)
- [x] New: Modem Hayes backend — AT command emulation (ATD, ATA, ATH, ATZ, ATE, ATS0, +++ escape)
- [x] New: TCP server mode (modem:listen:port) for hosting BBS
- [x] New: COM serial backend via termios (Linux) — baud 50-115200, databits 5-8, parity N/E/O
- [x] New: ACIA state in savestate (.ost "SER" section, 14 fields)
- [x] New: Configurable ACIA base address (--acia-addr, default $031C)
- [x] New: 64KB RX/TX ring buffers for modem backend
- [x] 6 new tests (303 total, 100% pass)

### Sprint 34: Serial hardening + bas2tap + AY fix + frame limiter (v1.16.0-alpha) - DONE
- [x] Fix: 50 Hz PAL frame limiter (SDL_Delay pacing, was running at 60 Hz+)
- [x] Fix: AY-3-8912 reg7 bit 6 controls keyboard scan (Port A direction)
- [x] Fix: acia_init() garbage from uninitialized stack (memset before read)
- [x] Fix: WDC 65C51 IRQ mode re-asserts while RDRF set (was no-op)
- [x] Fix: acia_tick() aggregated (one call per instruction, ~5x less overhead)
- [x] Fix: Echo via TX timing, DCD/DSR in status, TX overwrite traced, EINTR
- [x] New: --serial modem (pure AT command mode, ATD host:port to dial)
- [x] New: --serial digitelec:host:port (DTL 2000 V23 modem, DTR/CTS/DCD)
- [x] New: --serial com:baud,bits,P,stop,device (real serial port, termios)
- [x] New: --serial-trace FILE (debug TX/RX/signals with CPU timestamps)
- [x] New: --serial-buffer N (RX FIFO) + --serial-irq-on-rdrf (65C51 mode)
- [x] New: bas2tap tokenizer with 120 keywords extracted from ROM $C0EA
- [x] New: Digitelec DTR edge-detect factored (digitelec_check_dtr)
- [x] New: EINTR-safe read/write in all backends
- [x] 2 new tests (test_rx_fifo, test_irq_65c51_mode), 305 total

### Sprint 34b: HIRES bit 7 fix + keyboard tests (v1.16.1-alpha) - DONE
- [x] Fix: HIRES bit 7 inversion swap encre/papier (was XOR ^7 color complement)
- [x] New: 21 keyboard mapping tests (init, press_char, arrows QWERTY/AZERTY, multi-touch)
- [x] New: keytest.bas example program (key code display utility)
- [x] Docs: CLAUDE.md improvements (dynamic test counts, ACIA I/O routing, condensed CLI)
- [x] 22 new tests, 327 total

### Sprint 34c: Keyboard naming convention cleanup (v1.16.2-alpha) - DONE
- [x] Refactor: matrix[col] indexed by HW col (VIA ORB[0:2]), bit = HW row (PSG R14)
- [x] Refactor: char_entry_t fields {row,col} → {col,row}; U/S macros args swapped
- [x] Refactor: ORIC_LSHIFT/RETURN_ROW/_COL constants names swapped, values unchanged
- [x] Refactor: qwerty_tab[] / special_keys[] / handle_qwerty aligned on HW semantics
- [x] Refactor: test KEY_IS_PRESSED(kb, col, row); ORIC_*_COL/_ROW args reordered
- [x] Docs: keyboard.h matrix layout convention explicit (col indexed, row bits)
- [x] 327 tests pass (zero behavior change, pure rename)

### Sprint 34d: TAP writer ROM-format compatibility (v1.16.3-alpha) - DONE
- [x] Fix: tap_write_header insère 2 octets réservés ($00 $00) après marker $24
- [x] Header passé du format 7-byte (tool legacy) au 9-byte ROM-compatible
- [x] Oricutron / Euphoric / hardware physique acceptent désormais les .tap
      générés par bin2tap et bas2tap (avant : plantage CLOAD "" sur asteroids)
- [x] Parser tap_read_header inchangé (gère déjà 7-byte ET 9-byte)
- [x] Investigation rapport HIRES TEXT glitch (équipe Asteroids) = faux positif
      Phosphoric ; charset HIRES $9800 doit être initialisé par le programme
- [x] 327 tests pass (aucune régression)

### Sprint 34e: TAP writer machine-code auto-run flag (v1.16.4-alpha) - DONE
- [x] Fix: bin2tap utilise auto-run = $C7 pour binaire (et non $80)
- [x] $80 = RUN BASIC ; $C7 = JMP start_addr (convention CSAVE "...",A,E,J)
- [x] Aligné sur tous les .tap commerciaux (007, ATLANTIS, citadel, johnny…)
- [x] Header asteroids final: `16 16 16 24 00 00 80 c7 …` (ROM-compat complet)
- [x] Confirmé OK Oricutron + Phosphoric (chargement + auto-exec)
- [x] 327 tests pass

### Sprint 34f: TAP reader 9-byte header parsing fix (v1.16.5-alpha) - DONE
- [x] Fix: tap_read_header lit type/auto-run au bon offset en 9-byte ROM
- [x] Avant: type=raw[0], auto=raw[1] (les 2 octets réservés $00 $00)
- [x] Après: skip 2 octets réservés, type=raw[2], auto=raw[3]
- [x] Conséquence : fast-load binaires .tap (asteroids) plus interprétés
      comme BASIC, plus de basic_rechain destructeur + RUN auto-typé
      causant ?ILLEGAL QUANTITY ERROR
- [x] Vérifié TYRANN.SAV (type=$80), examples BASIC (type=$00), asteroids ($80)
- [x] 327 tests pass

### Sprint 34g: Fast-load auto-exec for machine code (v1.16.6-alpha) - DONE
- [x] Feat: fast-load auto-exec quand type=$80 + auto-run bit 7 mis ($C7/$80)
- [x] Force cpu.PC = start_addr après injection deferred (équivalent JMP ROM)
- [x] Nouveau champ fastload_auto_run dans emulator_t
- [x] asteroids.tap s'auto-exécute en fast-load (mode -f)
- [x] TYRANN.SAV.tap (auto-run=$00) inchangé — pas d'exec forcé
- [x] 327 tests pass

### Sprint 34h: Makefile header-dependency tracking (v1.16.7-alpha) - DONE
- [x] Fix: -MMD -MP ajouté à CFLAGS, gcc émet des .d listant les includes
- [x] -include $(OBJECTS:.o=.d) fait lire les dépendances au prochain build
- [x] Touch include/*.h déclenche maintenant le rebuild des .c qui l'incluent
- [x] Évite les binaires stale (cause du faux négatif auto-exec MC v1.16.6)
- [x] clean: supprime aussi les .d générés
- [x] 327 tests pass

### Sprint 34i: Auto-CLOAD pour -t sans -f (v1.16.8-alpha) - DONE
- [x] Feat: phosphoric -t game.tap auto-injecte CLOAD"" après BASIC ready
- [x] Mécanisme via type_keys existant, déclenché ~3M cycles après boot
- [x] Routine CLOAD ROM lit la cassette + auto-run flag traité normalement
- [x] tape_auto_cload_pending dans emulator_t activé hors fast_load
- [x] Comportement -f inchangé
- [x] 327 tests pass

### Sprint 34j: Fast-load timing + bin2tap --no-autorun (v1.16.9-alpha) - DONE
- [x] Fix: délai auto-exec fast-load 3M → 5M cycles (init ROM complète)
- [x] À 3M cycles, VIA PCR/IER pas finis de configurer ⇒ glitch HIRES
      + frame-wait CB1 bloqué (rapport équipe Oric Asteroids)
- [x] À 5M cycles, ROM dans boucle READY = état VIA/ULA stable
- [x] Auto-CLOAD"" (-t sans -f) aligné sur même délai 5M
- [x] Feat: bin2tap --no-autorun = force auto-run $00 (opt-out flag)
- [x] Doc alternative: -t sans -f utilise CLOAD ROM complète, état propre
- [x] 327 tests pass

### Sprint 34k: Fast-load séparation injection/auto-exec (v1.16.10-alpha) - DONE
- [x] Fix: CALL manuel ILLEGAL QUANTITY dans fenêtre 3.6M-5M cycles
- [x] Cause racine : injection à 5M, mais READY à 3.6M → fenêtre 1.4s où
      $0500 = $00 (init RAM) → JMP via CALL = BRK = ?ILLEGAL QUANTITY
- [x] Phase 1 injection RAM → 3M cycles (avant READY)
- [x] Phase 2 auto-exec / RUN → 5M cycles (VIA stable)
- [x] fastload_autoexec_pending dans emulator_t
- [x] Validé : CALL 1280 manuel sur binaire test = OK (L10 = "Ready" pas erreur)
- [x] 327 tests pass

### Sprint 34aw: WD1793 cycle-accurate sur bus DSK LOCI (v1.16.50-alpha) - DONE
- [x] Découverte : fdc_t cycle-accurate déjà existe (src/storage/disk.c)
- [x] Réutilisation au lieu de réinventer (Microdisc le sert déjà)
- [x] loci_t étendu : dsk_fdc + dsk_image[4] + dsk_image_size[4]
      + dsk_tracks[4] + dsk_sectors[4]
- [x] Callbacks DRQ : loci_fdc_set_drq/clr_drq → loci.dsk_drq (active-low)
- [x] dsk_open : slurp .DSK en RAM (≤ 1 MB), apply_selection si active
- [x] dsk_close : free buffer + détache FDC si active
- [x] loci_dsk_read/write routent vers fdc_read/write (reg 0-3)
- [x] STATUS OR'd-in NOT_READY si drive non monté
- [x] fdc_ticktock hooké dans main loop
- [x] 3 tests nouveaux (Restore command + drive switch + DRQ callback)
- [x] 119 tests test-loci (vs 116), 484 global, 0 régression
- [x] Makefile : src/storage/disk.c ajouté à TEST_LOCI_SRCS

### Sprint 34av+: E2E LOCI complet 007 jeu chargé (v1.16.49-alpha) - DONE
- [x] Doc LOCI TUI récupérée wiki sodiumlb (raccourcis t/a-d/m/o/r/s/F)
- [x] Workflow : t → SPACE (picker) → SPACE (file) → ESC (boot)
- [x] E2E 007.TAP : LOCI → t+SPACE+SPACE+ESC → BASIC 1.1 → CLOAD → run
- [x] Multi-block TAP : 3 getsync (offsets 0, 137, 8322)
- [x] Auto-run titre "007 Dangereusement Vôtre" DOMARK 1985 affiché
- [x] CR : docs/CR/2026-06-07_E2E_LOCI_007_Tape_Loaded.md

### Sprint 34av: Automatisation E2E LOCI (v1.16.48-alpha) - DONE
- [x] \e (ESC) ajouté à oric_keyboard_press_char (col=1, row=5)
- [x] Flèches \u \d \l \r (col=4, rows 3/6/5/7) via sentinelles 0x80-0x83
- [x] \e + \u/d/l/r parsers dans --type-keys mode matrix
- [x] Mode loci-hid: dans --type-keys (préfixe + HID xram)
- [x] E2E test : boot LOCI → ESC → BASIC 1.1 → PRINT/RUN automatisés
- [x] Navigation TUI partielle (sélection picker = SPACE, à itérer)
- [x] 481 tests pass, 0 régression

### Sprint 34au: Impl 7 ops LOCI stubbées (v1.16.47-alpha) - DONE
- [x] op_cpu_phi2 retourne 1 MHz dans AXSREG
- [x] op_oem_codepage no-op success
- [x] op_stdin_opt no-op success
- [x] op_map_tune_tmap + tior + tiow + tiod + tadr (5 ops via helper commun)
- [x] Dispatch updated avec les 7 case statements
- [x] Couverture ops : 27/36 → 34/36 (94%)
- [x] 8 tests test-loci nouveaux + 2 mis à jour (opcode $07 indéfini)
- [x] 116 tests test-loci, 481 tests global, 0 régression

### Sprint 34at: CSAVE ORIC-1 fix via Option A sénior (v1.16.46-alpha) - DONE
- [x] Diagnostic Phosphoric vs Oricutron via Senior_Followup doc + 48 bytes
- [x] PCLOG inconditionnel dans cpu_step prouve $E7FE jamais, $E80A oui
- [x] Sénior valide Option A après désasm indépendant : csave_end → $E80A
- [x] writefileheader_entry trap nouveau (snapshot AVANT mutation $5F/$60)
- [x] csave_header_snap[9] + csave_fname_snap[17] + csave_snap_valid
- [x] csave_in_progress guard contre re-entry CLOAD sur shared $E80A
- [x] Atmos $E93C non touché (validé sénior)
- [x] Round-trip ORIC-1 CSAVE/CLOAD/LIST validé E2E auto
- [x] Régression Atmos OK
- [x] 473 tests pass, 0 régression

### Sprint 34as: Follow-up review sénior — MBR + CSAVE staging buffer (v1.16.45-alpha) - DONE
- [x] MBR parser : détection auto, partition_lba propagé via read/write_sector
- [x] Types FAT supportés : 01/04/06/0B/0C/0E/1B/1C
- [x] 3 tests MBR (open, list, read par partition)
- [x] CSAVE Atmos source les params depuis $02A8..$02B0 (staging buffer)
- [x] Désassemblée $E60F-$E618 décodée (LDX#9 ; LDA $02A7,X ; DEX ; BNE)
- [x] csave_header_buf + csave_filename_buf dans rom_patches_t (Atmos)
- [x] Auto-flag et type byte réels (plus de C7/00 forcé)
- [x] BASIC 1.0 reste sur fallback TXTTAB/VARTAB (csave_end ORIC-1 broken
      séparément — sprint 34at)
- [x] 473 tests pass (470 + 3 MBR), 0 régression
- [x] AIGLE.TAP régression OK

### Sprint 34ar: Review ingé sénior — 3 fixes + cleanup (v1.16.44-alpha) - DONE
- [x] Diagnostic "Errors found" craqué par ingé sénior :
  - [x] $02B1 storezero (Atmos seulement) — parity accumulator
  - [x] SEC à readbyte_end (Atmos) — VERIFY logic needs C=1
- [x] readbyte_storezero + readbyte_setcarry dans rom_patches_t
- [x] Patch readbyte_entry data-driven (les 2 ROMs BASIC)
- [x] fd_kind[] et dir_kind[] remplacent pointer-tag (UB-adjacent éliminé)
- [x] mkstemp() au lieu de /tmp/loci_extract_<basename>
- [x] Silence handler EOT retiré (compensation devenue inutile)
- [x] AIGLE.TAP régression OK
- [x] Round-trip CSAVE/CLOAD : pas de "Errors found", LIST correct
- [x] 470 tests pass, 0 régression
- [x] Référence : .pch d'Oricutron + désasm Atmos GetTapeByte (sénior)

### Sprint 34aq: Fix CSAVE TAP format byte-compatible (v1.16.43-alpha) - DONE
- [x] Diagnostic : hexdump TAP CSAVE vs AIGLE.TAP révèle off-by-one
- [x] Putbyte capture désactivée (byte-stream ROM non fiable)
- [x] Reconstruction TAP depuis RAM au csave_end :
  - [x] TXTTAB/VARTAB → start_addr / end_addr
  - [x] Lecture program data depuis RAM[start..end]
  - [x] Header layout canonique 2-padding (offset 4-5) + type + auto
  - [x] Filename sanitization stricte [A-Z0-9_-.] max 11
- [x] Round-trip CSAVE → NEW → CLOAD → LIST validé via --type-keys
- [x] 29 bytes TAP consommés par readbyte, PC=$C5E8 final BASIC OK
- [x] 470 tests pass, aucune régression

### Sprint 34ap: LOCI SDIMG write support FAT16/32 (v1.16.42-alpha) - DONE
- [x] API write : fopen_ex, fwrite, unlink, rename, mkdir, sync
- [x] write_sector avec EROFS si image RO hôte
- [x] write_fat_entry : update sur les NumFATs miroirs
- [x] alloc_free_cluster + free_cluster_chain + extend_chain
- [x] find_dir_entry + alloc_dir_entry (étend cluster dir si plein)
- [x] to_fat_83 normalisation strict (8.3, chars interdits)
- [x] update_dir_entry / write_new_entry / mark_entry_deleted
- [x] sdimg_handle_t : flag writable + dir_entry_lba/off
- [x] op_open route O_CREAT/O_TRUNC/RDWR vers fopen_ex(mode≥1)
- [x] op_write_xstack/xram routent vers loci_sdimg_fwrite
- [x] op_unlink/rename/mkdir routent vers SDIMG API
- [x] Fallback rb+ → rb si image RO côté hôte
- [x] tests/unit/test_loci_sdimg_write.c : 12 tests cold-roundtrip
- [x] Makefile test-loci-sdimg-write + ajout à la cible tests
- [x] 470 tests global pass (458 + 12), 0 régression

### Sprint 34ao+: LOCI SDIMG E2E intégration (v1.16.41-alpha) - DONE
- [x] Fix picker TUI vide : d_attrib normalisé (DIR=0x10 ou 0)
- [x] Fix OPENDIR path vide accepté (= racine)
- [x] Fix MIA_BOOT extrait ROM depuis SDIMG vers /tmp/loci_extract_*
- [x] Fix op_mount LOCI_MNT_TAP appelle loci_tape_mount_cb
- [x] loci_tape_mount_cb dans main.c : remplit emu.tapebuf + tape_loaded
- [x] Fix rom_patches re-détectés au ROM swap (BASIC 1.0 ↔ 1.1)
- [x] Tool tools/mkloci_sd.c : populator FAT16 standalone
- [x] VALIDATION INTERACTIVE : AIGLE.TAP chargé depuis loci_demo.img
- [x] CLOAD"" sous BASIC Atmos fonctionne avec tape extrait

### Sprint 34ao: LOCI backend image SD raw FAT16/32 (v1.16.40-alpha) - DONE
- [x] Nouveau module src/io/loci_sdimg.{c,h} (parseur FAT autonome ~470 LOC)
- [x] BPB parsing : auto-détection FAT16 vs FAT32 par cluster count
- [x] FAT chain walk pour seek arbitraire dans les fichiers
- [x] Lookup case-insensitive 8.3 (skip LFN slots et volume labels)
- [x] CLI --loci-sdimg PATH avec validation exclusion mutuelle vs --loci-flash
- [x] Champ void* sdimg opaque dans loci_t (pas de couplage d'header)
- [x] API loci_attach_sdimg / loci_detach_sdimg
- [x] 11 ops file/dir dispatchées vers backend SDIMG quand activé
- [x] 5 ops d'écriture rejetées EACCES (read-only v1)
- [x] sdimg_errno_to_loci mapping POSIX→LOCI errno
- [x] Tag sentinel dans fds[]/dirs[] pour éviter collisions POSIX
- [x] loci_cleanup conditionnel (skip fclose/closedir si sdimg actif)
- [x] tests/unit/test_loci_sdimg.c : 10 tests avec génération FAT16 inline
- [x] Makefile : src/io/loci_sdimg.c ajouté + cible test-loci-sdimg
- [x] 458 tests global pass (448 + 10 nouveaux), 0 régression

### Sprint 34an: LOCI fix critique MIA spin window ABI (v1.16.39-alpha) - DONE
- [x] Cause racine identifiée par ingé principal après review CR
- [x] api_install_blocked_stub + api_install_released_stub (Option B)
- [x] api_set_ax étendu : LDX# à $03B5, RTS à $03B7
- [x] api_return_ax/axsreg/errno : terminent par api_install_released_stub
- [x] Dispatch $03AF complet : 0x00 zxstack / 0xFF exit / autre
- [x] Default ENOSYS via api_return_errno (released auto)
- [x] seed_initial_stub à init/reset (released no-op A=X=SREG=0)
- [x] 3 tests DIFFÉRENTIELS 6502 (vrai cpu_step + memory I/O)
- [x] Makefile test-loci link cpu6502+memory+addressing+banking
- [x] test_init_zeroes_state mis à jour pour stub seedé
- [x] 108 tests test-loci, 448 tests global pass
- [x] LOCI ROM atteint while(1), spinner visible, clavier réactif
- [x] USER VALIDATION : ESC → boot fresh, navigation TUI complète,
      MIA_BOOT → ROM swap BASIC 1.1 confirmé en interactif
- [x] END-TO-END : premier émulateur grand public supportant LOCI

### Sprint 34am: LOCI fix clavier — pre-seed PSG R7=$7F (v1.16.38-alpha) - DONE
- [x] Cause racine identifiée par instrumentation (PCR/ay writes)
- [x] ROM LOCI ne fait jamais ay_write_data(7) — dépend du firmware Pi Pico
- [x] Fix : main.c init psg.registers[7]=$7F après loci_init
- [x] TUI LOCI répond aux flèches/ESC/Return
- [x] 445 tests global pass

### Sprint 34al: LOCI bridge souris SDL → mou_xram (v1.16.37-alpha) - DONE
- [x] loci_mou_report(buttons, dx, dy, wheel, pan)
- [x] Bridge SDL : MOUSEMOTION/BUTTONDOWN/UP/WHEEL
- [x] Cumul deltas matches firmware mou_xram_data
- [x] 4 nouveaux tests, total 105 test-loci, 445 global
- [x] NOTE : ROM LOCI v0.3.0 utilise matrice clavier Oric (PSG R14)
        et NON le HID — bridge HID utile pour programmes 6502 modernes

### Sprint 34ak: LOCI bridge SDL keyboard → HID (v1.16.36-alpha) - DONE
- [x] loci_sync_kbd_from_sdl(emu) dans main.c (#ifdef HAS_SDL2)
- [x] Lecture SDL_GetKeyboardState + SDL_GetModState
- [x] Mapping 1:1 SDL_SCANCODE_* ↔ HID usage code (design SDL)
- [x] Modifier byte (LCtrl/LShift/LAlt/.../RCtrl/RShift/RAlt/...)
- [x] Boot keyboard 6-key limit respecté
- [x] Hook après KEYDOWN et KEYUP : mirror complet
- [x] 5 nouveaux tests bitmap conventions (A/Enter/Up/6-keys/release)
- [x] 101 tests test-loci, 441 global
- [x] BOUCLE INTERACTIVE COMPLÈTE : ROM LOCI navigable au clavier

### Sprint 34aj: LOCI bindings clavier SDL F5/F8 (v1.16.35-alpha) - DONE
- [x] F8 KEYDOWN → loci_action_button_short (install trap + IRQ)
- [x] F8 KEYUP → loci_action_button_release (V flag + restore vector)
- [x] F5 étendu : si --loci, appelle aussi loci_reset après cpu_reset
- [x] Préservation des mounts vérifiée par test dédié
- [x] Help text mis à jour
- [x] 1 nouveau test (96 total)

### Sprint 34ai: LOCI action button warm IRQ trap (v1.16.34-alpha) - DONE
- [x] loci_action_button_short : installe trap 6 octets MIA $03BA-$03BF
- [x] loci_action_button_release : release + restore vecteur
- [x] loci_set_action_callbacks : enregistre hooks host
- [x] Côté main.c : install_cb patche mem->rom[$3FFE/F] + cpu_irq_set
- [x] release_cb : cpu.P |= FLAG_OVERFLOW + cpu_irq_clear + restore vector
- [x] action_active + saved_irq_vector dans loci_t
- [x] Idempotence : 2nd short while active = noop, release inactive = noop
- [x] 8 nouveaux tests test-loci, total 95, 435 tests global
- [x] Bouton Reset/Update : design documenté (réponse précédente),
      pas implémentés sur cette itération

### Sprint 34ah: LOCI tests intégration scénarios bootés (v1.16.33-alpha) - DONE
- [x] 7 scénarios d'intégration composés (test_integration_*) :
      mount session 4 slots, dir enum 3 files, TAP multi-header A/B/C,
      MIA_BOOT mounted ROM wins, HID Shift+A bitmap, locirom binary
      sanity 16KB + reset vector, DSK mount + bus probe CTRL switch
- [x] E2E boot validé : ROM LOCI reach $C354 en 500k cycles, aucun crash
- [x] 87 tests test-loci total (+7 du sprint)
- [x] **Série Sprint LOCI complète** : 34y → 34ah (9 sprints, 27/36 ops)

### Sprint 34ae: LOCI DSK multi-drive WD179x stub (v1.16.32-alpha) - DONE
- [x] dsk_fp[4] : 4 drives indépendants
- [x] Auto-open sur MOUNT drive 0-3
- [x] Auto-close sur UMOUNT
- [x] Bus registres $0310-$0314 + $0318 stub
- [x] CMD/STAT reporte NOT_READY si drive non monté
- [x] CTRL decode drive select bits 5-6
- [x] TRACK/SECT/DATA passthrough latch
- [x] Routing prioritise Microdisc si présent, sinon LOCI DSK
- [x] 8 nouveaux tests, total 80, 420 tests global
- [x] WD1793 cycle-accurate deferred (sprint séparé si besoin)

### Sprint 34af: LOCI TAP cassette + ops 0x92-94 (v1.16.31-alpha) - DONE
- [x] Backend tap_fp/size/counter dans loci_t
- [x] Auto-mount slot 4 (LOCI_MNT_TAP) → tap_open
- [x] op 0x92 TAP_SEEK : AXSREG pos clamp size-1, fseek
- [x] op 0x93 TAP_TELL : retourne counter
- [x] op 0x94 TAP_READ_HEADER : scan sync 16 16 16 24, lit header 16 octets
- [x] Sanitise filename : non-printable → '?'
- [x] Bus $0315-$0317 stub : CMD/STAT/DATA
- [x] CMD REW reset counter
- [x] STAT reporte NOT_READY si pas de tape
- [x] 10 nouveaux tests test-loci

### Sprint 35a: IPC control mode OricForge (v1.16.72-73-alpha) - DONE
Référence : `docs/control_protocol.md`
- [x] CLI `--control` (implique `--debug` + `--headless`)
- [x] Routage logs vers stderr (`log_set_stream(FILE*)`)
- [x] Module `src/control.c` + `include/control.h`
- [x] 16 commandes : `hello`, `regs`, `set`, `read`, `write`, `peek`, `break`, `unbreak`, `break-list`, `step`, `next`, `step-out`, `continue`, `pause`, `reset`, `quit`
- [x] 2 events : `EVT ready`, `EVT stopped` (`reason=break/step/user/watch`)
- [x] Async pause-while-running (poll stdin par frame, `ERR busy` pour cmds non-{pause,quit})
- [x] Hello/version handshake avec `caps=` extensible
- [x] Scenarios E2E IPC : sync handshake + async pause (7/7 scenarios E2E PASS)

### Sprint 35b: OricForge backlog (v1.16.74-alpha) - DONE
- [x] `watch <addr>` / `unwatch` / `watch-list` + `EVT stopped reason=watch`
- [x] `raster <line>` / `unraster` + `EVT stopped reason=raster`
- [x] `load-tap` / `load-rom` / `load-sym`
- [x] `disasm <addr> <n>` (1..64 instructions, parseable)
- [x] `EVT halt reason=jam/cycle_limit` (back-to-ROM laissé au break user-side)
- [x] `debugger_t.last_break_reason[]` pour précision `EVT stopped reason=…`
- [x] Caps updated : `step-out,peek,hello,async-pause,watch,raster,load-tap,load-rom,load-sym,disasm`
- [ ] `bread <addr> <len>` (framing binaire) — reporté à 35c selon scope
- [ ] Client de référence Python (livré par OricForge en parallèle)

### Sprint 36e: Conformité API LOCI vs firmware officiel (v1.16.81-alpha) - DONE
- [x] Audit complet de l'émulation LOCI contre les sources `sodiumlb/loci-firmware` (registres I/O, conventions API, 35 opcodes)
- [x] Fix `lseek` op $1A : whence int8 au sommet de la xstack + offset int32 short-stack 0-4 octets signe-étendu (`api_pop_int32_end`) ; mapping firmware 0=SEEK_CUR / 1=SEEK_END / 2=SEEK_SET (les deux étaient inversés) ; helper partagé `pop_lseek_args()` POSIX+SDIMG
- [x] Fix `phi2` op $02 : retourne 1000 kHz dans AX (convention `cpu_api_phi2`) au lieu de 1 000 000 Hz en AXSREG (débordait AX)
- [x] Fix `LOCI_XSTACK_SIZE` 256 → 512 (= firmware `XSTACK_SIZE 0x200`, `mem.h`) ; `read_xstack` accepte désormais count ≤ 512
- [x] 4 nouveaux tests (whence CUR/END + short-stack 0 et 1 octet signe-étendu, capacité xstack 512, read_xstack > 256) ; tests lseek/phi2 existants réalignés sur la convention firmware
- [x] Backlog 36f (divergences moyennes documentées dans l'audit) : console UART $03A0-$03A3 (bit7 TX-ready minimal), protocole TAP bas niveau $0315/$0317 (PLAY/REC/READ_BIT/FFW), ACIA LOCI $0380-$0383, op $A6 `adj_scan`, $031A-$031B, bits bas $0314/$0318 → livré en Sprint 36f (v1.16.82-alpha)

### Sprint 36f: Backlog conformité LOCI — console UART, TAP bas niveau, ACIA $0380 (v1.16.82-alpha) - DONE
- [x] Console UART $03A0-$03A2 : flags TX-ready/RX-ready, TX bufferisé ligne → log, file RX 64 octets (`loci_cons_inject`) ; $03A3 snoop ULA (write clear)
- [x] Protocole TAP bas niveau $0315-$0317 : PLAY / REC / READ_BIT (trame 14 bits `tap_encode_byte` : start 0, 8 data LSB-first, parité impaire, stops) / FFW ; lead-in 8×0x16 ; EOF → 0x00
- [x] Protection moteur firmware-conforme : PLAY/REC/READ_BIT latchés moteur coupé, exécutés au démarrage moteur (snoop VIA ORB $0300 bit 6 → `loci_tap_motor`)
- [x] Montage TAP r+b avec repli lecture seule + WPROT dans $0316
- [x] Op $A6 `adj_scan` : ack AX=0, xram[$FFF0] = scan terminé
- [x] $031A-$031B dans la fenêtre DSK (stockage simple, init 0)
- [x] $0314/$0318 : octets exacts 0x80/0x00 (suppression du padding `|0x7F`)
- [x] ACIA par défaut à $0380-$0383 sous `--loci` + `--serial` (override `--acia-addr`)
- [x] Fix build headless : `_POSIX_C_SOURCE 200809L` pour `clock_gettime` (régression sprint 36a, visible uniquement sans SDL2=1)
- [x] 11 nouveaux tests unitaires (150 au total sur test-loci)
- Reste hors scope (documenté) : émulation PIO du pattern matcher ULA $03A3 (write-only chez nous), BUSY observable (handlers synchrones), LittleFS "0:" distinct

### Sprint 36d: LOCI identity marker register $0319 (v1.16.80-alpha) - DONE
- [x] Found while building Phosphoric-based test harness for locifilemanager-v2 (LOCI Oric Atmos client)
- [x] Root cause : client `loci_present()` checks `peek($0319) == 'L'`, a real-hardware convention (`loci-firmware/mia.c`: `IOREGS(0x0319)='L'` set once at init, writes disabled) — Phosphoric's LOCI DSK range only covered $0310-$0314 + $0318
- [x] New `LOCI_DSK_IO_ID` ($0319) : returns `'L'` (0x4C) when LOCI enabled, `0xFF` otherwise ; writes are no-op
- [x] 2 new unit tests (`test_dsk_id_register_returns_loci_marker`, `test_dsk_id_register_ff_when_disabled`)

### Sprint 36c: Fix rechain parasite CLOAD code machine (v1.16.79-alpha) - DONE
- [x] Diagnostic forum « asteroids.tap no autorun » : reproduit via chemin ROM réel (auto-CLOAD sans `-f`)
- [x] Root cause : garde rechain lisait le type en `$66` ; ROM 1.0 stocke l'en-tête inversé → type en `$64` (Atmos : `$02AE`)
- [x] Nouveau champ `rom_patches_t.tape_type_addr` (ORIC-1 `$0064` / Atmos `$02AE`)
- [x] Vérifié : ROM 1.0 autorun MC = `$E7AA` → `JMP ($005F)` ; Asteroids boote écran titre via CLOAD réel
- [x] `tests/integration/test_mc_autorun_rechain.sh` + `make test-mc-autorun` (4 scenarios, dont cas témoin rechain BASIC/TYRANN)

### Sprint 36b: Game compatibility regression suite (v1.16.77-alpha) - DONE
- [x] `tests/integration/test_game_compat.sh` — boote 7 titres Oric commerciaux depuis OricProgramsLib et vérifie signature distinctive d'intro
- [x] Cible `make test-game-compat`
- [x] 15/15 PASS (étendu de 7 → 15 titres en v1.16.78-alpha) : manic-miner, hopper, breakout, harrier-attack, atlantis, oric-trek, tyrann, zaxxon, kong, cobra, exocet, alien-attack, colditz, mr-wimpy, chess-ii
- [x] Skip gracieux si `ORIC_PROGRAMS_LIB` absent
- [x] Item dormant phase 4 « Game compatibility testing » coché

### Sprint 36a: Performance benchmark harness (v1.16.76-alpha) - DONE
- [x] CLI flag `--bench` : ligne `BENCH cycles=… mhz_eq=… speed_ratio=…x frame_us=…` à l'exit
- [x] `tools/bench.sh` : 4 scenarios (basic-idle, sedoric-native, sedoric-loci, tape-007-fastload)
- [x] Cible `make bench` (variantes `BENCH_TSV=1`, `CYCLES=…`)
- [x] Baseline mesurée : ~99.5x real-time sur basic-idle, ~81-93x sur charges réelles
- [x] Overhead historique <5% confirmé : ~1% sur basic-idle (201 µs / 20 ms frame budget)
- [x] Scenario E2E `36a --bench produces parseable BENCH line` (13/13 E2E PASS)

### Sprint 35c: Hardening + intégration OricForge (v1.16.75-alpha) - DONE
- [x] `bread <addr> <len>` framing binaire length-prefixed (1..65536 octets)
- [x] Client Python `tests/integration/phos_smoke_client.py` (~250 LOC)
- [x] Scenario E2E `35c Python smoke client (handshake + bread)`
- [x] Hardening : SIGPIPE ignoré + ferror(stdout) check pour shutdown propre
- [x] Doc protocole : parsing hex (piège decimal vs hex sur les lengths)
- [ ] Framing strict (numéros de séquence, ACK) — pas demandé par OricForge, différé tant que pas besoin
- [ ] Server-side timeouts — pas demandé, le client gère ses timeouts

### Sprint 34d: Audit gaps debug post-LOCI — backlog (v1.16.67-71) - DONE
Référence : `docs/CR/2026-06-07_Debug_Gaps_Audit_Post_LOCI.md`
- [x] Fix bug parser symboles Format A préfixe `0x` non comptabilisé (`symbols.c:hex_parse`) — livré v1.16.67-alpha
- [x] P0-A : résolution des symboles sur opérandes du disasm (`debugger.c:append_operand_symbols`) — livré v1.16.67-alpha
- [x] P0-B : commande REPL `loci` (snapshot complet MIA : ops, xstack, xram, fds, mounts, TAP/DSK) — livré v1.16.69-alpha
- [x] P1-C : commande REPL `disk`/`fdc` (Microdisc + WD1793 + 4 drives) — livré v1.16.68-alpha
- [x] P1-D : commande REPL `acia`/`serial` (6551 + signaux + FIFO + backend) — livré v1.16.68-alpha
- [x] P1-E : commande REPL `tape`/`cassette` (position, status, preview bytes) — livré v1.16.68-alpha
- [x] P2-F : rewind / single-step inverse (`u`/`undo`, ring 16 snapshots CPU+RAM) — livré v1.16.71-alpha
- [x] P2-G : raster-line breakpoint (`br N`, `brd N`/`brd *`) — livré v1.16.70-alpha

### Sprint 34c: Backlog issu de la senior review post-34b5 - DONE
Référence : `docs/CR/2026-06-07_Senior_LOCI_Pipe_Review_Post_34b5.md`
- [x] R1 — Fix `op_readdir` POSIX (mauvais base path pour `stat`) — `loci.c:1549-1551`, livré v1.16.62-alpha
- [x] R2 — Durcir `resolve_path` (tokeniser `/`, rejeter `..` exact, plus de `strstr`) — `loci.c:702-718`, livré v1.16.62-alpha
- [x] R3 — Persister écritures DSK en mode raw (helper `dsk_flush`, no-op si MFM_DISK détecté) — livré v1.16.63-alpha
- [x] R4 — Split `loci.c` (2380 LOC) en 4 TUs : `loci_core` (666) / `loci_fs` (904) / `loci_bus` (342) / `loci_boot` (104) + `loci_internal.h` (129) — livré v1.16.66-alpha
- [x] R5 — Tests chemins d'erreur API ops (8 nouveaux EBADF/EINVAL/ENOENT/EACCES) + fix bug `op_rename` découvert au passage — livré v1.16.64-alpha
- [x] Hors R1-R5 : `op_uname` release tracke `EMU_VERSION` — livré v1.16.65-alpha
- [x] Hors R1-R5 : `loci_overlay_buf` déporté vers `emulator_t`, free dans `emulator_cleanup` — livré v1.16.65-alpha

### Sprint 34b5: E2E pipe TAP LOCI — 007 jeu commercial (v1.16.60-alpha) - DONE
- [x] Étend la régression au pipe TAP+ROM-swap LOCI (différent du pipe DSK 34b0-b4)
- [x] 007 « Dangereusement Vôtre » (DOMARK 1985) chargé via TUI LOCI : `t/SPACE/SPACE/ESC` + `CLOAD""` auto-run
- [x] Écran intro 100 % identique (0/1120 byte diff) vs boot natif fast-load (`-t 007.tap -f`)
- [x] Scenario `34b5` ajouté au script E2E
- [x] 5/5 scenarios E2E PASS ; 484 tests unitaires PASS

### Sprint 34b4: RUN after LOAD via LOCI (exec round-trip) (v1.16.59-alpha) - DONE
- [x] `RUN` après LOAD via LOCI exécute le programme rechargé : écran « HELLO LOCI » affiché
- [x] Test différentiel native vs LOCI : écran post-RUN 100 % identique (0/1120 byte diff)
- [x] Scenario `34b2b — RUN after LOAD` ajouté au script E2E
- [x] Cleanup WIP roadmap obsolète (34ay résolu en 34az)
- [x] 4/4 scenarios E2E PASS ; 484 tests unitaires PASS

### Sprint 34b3: Régression E2E LOCI/Sedoric — script + cible Makefile (v1.16.58-alpha) - DONE
- [x] `tests/integration/test_loci_sedoric_e2e.sh` : rejoue 34b0/b1/b2, compare native vs LOCI
- [x] Skip gracieux si ROMs/images absentes (assets non versionnés)
- [x] Cible `make test-loci-e2e` opt-in (pas dans `tests` standard : assets externes + ~3 min)
- [x] 3/3 scenarios E2E PASS sur la machine de dev
- [x] 484 tests unitaires PASS (suite standard inchangée)

### Sprint 34b2: Sedoric SAVE/NEW/LOAD round-trip via LOCI (v1.16.57-alpha) - DONE
- [x] `SAVE"TEST.BAS"` → Sedoric écrit programme + entrée directory via WD1793 Write Sector LOCI
- [x] `NEW` puis `LOAD"TEST.BAS"` → programme rechargé depuis disque virtuel
- [x] Test différentiel native vs LOCI : BASIC text `$0500-$053F` byte-identique
- [x] Premier test write WD1793 LOCI (jusqu'ici seules les lectures étaient validées)
- [x] 484 tests PASS (aucune modif code source)

### Sprint 34b1: Sedoric DIR + LOAD"MENU.COM" via LOCI (v1.16.56-alpha) - DONE
- [x] BASIC + `DIR` : catalogue Sedoric affiché (12 entrées, 94 fichiers, 1447 secteurs libres)
- [x] `LOAD"MENU.COM"` : programme machine Sedoric chargé + exécuté → menu anglais affiché
- [x] Test différentiel native vs LOCI : DIR screen 100 % identique, LOAD screen 100 % identique
- [x] Validation : pipe LOCI WD1793 lit secteurs sur tracks distinctes (boot t0 → directory t20 → MENU.COM)
- [x] 484 tests PASS (aucune modif code source)

### Sprint 34b0: Interaction Sedoric V4.0 via LOCI — DOS VERSION (v1.16.55-alpha) - DONE
- [x] Frappe `6` (option « Version du DOS ») captée par Sedoric via pipe LOCI
- [x] Frappe `A` (drive A) déclenche lecture secteur version sur WD1793 LOCI
- [x] Écran « SEDORIC V4.000 01/01/16 » + crédits + « Another disc? (Y/N) »
- [x] Test différentiel native vs LOCI : écran texte 100 % identique (0/1120 byte diff)
- [x] Validation : pipe LOCI clavier → Sedoric → FDC fonctionne en lecture continue post-boot
- [x] Hygiène : `.gitignore` étendu (binaires tests récents, *.d, tools/, tapes/, loci_demo.img)
- [x] 484 tests PASS (aucune modif code source)

### Sprint 34az: ROMDIS dynamique — boot Sedoric COMPLET (v1.16.54-alpha) - DONE
- [x] Bug identifié via senior review : `basic_rom_disabled` hardcodé true
- [x] Fix : `romdis = (CTRL & 0x02) == 0` dynamique, miroir microdisc natif
- [x] E2E : menu SEDORIC DOS V4.0 affiché complet (10 options), identique au boot natif Microdisc
- [x] Test différentiel : boot natif + boot LOCI → même écran au cycle 35M
- [x] 484 tests PASS

### Sprint 34ay: WD1793 INTRQ asynchrone — boot Sedoric stage 1 (v1.16.53-alpha) - DONE
- [x] `loci_fdc_set_intrq` → cpu_irq_set(IRQF_DISK) si INTENA actif
- [x] `loci_fdc_clr_intrq` → cpu_irq_clr(IRQF_DISK) symétrique
- [x] E2E : « Booting. » sur l'écran, CPU exécute en RAM PC=$04F7
- [x] 484 tests PASS (118 LOCI + 366 core)
- [x] WIP résolu en 34az : ROMDIS dynamique manquant (pas Read Multiple / DRQ pacing), boot complet validé + interactions DIR/LOAD/SAVE/RUN en 34b0-34b4

### Sprint 34ad: LOCI ROM swap runtime MIA_BOOT (v1.16.30-alpha) - DONE
- [x] Op 0xA0 MIA_BOOT décodé : A=settings, dispatcher
- [x] Constants LOCI_BOOT_FDC/TAP/B11/TAP_BIT/TAP_ALD/RESUME/FAST
- [x] Mécanisme callback : loci_set_rom_swap_callback(cb, ctx)
- [x] Résolution path : slot ROM monté > B11 → basic11b.rom > basic10.rom
- [x] Sandbox via flash_root resolver
- [x] Côté main.c : loci_rom_swap_cb avec memory_load_rom + cpu_reset
- [x] Support base_addr=$C000 uniquement en 34ad (BASIC)
- [x] base_addr=$A000 ack mais log "delegated to --disk-rom"
- [x] RESUME bit → ack sans callback
- [x] No callback → ack OK (test mode)
- [x] Callback failure → EIO
- [x] boot_settings latched pour introspection
- [x] 8 nouveaux tests test-loci, total 62, 402 tests global
- [x] 24/36 ops implémentées (67%)

### Sprint 34ag: LOCI USB HID stubs kbd/mou/pad (v1.16.29-alpha) - DONE
- [x] PIX_XREG (0x01) décodé : device/channel/addr/word depuis xstack
- [x] Route channel*256+addr : 0=kbd, 1=mou, 2=pad
- [x] kbd_xram/mou_xram/pad_xram init à 0xFFFF (sentinel firmware)
- [x] API bridge loci_kbd_set_report(modifier, keycodes[6])
- [x] API bridge loci_kbd_clear()
- [x] Bitmap firmware-truthful : bit (kc&7) du byte (kc>>3)
- [x] Modifier byte 28 (HID_KEY_CONTROL_LEFT >> 3)
- [x] No-key sentinel byte 0 bit 0
- [x] 10 nouveaux tests test-loci, total 54, 394 tests global
- [x] Sprint sauté hors ordre (34ag avant 34ad-34af) pour valider HID
- [x] Bridge SDL → loci_kbd_set_report : différé (besoin scénario end-to-end)

### Sprint 34ac: LOCI Dir API + uname (v1.16.28-alpha) - DONE
- [x] op 0x80 OPENDIR : opendir host, retourne dir_fd >= 32
- [x] op 0x81 CLOSEDIR : closedir, libère slot
- [x] op 0x82 READDIR : push dirent 72 octets (d_fd/d_name/d_attrib/d_size)
- [x] op 0x83 MKDIR : mkdir mode 0755
- [x] op 0x98 UNAME : 5 chaînes (sysname/nodename/release/version/machine)
- [x] Table dir_fd 8 slots (offset 32, matches firmware)
- [x] Filtrage automatique des entrées `.` et `..`
- [x] Stat host pour d_attrib (AM_DIR si dossier) et d_size
- [x] 6 nouveaux tests test-loci, total 44, 384 tests global
- [x] 23/36 ops implémentées (64%)
- [x] ROM LOCI reste PC=$C354 (TUI loop, attente input)

### Sprint 34ab: LOCI xram DMA + mount/umount/getcwd (v1.16.27-alpha) - DONE
- [x] xram 64 KB tampon partagé loci_t.xram
- [x] Fenêtre DMA 0 : $03A4 RW0, $03A5 STEP0 (signed), $03A6/A7 ADDR0
- [x] Fenêtre DMA 1 : $03A8 RW1, $03A9 STEP1, $03AA/AB ADDR1
- [x] Auto-incrément read/write avec step signé (positif/négatif)
- [x] op 0x17 READ_XRAM : pop (count, xram_addr), fread → xram
- [x] op 0x19 WRITE_XRAM : pop args, fwrite ← xram
- [x] op 0x90 MOUNT : drive 0-5, valide existence fichier, store path
- [x] op 0x91 UMOUNT : clear slot
- [x] op 0x88 GETCWD : returns path du slot, A=255 → derive (ROM > DSK0 > TAP)
- [x] Mount table 6 slots (LOCI_MNT_TAP=4, LOCI_MNT_ROM=5)
- [x] 11 nouveaux tests test-loci, total 38, 378 tests global
- [x] Mount image disque/tape/ROM différé (path tracking only)
- [x] ROM LOCI atteint PC=$C354 — boucle TUI active

### Sprint 34aa: LOCI File I/O POSIX subset (v1.16.26-alpha) - DONE
- [x] op 0x14 OPEN : fopen + flags POSIX, retourne fd ≥ 3
- [x] op 0x15 CLOSE : fclose, libère slot fd
- [x] op 0x16 READ_XSTACK : fread, push résultat sur xstack
- [x] op 0x18 WRITE_XSTACK : fwrite depuis xstack
- [x] op 0x1A LSEEK : SET/CUR/END, retourne pos en AXSREG
- [x] op 0x1B UNLINK : unlink host fs
- [x] op 0x1C RENAME : rename host fs
- [x] Table fd 16 slots avec offset 3 (stdin/out/err réservés)
- [x] Sandbox path : flash_root, traversal .. → EACCES, préfixe X: strippé
- [x] CLI --loci-flash DIR (implique --loci)
- [x] Mapping flags O_RDWR/CREAT/TRUNC/APPEND/EXCL → fopen mode
- [x] Mapping errno host → LOCI errno
- [x] Constants LOCI_O_* exposées dans header
- [x] 8 nouveaux tests, total 27 test-loci, 367 tests global
- [x] READ_XRAM/WRITE_XRAM différés sprint 34ab
- [x] ROM LOCI progresse PC $F641 → $EB18

### Sprint 34z: LOCI ops système + RTC + RNG (v1.16.25-alpha) - DONE
- [x] Helpers api_return_ax/axsreg/errno (semantics firmware)
- [x] xstack push_n/push_u32/push_i32 + sync + zero
- [x] Pop sémantique de $03AC (read auto-pop, sprint 34y stub corrigé)
- [x] op 0x01 PIX_XREG : stub silencieux (USB HID en 34ag)
- [x] op 0x04 RNG_LRAND : random 31-bit positif dans AXSREG
- [x] op 0x0F CLOCK : uptime us_64/10000
- [x] op 0x10 CLK_GETRES : push (sec=1, nsec=0), ax=0
- [x] op 0x11 CLK_GETTIME : push (host_time, nsec=0), ax=0
- [x] op 0x12 CLK_SETTIME : pop 8 octets, ax=0 (no host clock change)
- [x] EINVAL si clock_id != CLOCK_REALTIME (CLK_ID 0)
- [x] 8 nouveaux tests test-loci, total 19 ; 359 tests total
- [x] Validation : ROM LOCI progresse PC $F2C3 → $F641

### Sprint 34y: LOCI skeleton + dispatcher (v1.16.24-alpha) - DONE
- [x] Module src/io/loci.c + include/io/loci.h
- [x] Décodeur bus MIA $03A0-$03BF (register file 32 octets, R/W)
- [x] Dispatcher 36 API ops (enum complet, op_name pour diag)
- [x] Tous ops stubés ENOSYS (LOCI_ENOSYS = 13)
- [x] xstack 256 octets avec push/pop via $03AC
- [x] CLI --loci active le périph
- [x] Hooks io_read_callback / io_write_callback (prioritaire avant ACIA/VIA)
- [x] ROM LOCI v0.3.0 téléchargée roms/loci/locirom (16KB, reset $FF29)
- [x] Boot ROM LOCI confirmé fonctionnel
- [x] 11 tests test-loci ; total 351 tests pass
- [x] Premier sprint série 34y→34ah pour LOCI complet

### Roadmap LOCI (sprint backlog restant — DERIVÉ DU FIRMWARE)

- **34z** : xstack window auto-inc + ops 0x01/04/0F-12 (système, RTC, RNG) ~250 LOC
- **34aa** : File I/O ops 0x14-1C avec host fs (POSIX-like) ~400 LOC
- **34ab** : mount/umount 0x90-91 (mount points host) ~200 LOC
- **34ac** : Dir API 0x80-83/88 + uname 0x98 ~250 LOC
- **34ad** : ROM swap runtime via API call + --loci-rom FILE ~150 LOC
- **34ae** : Multi-drive WD179x extension $0310-$0318 ~300 LOC
- **34af** : TAP $0315-$0317 + ops 0x92-94 ~200 LOC
- **34ag** : USB HID stubs (kbd/mou/pad) PIX XREG channels 0x000-2 ~250 LOC
- **34ah** : Tests intégration ROM LOCI réelle, scénarios bootés

### Sprint 34x: Cycle counts dans le disasm (v1.16.23-alpha) - DONE
- [x] cpu_opcode_cycles(opcode) : nouvelle API publique cpu6502.h
- [x] REPL show_disassembly : "  ; N cyc" après chaque instruction
- [x] TUI draw_disasm : "[N] ; SYMBOL" compact (largeur fenêtre)
- [x] Sprint déclenché par axe "tuning fin / démos cycle-locked"
- [x] 340 tests pass

### Sprint 34w: TUI ncurses multi-panes optionnel (v1.16.22-alpha) - DONE
- [x] Build flag TUI=1 (linke -lncursesw, sinon stubs static inline pour compat)
- [x] Module src/tui.c + include/tui.h
- [x] CLI --tui (force --debug, requiert build TUI=1)
- [x] Layout 6 panes :
      - Registres + Stack (haut, côte à côte)
      - Disassembly (centre, PC vert+gras, breakpoints magenta)
      - Memory + Breakpoints/Watchpoints (bas, côte à côte)
      - Status line (bas avec raccourcis)
- [x] Symboles annotés en jaune+gras dans le disasm
- [x] Headers de panes en cyan+gras
- [x] Raccourcis : s/n/c/q + PgUp/PgDn (mem scroll) + g (goto mem) + : (cmd)
- [x] Step-over JSR géré comme dans le REPL (temp breakpoint à PC+3)
- [x] ':' suspend ncurses, exécute la ligne via debugger_repl_run_line,
      retour TUI au prochain key press
- [x] Refactor debugger_repl : process_repl_line interne extrait, exposé
      via wrapper debugger_repl_run_line (sans changement de comportement)
- [x] emulator_t.tui_mode flag ; main loop route vers tui_repl ou
      debugger_repl selon ce drapeau
- [x] tui_init/tui_cleanup gérés dans emulator_init/cleanup
- [x] Sprint déclenché par gap n°5 du comparatif debug vs Oricutron
- [x] DERNIER gap du comparatif fermé — n°1..5 tous DONE
- [x] 340 tests pass

### Sprint 34v: Édition mémoire live REPL (v1.16.21-alpha) - DONE
- [x] Syntaxe `m ADDR = V1 [V2 V3 ...]` : écrit séquence d'octets
- [x] ADDR via parse_addr (hex ou symbole)
- [x] Tokens : hex ($/0x/a-f implicite) ou décimal, sep espace ou ,
- [x] Validation 0..255 par octet, max 256 par appel
- [x] Confirmation "Wrote N bytes to $XXXX-$YYYY"
- [x] Rétro-compatible : sans = la commande reste un dump
- [x] Sprint déclenché par gap n°4 du comparatif debug vs Oricutron
- [x] 340 tests pass

### Sprint 34u: Breakpoints conditionnels (v1.16.20-alpha) - DONE
- [x] Syntaxe `b ADDR if EXPR` — EXPR évaluée à l'arrivée du PC
- [x] Opérandes : A, X, Y, SP, P, PC, M[ADDR] (ADDR hex ou symbole)
- [x] Opérateurs : == != < <= > >=
- [x] RHS : hex ($XX, 0xXX, A-F implicite), décimal, ou symbole
- [x] Listing b : condition affichée à droite (cond_text 48 chars)
- [x] Struct breakpoint_t remplace uint16_t[] (addr+has_cond+cond+text)
- [x] debugger_check_pc : nouvelle fonction interne qui évalue
- [x] debugger_is_breakpoint reste pré-filtre par adresse (compat)
- [x] Sprint déclenché par gap n°3 du comparatif debug vs Oricutron
- [x] 340 tests pass

### Sprint 34t: Disassembleur paginé avec historique (v1.16.19-alpha) - DONE
- [x] Curseur persistant : `d` sans argument continue depuis la fin
      de la dernière page (reset à chaque break)
- [x] `d +` (next) et `d -` (previous via historique ring 16 entrées)
- [x] `d ADDR [n]` saute et pousse la page courante dans l'historique
- [x] Taille de page persistante : `d ADDR N` mémorise N
- [x] show_disassembly retourne l'adresse de fin de page
- [x] Sprint déclenché par gap n°2 du comparatif debug vs Oricutron
- [x] 340 tests pass

### Sprint 34s: Symboles debugger — chargement .sym/.lab/.sym65 (v1.16.18-alpha) - DONE
- [x] Nouveau module utils/symbols (.h + .c)
- [x] 4 formats auto-détectés :
      Oricutron addr-first ($XXXX NAME / XXXX NAME),
      EQU (NAME = $XXXX / NAME EQU $XXXX),
      VICE / xa65 .lab (al C XXXX .NAME)
- [x] Commentaires ; et #, séparateurs , : =, leading . ou _ strippé
- [x] CLI --symbols FILE (multiple loads accumulés)
- [x] REPL : b NAME, w NAME, d NAME, m NAME résolvent via symboles
- [x] Nouvelle commande sym [NAME|ADDR] : liste/résout symboles
- [x] PC display dans regs : ajout <SYMBOL> si match
- [x] Disasm : label affiché sur sa propre ligne avant l'instruction
- [x] Listes b/w : symboles affichés à côté des adresses
- [x] Fix régression hex glouton : "CLOAD_END = $E502" était parsé
      comme adresse $C avec nom "LOAD_END" (parser hex acceptait préfixe C)
- [x] 10 nouveaux tests test-symbols (formats, mixed, case-insensitive,
      régression bug, fichier manquant, accumulation)
- [x] Sprint déclenché par gap #1 du comparatif debug Phosphoric vs Oricutron
- [x] 340 tests pass

### Sprint 34r: Attributs texte ULA — double hauteur / alt charset / blink (v1.16.17-alpha) - DONE
- [x] decode_attr case 0x08 décode bit 0 (alt) / bit 1 (double) / bit 2 (blink)
      (auparavant break muet → groupe 0x08-0x0F totalement ignoré)
- [x] Double hauteur : dbl_phase top/bottom, suivie en fin de rangée (chline==7).
      Phase pour rangée suivante ; reset à 0 si DBL absent (chaîne brisée).
- [x] Alt charset : base $B800 (texte) / $9C00 (HIRES) au lieu de $B400/$9800.
      Path test/headless utilise 128 chars suivants du buffer.
- [x] Blink : XOR de char_inv si bit blink actif et frame_counter & 0x10 (~3 Hz).
- [x] text_attr reset par scanline (sémantique ULA correcte).
- [x] frame_counter incrémenté à y==0.
- [x] Fix concret : titre Asteroids d'Artic ("ARTIC METEORS") lisible
      (auparavant "ABTF METEABS" — moitié basse pointait sur moitié haute).
- [x] 330 tests pass (24/24 cpu, 11/11 video, etc.)

### Sprint 34q: CI sécurité cppcheck + flawfinder + 3 bugs corrigés (v1.16.16-alpha) - DONE
- [x] Ajout make cppcheck (warning,performance,portability ; exclusion third_party/)
- [x] Ajout make flawfinder (minlevel=2, error-level=5)
- [x] Ajout make security-check (pipeline cppcheck + flawfinder)
- [x] Fix off-by-one rechainement BASIC : ram[ptr+1] hors bornes quand ptr=0xBFFF
- [x] Fix double buffer overflow --dump-ram-at : fwrite(ram,0x10000) → sizeof(ram)
- [x] 330 tests pass

### Sprint 34p: Fix hardware-accurate VIA Port A — phantom '7' (v1.16.15-alpha) - DONE
- [x] Investigation : comparaison Oricutron vs Phosphoric (modèle push vs pull PSG→VIA)
- [x] Cause racine : psg_porta_read() appelé sur toute lecture Port A, selected_reg ≠ 14
      → ay_read_data() retourne registre erroné (0x00) → bit0=0 même avec DDRA=$00
- [x] Fix : VIA_ORA / VIA_ORA_NH lisent via->ira (mis à jour par psg_decode en READ mode)
- [x] Suppression psg_porta_read() — callback inutile et source du bug
- [x] Fix type_keys \n\n : gap d'un frame entre deux RETURN consécutifs
- [x] 3 nouveaux tests test_keyboard (SHIFT LOCK, double RETURN, LSHIFT seul)
- [x] Mise à jour test_io.c : test_port_a_read / test_ora_no_handshake → via.ira
- [x] Confirmé : phantom absent sur programme utilisateur
- [x] 330 tests pass

### Sprint 34o: Outillage debug IRQ (--trace-irq, --dump-ram-at) (v1.16.14-alpha) - DONE
- [x] Demande formulée par équipe asteroids (debug IRQ T1 user vector)
- [x] --trace-irq FILE : log chaque IRQ entrée + RTI avec contexte complet
      (cycle, PC_pre, target $FFFE/F, IFR snapshot, IER snapshot, srcmask)
- [x] --dump-ram-at CYCLES:FILE : 64KB RAM dump à cycle précis
- [x] Hooks dans handle_irq + RTI opcode (cpu6502.c + opcodes.c)
- [x] Champ void* irq_trace_fp dans cpu6502_t (sans <stdio.h> dans header)
- [x] CLI parsing + cleanup + help text
- [x] Validé : 284 IRQ tracés sur asteroids.tap Atmos en 5.5M cycles
- [x] 327 tests pass

### Sprint 34n: Rendu scanline-par-scanline ULA hardware-accurate (v1.16.13-alpha) - DONE
- [x] Diagnostic : asteroids.tap noir sur Atmos, OK Oric-1 (et OK sur Oricutron)
- [x] Cause racine : rendu frame-end de Phosphoric vs scanline d'Oricutron
- [x] Pendant hires_init asteroids, attributs résiduels charset Atmos
      ($1A,$1C,$1E,$18) en zone HIRES → vid_mode oscille → finit TEXT
- [x] Refactor video.c : video_render_scanline(vid, mem, y) standalone
- [x] Refactor main.c inner loop : appel render_scanline tous les 64 cycles
- [x] vid_mode persistant intra-scanline ; switch read-source mid-line si
      attribut flip vid_mode (HIRES↔TEXT base)
- [x] Validé : asteroids.tap Atmos affiche maintenant ASTEROIDS PRESS SPACE
- [x] Pas de régression Oric-1 ; bottom text rows (200-223) toujours OK
- [x] 327 tests pass

### Sprint 34m: Fix Mutter « ne répond pas » Wayland (v1.16.12-alpha) - DONE
- [x] Diagnostic : Phosphoric appairait comme « ne répond pas » sur Wayland
      dès que la fenêtre restait statique (cas asteroids.tap loopant sur
      frame_wait() après 1.16.11)
- [x] Cause racine : SDL_RENDERER_PRESENTVSYNC dans renderer.c:28 pouvait
      bloquer SDL_RenderPresent indéfiniment sur compositeurs Wayland avec
      fenêtre occluse → SDL_PollEvent jamais appelé → _NET_WM_PING jamais
      répondu → Mutter timeout (~5 sec)
- [x] Fix : suppression du flag, SDL_Delay(20-elapsed) assure 50 Hz seule
- [x] Validé : utilisateur confirme « cela fonctionne »
- [x] 327 tests pass

### Sprint 34l: Hardware fidelity CB1 ≠ VSync (v1.16.11-alpha) - DONE
- [x] Diagnostic divergence asteroids.tap Oricutron vs Phosphoric
- [x] Cause racine : Phosphoric pulsait CB1 à chaque trame, mais le vrai
      Oric ne câble PAS CB1 au signal VSync de l'ULA
- [x] Suppression du trigger CB1 dans la boucle frame (src/main.c:852-866)
- [x] Constantes VSYNC_CYCLE/VSYNC_START_LINE conservées (documentation
      timing PAL uniquement, ne pilotent plus aucune broche)
- [x] Commentaires cb1_pin mis à jour (« idle on Oric »)
- [x] Test confirmation : asteroids.tap loope sur $0D8E A=$40 Y=$0E,
      identique à la capture Oricutron — divergence éliminée
- [x] Note technique livrée à l'équipe asteroids
      (docs/notes/2026-05-13-asteroids-vsync-cb1.md)
- [x] 327 tests pass

## Future Enhancements

### Advanced Features
- [x] ORIC Atmos support
- [ ] Telestrat support
- [x] Save states
- [x] Joystick support
- [x] Printer emulation (text + MCP-40 plotter)
- [x] Display scaling (x1, x2, x3, x4)
- [x] Trace logging
- [x] Serial interface (ACIA 6551, RS232, Minitel/V23)
- [x] Digitelec DTL 2000 (faithful PIA 6821 + ACIA 6850 at $03F8-$03FD, OCR-verified registers, IRQ wired, cassette-validated, TX/RX serial trace, OTRM 2.00 boot-validated)
- [x] PicoWiFiModemUSB (modem WiFi LOCI, jeu AT v0.1.0)

### Hors périmètre « fidèle au firmware » (extensions assumées)

Fonctionnalités utiles MAIS qui n'existent PAS sur le vrai matériel.
À n'implémenter que comme options explicitement non-fidèles, jamais
activées par défaut, clairement signalées dans l'aide CLI.

- [ ] **Proxy TLS terminant pour PicoWiFi** (HTTPS pour l'Oric) : le vrai
      PicoWiFiModemUSB est un pont TCP transparent — il ne termine PAS TLS
      (`ATGET` = HTTP seulement, `ATDT:443` = octets bruts). Un 6502 à 1 MHz
      ne peut pas faire un handshake TLS. Piste : option `--serial-tls-proxy`
      où l'émulateur (hôte) déchiffre TLS et présente du clair à l'Oric.
      C'est une capacité AU-DELÀ du firmware réel → divergence assumée,
      séparée du backend `picowifi` fidèle. Priorité basse.

### Developer Tools
- [x] Memory viewer (debugger: m command)
- [x] Register inspector (debugger: r command)
- [x] Performance profiler
- [x] ROM analysis tools

---

## Success Metrics

### Technical
- [x] 100% CPU instruction compatibility (256/256 opcodes : 151 officiels + 105 illégaux NMOS)
- [x] Cycle-accurate timing
- [x] 100% game compatibility (41/41 programs tested: 26 TAP + 11 DSK + 4 demos)
- [x] Code coverage measured: 72.2% global, 78.9% excluding interactive REPL
- [x] <5% CPU overhead on modern hardware — mesuré sprint 36a : ~1% overhead réel (201 µs / 20000 µs frame budget, basic-idle). Voir `make bench`.

### Quality
- [x] 330 tests passing (make tests)
- [x] Zero critical bugs in test suite
- [x] API documentation complete
- [x] Comprehensive user guide

---

**Last Updated**: 2026-07-04
**Current Version**: 1.50.0-alpha
**Next Milestone**: 2.0.0 (Telestrat / stable release)
